# BÁO CÁO THỰC TẬP NGÀY 5/5

## I. Xu HƯớng hiện Nay

1. Serverless
Serverless là gì?

Serverless là loại mô hình tính toán đám mây cơ bản được thiết lập dựa trên nhà cung cấp dịch vụ đám mây, quản lý tài nguyên máy chủ ở mức tối thiểu. Trong mô hình này, người dùng chỉ cần tập trung vào việc phát triển
 mã và ứng dụng của họ mà không cần quan tâm đến việc quản lý cơ sở hạ tầng.

Quá trình hoạt động của Serverless được xem là không có máy chủ. Thay vào đó, người dùng vẫn sử dụng các tài nguyên máy chủ nhưng được quản lý hoàn toàn bởi nhà cung cấp dịch vụ đám mây.

Mô hình Serverless mang lại nhiều lợi ích như giảm chi phí vận hành, mở rộng và thu hẹp linh hoạt, giảm áp lực quản lý hạ tầng. Tuy nhiên, người dùng cũng nên xem xét các điểm hạn chế về thời gian chạy hàm, quản lý tài nguyên phức tạp và sự khó khăn trong việc điều chỉnh hiệu suất ứng dụng.

Ưu và nhược điểm của Serverless



Ưu điểm

Chi phí linh hoạt: Serverless cho phép người dùng chỉ trả tiền cho công việc tính toán thực sự được sử dụng, do đó giảm thiểu lãng phí và tối ưu hóa chi phí hoạt động.
Linh hoạt trong việc mở rộng và thu nhỏ: Với Serverless, người dùng có thể tự động mở rộng hoặc thu gọn tài nguyên tính toán theo yêu cầu của ứng dụng mà không cần phải mài thủ công.
Quản lý hạ tầng đơn giản: Người dùng không cần lo lắng về quản lý cơ sở hạ tầng máy chủ, nhà cung cấp dịch vụ đám mây sẽ đảm bảo nhận được phần lớn công việc này, giúp tiết kiệm thời gian và tài nguyên.
Tự động co giãn: Hệ thống Serverless có khả năng tự động co giãn để đáp ứng nhu cầu khi tải cao mà không cần thiết kế thủ công từ người quản trị.
Giảm thiểu thời gian phát triển và phát triển khai: Bởi vì người dùng chỉ cần tập trung vào phát triển khai mã và ứng dụng mà không cần quan tâm đến quản lý cơ sở hạ tầng máy chủ nên họ có thể tập trung nhiều hơn nữa vào việc phát triển năng lực, giá trị sản phẩm.

Nhược điểm
Phân tích tập trung về chế độ

Chế độ chạy hàm thời gian: Các Serverless dịch vụ thường có giới hạn về thời gian chạy hàm, giới hạn số lượng tài nguyên có sẵn cho mỗi hàm và thời gian thực thi. Điều này có thể ảnh hưởng đến các ứng dụng được yêu cầu thực hiện trong thời gian dài hoặc cần liên tục.
Quản lý tài nguyên phức tạp: Mặc dù Serverless giảm áp lực về quản lý hạ tầng cho người dùng nhưng việc quản lý và theo dõi tài nguyên Serverless thường trở nên phức tạp, đặc biệt khi phát triển ứng dụng lớn.
Khó khăn trong việc điều chỉnh ứng dụng hiệu suất: Đối với một số ứng dụng, quá trình tối ưu hóa hiệu suất có thể trở nên phức tạp hơn do cách máy chủ và tài nguyên được quản lý bởi nhà cung cấp dịch vụ dịch vụ đám mây.
Khó khăn trong công việc kiểm tra và gỡ lỗi: Khi phát triển ứng dụng Serverless, hoạt động kiểm tra và gỡ lỗi thường có thể trở nên khó khăn hơn so với việc phát triển khai báo trên cơ sở hạ tầng máy chủ lưu thông.
Việc phát tài nguyên cấp độ không được mong đợi trước đó: Mặc dù việc tự động co giãn lại hoạt động nhưng các tài nguyên được phát hiện không được mong đợi trước đó, điều này thường gây khó khăn trong việc quản lý hiệu suất và báo cáo chi phí.

Xác định thời điểm cần sử dụng Serverless
Trên thực tế có rất nhiều trường hợp sử dụng Serverless mang đến lợi ích vượt trội cho người thực hiện. Dưới đây là những vấn đề Serverless cơ bản mà bạn nên chú ý để tận dụng mô hình này:


Ứng dụng tải công việc không đều
Khi ứng dụng của bạn tải công việc không đều hoặc tải cao tập trung vào một số thời điểm cụ thể, Serverless có tác dụng tự động co giãn để đáp ứng nhu cầu mà không cần dùng đến các công cụ thủ công.

Yêu cầu vận dụng tài nguyên tính toán
Trong trường hợp ứng dụng của bạn yêu cầu khả năng mở rộng và thu gọn tài nguyên một cách linh hoạt,  theo nhu cầu cụ thể thì Serverless chính là một trong những lựa chọn tốt nhất thực hiện điều này. Công nghệ làm chi phí tính toán và tự động giãn nở theo điều kiện.

Phát triển các tính năng mới nhanh chóng
Serverless giúp giảm thiểu thời gian phát triển và phát triển khai báo, do đó mà nó rất thích hợp cho việc phát triển các tính năng mới. Đặc biệt là khi bạn cần có những tính năng nhanh chóng, tăng tốc quá trình phát triển sản phẩm.

Ứng dụng cần xử lý rút gọn và đáp ứng nhanh
Các trường hợp sử dụng Serverless rất phù hợp cho việc thực thi chức năng hoặc dịch vụ vi mô đơn giản, nhanh chóng mà không cần phải quản lý cơ sở hạ tầng máy chủ.

Ưu tiên chi phí và tối ưu hóa hiệu suất
Nếu việc tối ưu hóa hiệu suất và giảm thiểu chi phí vận hành được ưu tiên hàng đầu thì Serverless sẽ là giải pháp tốt cho bạn. Người dùng có thể chủ động tính toán mức chi phí tính linh hoạt và tiết kiệm hơn.

Những nhà cung cấp dịch vụ nổi tiếng 
AWS Lambda

Dưới đây là bản tóm tắt cực kỳ gọn gàng và "đánh trúng trọng tâm" về AWS Lambda từ bài viết dài ở trên, được hệ thống lại để bạn dễ nắm bắt nhất, đặc biệt là khi tiếp cận dưới góc độ quản trị hệ thống và phát triển backend:

### 1. Bản chất AWS Lambda là gì?
AWS Lambda là dịch vụ điện toán đám mây theo mô hình **Serverless** (không máy chủ) của Amazon. 
* Thay vì phải thuê và quản lý nguyên một con server (như EC2) để chạy app, bạn chỉ việc tải đoạn code của mình lên.
* Nền tảng sẽ tự động kích hoạt code chạy khi có sự kiện (trigger) và bạn **chỉ phải trả tiền cho từng mili-giây** code thực thi. Lúc code nằm im, hóa đơn bằng 0.



### 2. Cơ chế hoạt động (Góc nhìn quản trị)
* **Cô lập bằng Container:** Mỗi khi được gọi, code của bạn sẽ được AWS đưa vào chạy trong một container độc lập (dùng Amazon Linux).
* **Tự động hóa hoàn toàn:** AWS lo toàn bộ từ A-Z việc cấp phát CPU, RAM, bảo mật, vá lỗi OS, đến phòng chống xung đột mạng. Bạn được giải phóng hoàn toàn khỏi các tác vụ vận hành (Operations) truyền thống.
* **Scale cực bạo:** Gọi 1 request hay 100,000 requests cùng lúc, hệ thống sẽ tự động nhân bản các container để xử lý song song mà không sợ bị "nghẽn".

### 3. Thông số kỹ thuật & Môi trường cốt lõi
* **Ngôn ngữ quen thuộc:** Hỗ trợ native cho các ngôn ngữ phổ biến như **Python (2.7 đến 3.8)**, Node.js, Java, Go, C#... 
* **Giới hạn thời gian (Timeout):** Tối đa **15 phút** cho mỗi lần chạy (Không phù hợp để cắm các script chạy nền liên tục nhiều giờ).
* **Cấu hình:** Có thể kéo RAM lên tới 10GB (CPU sẽ tự động tăng theo tỷ lệ RAM).
* **Dung lượng code:** Gói code tối đa 50MB (nén zip) hoặc 250MB (chưa nén).

### 4. Dùng để làm gì là ngon nhất?
* **Xây dựng Backend cho Web/App:** Viết các logic xử lý dữ liệu (ví dụ bằng Python/Flask) kết hợp với API Gateway để tạo thành một hệ thống backend tự động mở rộng theo lượng người dùng.
* **Xử lý tự động (Event-driven):** Tự động bóp dung lượng ảnh ngay khi có người tải lên S3, hoặc xử lý luồng dữ liệu (Stream) từ thiết bị IoT gửi về.



AWS Lambda là một dịch vụ tính toán của Amazon Web Services (AWS) cho phép bạn chạy mã mà không cần quản lý máy chủ. Đây là một dịch vụ tính toán không có máy chủ mà bạn chỉ cần trả tiền cho thời gian thực mã hóa của bạn được thực thi, thay vì phải trả tiền cho công việc duy trì liên tục máy chủ.

Các chức năng trong AWS Lambda được kích hoạt bởi những sự kiện từ nhiều nguồn khác nhau như S3, SNS, API Gateway và nhiều dịch vụ khác của AWS. Bạn có thể sử dụng ngôn ngữ lập trình phổ biến như Node.js, Python, Java, C# và Go để viết hàm của mình.




Lợi ích của việc sử dụng AWS Lambda bao gồm:

Chi phí linh hoạt: Bạn chỉ trả tiền cho thời gian thực mã hóa của bạn được chạy, giúp tối ưu hóa chi phí và giảm lãng phí cho những thời gian máy chủ không được sử dụng.
Tự động co giãn: AWS Lambda tự động co giãn để đáp ứng lưu lượng công việc, không cần áp dụng các giải pháp thủ công từ bạn.
Không cần quản lý máy chủ: Bạn không cần phải quản lý cơ sở hạ tầng máy chủ, điều này giúp tập trung phát triển ứng dụng của bạn mà không cần lo lắng về việc quản lý máy chủ.
Tích hợp nền tảng: AWS Lambda tích hợp rất tốt với các dịch vụ khác trong hệ thống sinh thái của AWS, giúp bạn xây dựng các ứng dụng phức tạp và mạnh mẽ.



Google Cloud Function
Để bạn có một cái nhìn đối sánh trực quan và chi tiết nhất, mình sẽ giữ nguyên cấu trúc các tiêu đề từ bài viết AWS Lambda của bạn và "dịch" toàn bộ hệ quy chiếu sang **Google Cloud Functions (GCF)** nhé.

Dưới đây là bức tranh toàn cảnh về GCF:

### Google Cloud Functions (GCF) là gì?
Google Cloud Functions là một dịch vụ điện toán serverless (không máy chủ) cho phép các lập trình viên chạy mã code (hàm) để phản hồi lại các sự kiện trên nền tảng đám mây mà không cần phải quản lý hạ tầng hay máy chủ.

Tương tự như Lambda, GCF thực hiện tất cả công việc quản trị tài nguyên điện toán. Bạn chỉ việc viết một đoạn code (đơn mục đích) bằng ngôn ngữ quen thuộc, tải lên GCP, và Google sẽ tự động lo việc cấp phát tài nguyên, chạy code, mở rộng quy mô (scale) và ghi log. Dịch vụ này chỉ chạy khi có một "sự kiện" (trigger) gọi nó thức dậy, và bạn chỉ trả tiền cho chính xác lượng thời gian tính toán mà code chạy tới từng phần trăm giây.

### Google Cloud Functions hoạt động như thế nào?
GCF hoạt động dựa trên cơ chế phản hồi sự kiện (Event-driven). Đoạn code của bạn sẽ được kích hoạt bởi các trigger như: một request HTTP gửi đến (truy cập web/API), một file mới được upload lên Cloud Storage, hoặc một thông báo từ hệ thống nhắn tin Pub/Sub.

Khi một sự kiện xảy ra, Google Cloud sẽ tạo ra một môi trường thực thi an toàn (container) để chạy code của bạn. Điểm đặc biệt của GCF (đặc biệt là thế hệ thứ 2 - Cloud Functions gen 2) là nó được xây dựng trên nền tảng Cloud Run và Knative.
* Nếu không có ai truy cập, số lượng container bằng 0.
* Nếu có hàng nghìn request đổ về cùng lúc, hệ thống tự động nhân bản (scale out) ra hàng nghìn container tương ứng để xử lý song song. 
* Khi xử lý xong, hệ thống sẽ tự động dọn dẹp các container này để giải phóng tài nguyên.

### Vì sao Google Cloud Functions giúp tiết kiệm chi phí khi sử dụng?
Khác với việc thuê một máy ảo (Compute Engine) phải trả tiền 24/7 kể cả khi máy chủ chạy không tải, với GCF bạn chỉ bị tính phí theo **số lượng lượt gọi (invocations)** và **thời gian thực thi tính bằng mili-giây**. 

Nếu một ngày hệ thống của bạn không có luồng dữ liệu nào chạy qua, hóa đơn của bạn ngày hôm đó là 0 đồng. Hơn nữa, Google cung cấp một Bậc miễn phí (Free Tier) cực kỳ lớn (2 triệu lượt gọi mỗi tháng), giúp việc vận hành các ứng dụng quy mô nhỏ hoặc làm môi trường dev/demo gần như không tốn một xu.

### Các tính năng chính của Google Cloud Functions
* **Tạo Endpoint HTTP tức thì:** Ngay khi tạo hàm, GCF cung cấp sẵn cho bạn một đường link URL (HTTPS) mà không cần phải cấu hình qua một dịch vụ API Gateway lằng nhằng nào khác.
* **Môi trường lập trình thân thiện:** Bạn có thể bê nguyên các logic code viết bằng framework web quen thuộc như Flask vào thẳng GCF để xử lý request mà không cần thay đổi quá nhiều cấu trúc thư mục.
* **Tích hợp sâu với hệ sinh thái Google:** Dễ dàng kết nối và tương tác với cơ sở dữ liệu Firebase/Firestore, BigQuery, hoặc Google Cloud Storage chỉ với vài dòng lệnh cấp quyền.
* **Functions Framework mã nguồn mở:** Google cung cấp bộ thư viện mã nguồn mở giúp bạn có thể chạy thử mã GCF ngay trên máy tính cá nhân (localhost) trước khi đẩy lên cloud, giúp quá trình debug dễ dàng hơn rất nhiều.
* **Xử lý đồng thời (Concurrency - trên Gen 2):** Khác với thế hệ cũ mỗi container chỉ xử lý 1 request, GCF Gen 2 cho phép một container xử lý đồng thời lên tới 1000 requests, giúp giảm mạnh độ trễ và tiết kiệm chi phí.

### Top 5 ứng dụng phổ biến của Google Cloud Functions
1. **Xây dựng API/Backend Serverless:** Xử lý các luồng dữ liệu từ Frontend web hoặc ứng dụng di động gửi lên một cách nhanh chóng với các endpoint HTTP độc lập.
2. **Xử lý file tự động:** Tự động tạo ảnh thumbnail, đóng logo (watermark) hoặc chuyển đổi định dạng video ngay khi người dùng vừa upload file lên Google Cloud Storage.
3. **Webhook cho ứng dụng bên thứ 3:** Nhận và xử lý dữ liệu tự động từ các dịch vụ như GitHub, Stripe (thanh toán), hoặc hệ thống quản lý ERP nội bộ.
4. **Backend cho hệ thống IoT:** Thu thập, lọc và chuyển đổi dữ liệu đo đạc (telemetry) từ hàng ngàn thiết bị cảm biến gửi về qua Cloud Pub/Sub trước khi lưu vào cơ sở dữ liệu.
5. **Tự động hóa hệ thống (Cron jobs):** Kết hợp với Cloud Scheduler để dọn dẹp database, sao lưu hệ thống, hoặc gửi email báo cáo tự động vào đúng 12h đêm mỗi ngày.

### Google Cloud Functions hỗ trợ những ngôn ngữ lập trình nào?
Google liên tục cập nhật các môi trường chạy (runtime) để hỗ trợ các phiên bản mới nhất của các ngôn ngữ phổ biến:
* Node.js
* Python (3.7 đến 3.12 - hỗ trợ cực tốt cho các thư viện data và web framework)
* Go
* Java
* Ruby
* PHP
* .NET Core

### Những thông số kỹ thuật cơ bản trong Google Cloud Functions (Tham khảo Gen 2)
* **Thời gian chờ tối đa (Timeout):** Có thể cấu hình lên tới **60 phút** cho các request HTTP (rất dài so với con số 15 phút của AWS Lambda).
* **Cấu hình bộ nhớ (RAM):** Tùy chỉnh linh hoạt từ 128 MB lên đến tối đa **32 GB**.
* **CPU:** Tự động được cấp phát tỷ lệ thuận với lượng RAM bạn chọn (lên tới tối đa 8 vCPU).
* **Kích thước mã nguồn:** Tối đa 500 MB cho mã nguồn chưa nén (bao gồm cả các thư viện đi kèm).

### Ưu điểm của Google Cloud Functions
* **Trải nghiệm lập trình viên (DX) tuyệt vời:** Giao diện trực quan, dễ cấu hình, thiết lập "mì ăn liền". Bạn có thể có một API chạy thực tế chỉ trong chưa tới 3 phút.
* **Chi phí tối ưu:** Mô hình pay-as-you-go kết hợp bậc miễn phí trọn đời siêu hào phóng.
* **Bảo trì bằng 0:** Hoàn toàn rảnh tay khỏi việc vá lỗi hệ điều hành, cấu hình mạng lõi hay bảo trì máy chủ Linux định kỳ.
* **Tự động quy mô:** Chịu tải tốt từ 0 lên hàng chục nghìn lượt truy cập một cách mượt mà.

### Hạn chế của Google Cloud Functions
* **Cold Start (Độ trễ khởi động):** Giống như mọi hệ thống serverless, nếu một hàm lâu không được gọi, lần kích hoạt tiếp theo sẽ mất vài giây để hệ thống "khởi động lại" container, gây ra độ trễ nhỏ. (Có thể khắc phục bằng cách cấu hình số lượng *Minimum instances*, nhưng sẽ tốn phí duy trì).
* **Khó can thiệp sâu vào tầng OS:** Môi trường bị khóa cứng (lock-down). Nếu bạn cần cài đặt các gói phần mềm hệ thống (system packages) phức tạp ở tầng hệ điều hành, serverless nói chung không phải lựa chọn tối ưu.

### Hướng dẫn cách tạo hàm Google Cloud Functions
Bạn có thể tạo hàm trực tiếp qua giao diện Google Cloud Console vô cùng trực quan:
1. Đăng nhập vào Google Cloud Console, tìm kiếm và chọn dịch vụ **Cloud Functions**.
2. Nhấn nút **Create Function**.
3. Cấu hình cơ bản: Đặt tên cho hàm, chọn môi trường vùng (Region), và chọn Trigger là **HTTP** (để có URL).
4. Ở phần xác thực, có thể chọn *Allow unauthenticated invocations* nếu muốn public API để demo.
5. Chuyển sang bước viết code: Chọn Runtime (ví dụ: Python), trình chỉnh sửa code trực tuyến sẽ hiện ra. Bạn có thể dán trực tiếp đoạn code logic của mình vào đây.
6. Nhấn **Deploy** và chờ khoảng 1-2 phút. Xong! Bạn đã có một API hoàn chỉnh.

Một số lợi ích khi sử dụng Google Cloud Functions bao gồm:

Tự động co giãn: Dịch vụ có thể tự động co giãn để đáp ứng tải công việc, giúp bạn không cần phải lo lắng về việc quản lý tài nguyên.
Thời gian chạy chính xá: Bạn chỉ trả tiền cho thời gian thực mã hóa của bạn, giúp tối ưu hóa chi phí.
Tích hợp mạnh mẽ: Google Cloud Functions tích hợp tốt với các dịch vụ khác trong Google Cloud Platform như Google Cloud Storage, Firebase và nhiều dịch vụ khác giúp bạn xây dựng các ứng dụng phức tạp và mạnh mẽ.
Sử dụng nhiều trình cài đặt ngôn ngữ: Bạn có thể viết các chức năng của mình bằng các ngôn ngữ phổ biến như Node.js, Python, Go và Ruby.

Đã rõ yêu cầu của bạn. Dưới đây là phân tích chi tiết về công nghệ Container hóa và nền tảng Docker dưới góc độ học thuật và kiến trúc hệ thống.

---

### Phần 1: Khái quát về công nghệ Container hóa (Containerization)

**1. Định nghĩa**
Container hóa (Containerization) là một phương pháp ảo hóa cấp hệ điều hành (Operating-System-Level Virtualization). Kỹ thuật này cho phép triển khai nhiều môi trường thực thi người dùng (user-space instances) độc lập và biệt lập trên cùng một nhân hệ điều hành (OS Kernel) dùng chung. Các môi trường biệt lập này được gọi là các containers.

**2. Kiến trúc kỹ thuật cốt lõi**
Trong môi trường hệ điều hành Linux, công nghệ container hóa hoạt động dựa trên hai tính năng cốt lõi của Linux kernel:
*   **Namespaces:** Cung cấp cơ chế cô lập không gian làm việc (isolation). Mỗi container được cấp phát một namespace riêng biệt cho các tiến trình (PID), mạng (Network), điểm gắn kết tệp tin (Mount), và giao tiếp liên tiến trình (IPC). Điều này đảm bảo tiến trình trong container này không thể can thiệp vào tài nguyên của container khác.
*   **Control Groups (cgroups):** Cung cấp cơ chế phân bổ và giới hạn tài nguyên phần cứng. Cgroups kiểm soát và giới hạn lượng CPU, RAM, I/O disk mà mỗi tiến trình (hoặc nhóm tiến trình) trong container được phép tiêu thụ, ngăn chặn hiện tượng cạn kiệt tài nguyên hệ thống (resource starvation).



**3. Đối sánh với Ảo hóa phần cứng (Virtual Machine - VM)**
*   **Virtual Machine:** Hoạt động dựa trên một lớp phần mềm trung gian gọi là Hypervisor (trình quản lý máy ảo). Hypervisor ảo hóa toàn bộ phần cứng vật lý, yêu cầu mỗi máy ảo phải chạy một hệ điều hành khách (Guest OS) hoàn chỉnh. Điều này dẫn đến sự dư thừa tài nguyên (overhead) đáng kể.
*   **Container:** Không sử dụng Hypervisor và không chứa Guest OS. Các container chia sẻ chung nhân hệ điều hành của máy chủ (Host OS) và chỉ chứa các tệp nhị phân (binaries) cùng thư viện (libraries) cần thiết cho ứng dụng. Do đó, kích thước container rất nhỏ và thời gian khởi động gần như tức thời.

---

### Phần 2: Nền tảng Docker (Docker Platform)

**1. Khái niệm**
Docker là một nền tảng phần mềm mã nguồn mở tự động hóa việc triển khai các ứng dụng dưới dạng các container di động và tự động chứa (self-sufficient containers). Docker trừu tượng hóa sự phức tạp của các tính năng kernel (namespaces, cgroups) thông qua một công cụ quản lý cấp cao.

**2. Kiến trúc của Docker**
Docker sử dụng kiến trúc Client-Server, bao gồm các thành phần:
*   **Docker Daemon (`dockerd`):** Tiến trình chạy nền trên máy chủ, chịu trách nhiệm lắng nghe các yêu cầu từ Docker API và quản lý các đối tượng Docker (Image, Container, Network, Volume).
*   **Docker Client (`docker`):** Giao diện dòng lệnh (CLI) cho phép người dùng giao tiếp với Docker Daemon thông qua REST API.
*   **Docker Registry:** Kho lưu trữ tập trung chuyên phân phối các Docker Image (ví dụ: Docker Hub).

**3. Các thực thể cốt lõi trong Docker**
*   **Dockerfile:** Một tệp văn bản chứa các chỉ thị cấu hình (instructions) để tự động hóa quá trình đóng gói một Image.
*   **Docker Image:** Một mẫu (template) chỉ đọc, bao gồm nhiều lớp (layers) xếp chồng lên nhau. Nó chứa mã nguồn ứng dụng, môi trường thực thi (runtime), thư viện và các biến môi trường cần thiết.
*   **Docker Container:** Một thực thể đang thực thi (running instance) của một Docker Image. Nó có tính chất tạm thời (ephemeral) – mọi dữ liệu ghi trực tiếp vào lớp ghi (writable layer) của container sẽ bị mất khi container bị xóa.
*   **Docker Volume:** Cơ chế duy trì dữ liệu (data persistence) độc lập với vòng đời của container. Volumes cho phép gắn kết một không gian lưu trữ từ Host OS vào bên trong container.

---

### Phần 3: Ứng dụng thực tiễn - Triển khai kiến trúc Web Server bằng Docker

Việc triển khai các ngăn xếp máy chủ web truyền thống (như LAMP/LEMP) trên một máy chủ vật lý hoặc máy ảo nguyên khối (Monolithic) thường dẫn đến hiện tượng xung đột thư viện (dependency hell) và khó khăn trong việc nhân bản môi trường để mở rộng quy mô.

Khi áp dụng Docker, kiến trúc Web Server được thiết kế theo nguyên lý vi dịch vụ (Microservices), tuân thủ quy tắc "Một tiến trình trên mỗi vùng chứa" (One process per container).

**1. Phân rã kiến trúc (Decoupled Architecture)**
Một kiến trúc Web Server tiêu chuẩn sử dụng Python framework (như Flask hoặc Django) sẽ được phân tách thành các container biệt lập:
*   **Container 1 (Reverse Proxy/Web Server):** Chạy Nginx hoặc Apache. Xử lý các yêu cầu HTTP/HTTPS, định tuyến (routing) tĩnh, và chuyển tiếp các yêu cầu động (reverse proxying) đến Application Server.
*   **Container 2 (Application Server):** Môi trường thực thi mã nguồn. Chứa Python runtime, các gói phụ thuộc ảo (virtual environment requirements), và ứng dụng web (Flask/Django) hoạt động thông qua một giao diện chuẩn như WSGI (Gunicorn).
*   **Container 3 (Database Server):** Chạy hệ quản trị cơ sở dữ liệu (ví dụ: SQLite, MySQL, PostgreSQL). Sử dụng Docker Volume để đảm bảo tính toàn vẹn và duy trì dữ liệu khi container khởi động lại.

**2. Quản lý vòng đời với Docker Compose**
Để điều phối việc triển khai đa vùng chứa (multi-container deployment), các kỹ sư hệ thống sử dụng công cụ `docker-compose`. Công cụ này cho phép khai báo toàn bộ cơ sở hạ tầng dưới dạng mã (Infrastructure as Code - IaC) thông qua tệp định dạng YAML (`docker-compose.yml`).



Tệp YAML sẽ định nghĩa cấu trúc liên kết mạng (Network topology) ảo nội bộ, đảm bảo Nginx, Application Server và Database có thể giao tiếp với nhau bằng phân giải tên miền nội bộ (DNS resolution) mà không làm lộ các cổng dịch vụ không cần thiết ra môi trường mạng bên ngoài, từ đó gia tăng tính bảo mật cho hệ thống.



### 1. Khái niệm và Nguyên lý hoạt động
**Điện toán biên (Edge Computing)** là một mô hình điện toán phân tán được thiết kế để dịch chuyển khả năng tính toán và lưu trữ dữ liệu đến gần nhất với nguồn phát sinh dữ liệu (thiết bị đầu cuối). 

*   **Nguồn gốc:** Phát triển từ mạng phân phối nội dung (CDN) những năm 1990.
*   **Nguyên lý cốt lõi:** Thay vì truyền tải toàn bộ dữ liệu thô qua mạng về một trung tâm dữ liệu đám mây (Cloud Server) tập trung, quy trình phân tích và xử lý dữ liệu được thực hiện ngay tại chỗ (hoặc ở khoảng cách vật lý rất gần). Điều này giải quyết bài toán nút thắt cổ chai về băng thông và triệt tiêu độ trễ mạng trong mô hình điện toán truyền thống.

### 2. Thành phần cấu trúc hệ sinh thái
Hệ thống điện toán biên là sự phối hợp của 5 thành phần hạ tầng vật lý và logic:
*   **Thiết bị biên (Edge Device):** Các thiết bị trực tiếp thu thập dữ liệu (cảm biến IoT, camera, máy ATM). Năng lực tính toán của chúng thường hạn chế, chủ yếu phục vụ các phản hồi theo thời gian thực.
*   **Cổng biên (Edge Gateway):** Nút giao tiếp giữa thiết bị biên và đám mây. Đảm nhiệm việc chuyển đổi giao thức (protocol translation), tường lửa bảo mật và lọc dữ liệu thô trước khi truyền tải.
*   **Máy chủ biên (Edge Server):** Hệ thống máy chủ cấu hình cao (thường từ 8-16 core CPU, >16GB RAM) đặt tại các cơ sở cục bộ (nhà máy, chi nhánh). Đảm nhiệm việc chạy các ứng dụng doanh nghiệp và phân tích dữ liệu chuyên sâu tại chỗ.
*   **Nút biên (Edge Node):** Thuật ngữ logic chỉ chung cho các điểm xử lý trung gian (bao gồm cả thiết bị biên, cổng biên và máy chủ biên).
*   **Cloud Server (Đám mây trung tâm):** Nơi tiếp nhận các dữ liệu đã được tinh lọc từ tầng biên để lưu trữ dài hạn, hoặc thực hiện các tác vụ tính toán quy mô lớn (huấn luyện mô hình AI, phân tích Big Data).

### 3. Mô hình kiến trúc phân tầng (Tiered Architecture)
Kiến trúc của Edge Computing đóng vai trò là lớp trung gian kết nối giữa vạn vật (IoT) và Đám mây (Cloud), chia thành 3 tầng chức năng:
*   **Tầng thiết bị (Device Layer):** Điểm gốc tạo ra khối lượng dữ liệu khổng lồ liên tục.
*   **Tầng biên (Edge Layer):** Cung cấp tài nguyên tính toán cục bộ. Các tác vụ yêu cầu độ trễ cực thấp (như nhận diện khuôn mặt, điều hướng phương tiện tự hành) buộc phải hoàn tất ở tầng này.
*   **Tầng đám mây (Cloud Layer):** Xử lý các luồng công việc không đòi hỏi tính tức thời, cung cấp tài nguyên điện toán quy mô toàn cầu.



### 4. Đánh giá Ưu điểm và Rào cản kỹ thuật
**Lợi ích chiến lược:**
*   **Tối ưu hóa độ trễ (Low Latency):** Cực kỳ quan trọng đối với các hệ thống thời gian thực (real-time) như xe tự hành hay y tế từ xa.
*   **Tối ưu chi phí viễn thông:** Giảm thiểu đáng kể tải lượng dữ liệu truyền dẫn (bandwidth) qua mạng diện rộng (WAN).
*   **An toàn thông tin và Quyền riêng tư:** Dữ liệu nhạy cảm được xử lý và làm ẩn danh tại nguồn, hỗ trợ các doanh nghiệp tuân thủ tiêu chuẩn bảo mật (ví dụ: GDPR).
*   **Tính sẵn sàng cao (High Availability):** Có khả năng hoạt động độc lập ngay cả khi kết nối đến máy chủ trung tâm bị gián đoạn (quan trọng đối với hàng hải, khai khoáng).

**Thách thức triển khai:**
*   **Giới hạn phần cứng:** Các thiết bị biên không thể sở hữu sức mạnh tính toán vô hạn như Cloud.
*   **Phân mảnh tiêu chuẩn:** Thiếu vắng các giao thức chuẩn hóa chung giữa các nhà cung cấp nền tảng.
*   **Quản trị vòng đời dữ liệu:** Thách thức trong việc định tuyến và quyết định dữ liệu nào cần xóa bỏ tại biên, dữ liệu nào cần đồng bộ lên đám mây.
*   **Rủi ro bảo mật phân tán:** Việc quản lý bảo mật vật lý và phần mềm cho hàng ngàn thiết bị IoT phân tán phức tạp hơn nhiều so với việc bảo vệ một trung tâm dữ liệu tập trung.

### 5. Định vị Edge Computing vs Fog Computing vs Cloud Computing
*   **Edge Computing (Điện toán biên):** Xử lý dữ liệu *trực tiếp trên hoặc rất sát* với thiết bị đầu cuối (ví dụ: bộ vi xử lý ngay bên trong camera giao thông).
*   **Fog Computing (Điện toán sương mù):** Hoạt động ở cấp độ mạng cục bộ (LAN). Dữ liệu từ nhiều thiết bị đầu cuối được đẩy về một trạm nút mạng (node) lân cận để xử lý trước khi lên Cloud.
*   **Cloud Computing (Điện toán đám mây):** Xử lý tập trung ở khoảng cách xa hàng nghìn kilomet, đáp ứng các bài toán tính toán cường độ cao.
<img width="600" height="337" alt="image" src="https://github.com/user-attachments/assets/776aef22-d8d6-4b3a-af4e-f9229e0f1c35" />

### 6. Xu hướng phát triển tương lai
*   **Tích hợp hạ tầng viễn thông mới:** Băng thông rộng và độ trễ thấp của **5G và Wi-Fi 6** sẽ là chất xúc tác mạnh mẽ nhất cho hệ sinh thái biên.
*   **Micro Modular Data Centers (MMDC):** Sự dịch chuyển sang việc triển khai các trung tâm dữ liệu vi mô, dạng mô-đun linh hoạt đặt ngay tại các khu đô thị hoặc nhà máy để cung cấp năng lực máy chủ biên.
*   **Hợp nhất tiêu chuẩn:** Các tập đoàn công nghệ lớn (như AWS, Verizon) đang đẩy mạnh thiết lập các tiêu chuẩn chung để tăng tính tương tác giữa các hệ thống.



# II. Thực Hành 

1. Cài đặt Cài đặt Apache/Nginx


Linux (Ubuntu/CentOS): Sử dụng trình quản lý gói apt hoặc dnf.

sudo apt update && sudo apt install nginx hoặc apache2.

<img width="1233" height="362" alt="image" src="https://github.com/user-attachments/assets/c9f2ff0d-911b-4e6d-99d2-2ff5022f077e" />


<img width="1536" height="375" alt="image" src="https://github.com/user-attachments/assets/bd53ca53-2e38-4665-8cd4-fd0f5b00ab4b" />




Bước 1: Chuẩn bị 2 Backend (Host website bằng Apache)
Đầu tiên, bạn cần cấu hình Apache lắng nghe ở 2 cổng 8081 và 8082.

1. Đổi cổng lắng nghe của Apache:
Mở file ports.conf:

Bash
sudo nano /etc/apache2/ports.conf
Đổi Listen 80 thành:

Apache
Listen 8081
Listen 8082

Tuyệt vời! Bây giờ chúng ta sẽ kết hợp cả 3 yêu cầu này thành một bài Lab hoàn chỉnh: Nginx đứng ngoài cùng làm Reverse Proxy và Load Balancer, phân phối tải cho 2 backend (Apache) đang host code HTML/PHP.

Để thấy rõ Load Balancer hoạt động, chúng ta sẽ giả lập 2 server backend chạy trên cùng một máy ảo nhưng ở 2 cổng khác nhau (Ví dụ: Apache chạy ở cổng 8081 và 8082).

Dưới đây là các bước thực hiện chi tiết:

Bước 1: Chuẩn bị 2 Backend (Host website bằng Apache)
Đầu tiên, bạn cần cấu hình Apache lắng nghe ở 2 cổng 8081 và 8082.

1. Đổi cổng lắng nghe của Apache:
Mở file ports.conf:

Bash
sudo nano /etc/apache2/ports.conf
Đổi Listen 80 thành:

Apache
Listen 8081
Listen 8082

<img width="830" height="506" alt="image" src="https://github.com/user-attachments/assets/e358c127-ee22-4a6b-aa60-b3258c17f5b3" />


2. Tạo source code web cho 2 backend:
Chúng ta sẽ tạo 2 file index.php khác nhau để dễ dàng nhận biết Nginx đang điều hướng request vào server nào.

Bash
# Tạo thư mục
sudo mkdir -p /var/www/backend1
sudo mkdir -p /var/www/backend2

# Tạo code cho Backend 1
echo "<h1>Day la Backend 1 (Port 8081)</h1> <?php echo 'Thoi gian: ' . date('H:i:s'); ?>" | sudo tee /var/www/backend1/index.php

# Tạo code cho Backend 2
echo "<h1>Day la Backend 2 (Port 8082)</h1> <?php echo 'Thoi gian: ' . date('H:i:s'); ?>" | sudo tee /


<img width="1667" height="280" alt="image" src="https://github.com/user-attachments/assets/8f1511e0-4289-4322-a940-adbf1258a089" />


3. Tạo Virtual Host cho Apache:
Mở file cấu hình default của Apache:

Bash
sudo nano /etc/apache2/sites-available/000-default.conf
Xóa nội dung cũ và thay bằng cấu hình sau:

Apache
<VirtualHost *:8081>
    DocumentRoot /var/www/backend1
</VirtualHost>

<VirtualHost *:8082>
    DocumentRoot /var/www/backend2
</VirtualHost>
Lưu lại và khởi động lại Apache:

<img width="1009" height="681" alt="image" src="https://github.com/user-attachments/assets/9c2ce4c0-8da7-439b-882b-159535117377" />


Bash
sudo systemctl restart apache2


Bước 2: Cấu hình Nginx làm Reverse Proxy & Load Balancer
Bây giờ chúng ta sẽ cho Nginx lắng nghe ở cổng 80 (cổng HTTP mặc định) và đẩy traffic về 8081 hoặc 8082.

1. Sửa file cấu hình Nginx:

Bash
sudo nano /etc/nginx/sites-available/default
2. Khai báo Load Balancer và Reverse Proxy:
Xóa cấu hình mặc định (hoặc comment lại) và thay bằng đoạn này:

Nginx
# Khai báo nhóm server backend để Load Balancing
upstream my_php_backends {
    # Nginx mặc định sẽ dùng thuật toán Round Robin (chia đều)
    server 127.0.0.1:8081; # Backend 1
    server 127.0.0.1:8082; # Backend 2
}

server {
    listen 80;
    server_name _; # Lắng nghe mọi IP/Domain

    location / {
        # Cấu hình Reverse Proxy đẩy traffic về nhóm upstream ở trên
        proxy_pass http://my_php_backends;
        
        # Chuyển tiếp các thông tin thực của client cho backend biết
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

<img width="1001" height="527" alt="image" src="https://github.com/user-attachments/assets/3ba38778-c1d5-4525-b74e-2976e7889989" />

3. Kiểm tra và khởi động lại Nginx:

Bash
sudo nginx -t
sudo systemctl restart nginx

<img width="932" height="141" alt="image" src="https://github.com/user-attachments/assets/63a9152b-6044-48d6-b73b-1bcb6528baf4" />

Bước 3: Kiểm tra thành quả
Bây giờ bạn hãy mở trình duyệt trên máy thật và truy cập vào địa chỉ IP của máy ảo Linux (hoặc chạy lệnh curl http://localhost trực tiếp trên Linux).

Hoặc curl liên tục vào để thấy các site được chuyển qua lại với nhau

<img width="1100" height="222" alt="image" src="https://github.com/user-attachments/assets/ab4494cb-04ab-4e4f-b22c-ca9d5632b0c1" />



Lần truy cập đầu tiên: Bạn sẽ thấy dòng chữ "Day la Backend 1 (Port 8081)".
<img width="901" height="486" alt="image" src="https://github.com/user-attachments/assets/71b628c5-3a34-4300-b1a5-461a6f2e8822" />

F5 (Refresh) lại trang: Bạn sẽ thấy dòng chữ "Day la Backend 2 (Port 8082)".
<img width="831" height="452" alt="image" src="https://github.com/user-attachments/assets/c04f7640-44ce-408f-b6ec-885bd7ceb1c2" />

F5 lần nữa: Nó lại quay về Backend 1.

Nginx đã đóng vai trò làm Reverse Proxy ở cổng 80 (nhận yêu cầu từ người dùng và giấu đi các server backend bên trong) đồng thời làm Load Balancer (chia đều tải cho 2 server Apache bằng thuật toán Round Robin mặc định).

Nếu muốn điều chỉnh tải trọng (ví dụ Backend 1 mạnh hơn Backend 2), bạn có thể thêm tham số weight vào khối upstream (ví dụ: server 127.0.0.1:8081 weight=3;).



5 Cấu hình caching cho Nginx



• Enable HTTP/2 trên Nginx
HTTP/2 là giao thức mới được phát triển để thay thế HTTP/1.x – hiện đang là mặc định cài đặt trên tất cả các webserver. HTTP/2 là phiên bản hoàn toàn mới không phải bản chỉnh sửa của HTTP/1.x. Nó nhanh hơn, đơn giản hơn và mạnh hơn HTTP/1.x.

Ưu điểm của HTTP/2:

Tăng hiệu suất vận hành của giao thức thông qua Giảm độ trễ khi phản hồi tới người dùng cuối (Clients) và Sử dụng tài nguyên mạng của máy chủ (Server Push).


Cấu hình Nginx để sử dụng HTTP/2
Bước 1: Chỉnh sửa cấu hình Nginx
Các bạn mở và chỉnh sửa tệp cấu hình Nginx tại/etc/nginx/nginx.conf hoặc /etc/nginx/sites-available/default

Thêm dòng sau vào tệp cấu hình Nginx của bạn:

listen 443 ssl http2;

<img width="962" height="365" alt="image" src="https://github.com/user-attachments/assets/46b025d2-5e7c-4015-8afc-639108dadf9c" />

Triển khai HTTPS với Let’s Encrypt.

Bước 1: Tạo chứng chỉ bằng OpenSSL
Chạy lệnh này để tạo một cặp khóa có thời hạn 365 ngày:

Bash
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/ssl/private/nginx-selfsigned.key -out /etc/ssl/certs/nginx-selfsigned.crt
(Lưu ý: Khi nó hỏi thông tin, bạn cứ nhấn Enter bỏ qua hết cho nhanh).

Bước 2: Cấu hình vào file default
Mở lại file /etc/nginx/sites-enabled/default và thêm hai dòng này vào trong khối server { ... }:

Nginx
server {
    listen 443 ssl http2;
    server_name localhost;

    # Trỏ đúng về hai file vừa tạo ở Bước 1
    ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
    ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

    # ... các cấu hình proxy_pass cũ của bạn
}

Bước 2: Kiểm tra cấu hình và khởi động lại Nginx
Sau khi thêm cấu hình HTTP/2, bạn cần kiểm tra cấu hình của Nginx bằng lệnh sau:

  
Đàm Trung Kiên
sudo nginx -t

    <img width="952" height="164" alt="image" src="https://github.com/user-attachments/assets/2aa8d97d-e13b-4e64-b390-cf887808e418" />

Nếu không có lỗi, bạn có thể khởi động lại Nginx để áp dụng cấu hình mới:

  
Đàm Trung Kiên
sudo service nginx restart
    

