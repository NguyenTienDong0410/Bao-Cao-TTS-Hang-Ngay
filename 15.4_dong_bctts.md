### BÁO CÁO THỰC TẬP NGÀY 15/4

## I.Monitor Promethues + Grafana	

**1. Grafana Là Gì?**
Grafana là một ứng dụng web mã nguồn mở nổi tiếng cho phép phân tích và trực quan hóa dữ liệu, thiết kế các bảng điều khiển tương tác.
Nó cho phép bạn truy vấn dữ liệu và tạo các biểu đồ, bảng điều khiển từ các nguồn dữ liệu được hỗ trợ, giúp bạn dễ dàng hiểu và phân tích dữ liệu.

Grafana Được Sử Dụng Để Làm Gì?
Grafana cho phép bạn tạo các bảng điều khiển tương tác và trực quan hóa dữ liệu, tương tự như Tableau hoặc các đối thủ cạnh tranh khác.   
Grafana cho phép giám sát thời gian thực nhiều nguồn dữ liệu, chẳng hạn như giám sát các cơ sở Kubernetes, và hỗ trợ phân tích dữ liệu chi tiết, 
giúp bạn nhận thức được hiệu suất hệ thống. Ngoài ra, Grafana còn cho phép thiết lập các cảnh báo dựa trên các điều kiện được xác định trước, 
thông báo cho bạn về các vấn đề trước khi chúng trở thành sự cố.


**2. Yêu Cầu Chuẩn Bị**
Trước khi bắt đầu, bạn cần chuẩn bị một số điều kiện sau đây:

Máy chạy Ubuntu 22.04 .
Tài khoản người dùng không phải root có quyền sudo.

**3. Cài Đặt Grafana Trên Ubuntu 22.04**
Sau đây là hướng dẫn cài đặt Grafana trên Ubuntu 22.04 sử dụng kho APT chính thức của Grafana.
Bước 1 – Cập Nhật Hệ Thống
Trước tiên, bạn cần đảm bảo các gói phần mềm của hệ thống đã được cập nhật. Hãy chạy các lệnh sau trong terminal:

```
sudo apt update -y && sudo apt upgrade -y
```

Bước 2 – Cài Đặt Các Gói Cần Thiết
Tiếp theo, chạy lệnh sau để cài đặt các gói phần mềm cần thiết cho quá trình cài đặt Grafana:

sudo apt install -y apt-transport-https software-properties-common wget
<img width="1074" height="244" alt="image" src="https://github.com/user-attachments/assets/7456f48e-17a1-4f13-8367-614c4359404c" />

Bước 3 – Thêm Khóa GPG Của Grafana
Tiếp theo, tải và lưu khóa GPG của Grafana bằng các lệnh sau:

sudo mkdir -p /etc/apt/keyrings/
wget -q -O - https://apt.grafana.com/gpg.key | gpg --dearmor | sudo tee /etc/apt/keyrings/grafana.gpg > /dev/null
Lệnh đầu tiên tạo một thư mục để lưu khóa, còn lệnh thứ hai tải và lưu khóa GPG.
<img width="1486" height="113" alt="image" src="https://github.com/user-attachments/assets/44db9ef2-169d-4b6d-b18b-e36bbe141f1a" />


Bước 4 – Thêm Kho APT Của Grafana
Vì gói của Grafana không có sẵn trong kho Ubuntu mặc định, bạn cần thêm kho APT chính thức của Grafana vào danh sách các kho nguồn của hệ thống:

echo "deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main" | sudo tee -a /etc/apt/sources.list.d/grafana.list
Sau đó, cập nhật chỉ mục gói để bao gồm thông tin từ kho mới thêm:

```
sudo apt update
```

Bước 5 – Cài Đặt Grafana
Bây giờ, có thể cài đặt Grafana bằng cách chạy lệnh sau:

```
sudo apt install grafana
```

<img width="1338" height="656" alt="image" src="https://github.com/user-attachments/assets/19b4a35c-6252-48e8-b2ee-bfeedbe872fc" />



Bước 6 – Khởi Động Dịch Vụ Grafana
Sau khi hoàn thành quá trình cài đặt, bạn có thể xác minh phiên bản bằng lệnh:

sudo grafana-server -v
<img width="807" height="112" alt="image" src="https://github.com/user-attachments/assets/6cb56618-feca-45d1-a1d2-4fea413952d2" />

Tiếp theo, khởi động dịch vụ Grafana và cho phép nó khởi động cùng hệ thống:

sudo systemctl start grafana-server
sudo systemctl enable grafana-server

Bước 7 – Kiểm Tra Trạng Thái Dịch Vụ Grafana
Bạn có thể kiểm tra trạng thái dịch vụ Grafana bằng cách chạy lệnh:

sudo systemctl status grafana-server
Nếu dịch vụ Grafana được khởi động thành công, bạn sẽ thấy trạng thái “active”.

<img width="1448" height="359" alt="image" src="https://github.com/user-attachments/assets/0c6cc346-cb57-4120-8d81-fb7d05c18a6b" />

Bước 8 – Mở Cổng Trên Firewall
Cổng 3000 là cổng mặc định cho giao diện web của Grafana. Để cho phép truy cập ngoài vào Grafana, bạn cần mở cổng này trên tường lửa:

sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 3000/tcp

<img width="1022" height="242" alt="image" src="https://github.com/user-attachments/assets/bf6f1f25-ecd3-4d99-b689-c87650e1c63f" />

Bước 9 – Truy Cập Giao Diện Web Grafana
Để truy cập giao diện web của Grafana, mở trình duyệt và nhập địa chỉ IP của máy chủ (hoặc hostname nếu có) vào thanh địa chỉ, theo sau bởi cổng 3000. Địa chỉ URL sẽ có dạng:

http://192.168.197.133:3000
Sau khi tải xong, bạn sẽ thấy trang đăng nhập Grafana. Tên đăng nhập và mật khẩu mặc định là:

Username: admin
Password: admin
Bạn sẽ được yêu cầu tạo mật khẩu mới. Nhập mật khẩu an toàn và xác nhận.
<img width="1920" height="922" alt="image" src="https://github.com/user-attachments/assets/b3c3af73-e4c4-4b1e-892b-08c6b3b8202f" />

Giao diện sử dụng của Grafana
<img width="1909" height="891" alt="image" src="https://github.com/user-attachments/assets/df20c10a-ba27-40c1-8d7e-03421b3de850" />

**Cách Gỡ Cài Đặt Grafana**
Nếu cần gỡ cài đặt Grafana, hãy chạy các lệnh sau:

sudo systemctl stop grafana-server
sudo apt remove grafana

**Cài đặt Prometheus trên Ubuntu 22.04**
**Prometheus là gì?**
Prometheus là hệ thống giám sát và cảnh báo mã nguồn mở mạnh mẽ dành cho các hệ thống phân tán và Cloud Native. Điểm khác biệt của Prometheus nằm ở mô hình Pull-based,  
chủ động thu thập dữ liệu từ các đối tượng giám sát thông qua HTTP endpoint và lưu trữ dưới dạng Time-series (chuỗi thời gian).

Hệ thống cung cấp ngôn ngữ truy vấn PromQL linh hoạt, cho phép phân tích dữ liệu chuyên sâu và thiết lập các quy tắc Cảnh báo (Alerting) tự động khi có sự cố.  
Với khả năng tích hợp sâu rộng vào hệ sinh thái Docker, Kubernetes và Grafana, Prometheus đã trở thành tiêu chuẩn hàng đầu trong việc quản trị và theo dõi hiệu năng hạ tầng công nghệ hiện đại.

<img width="1210" height="528" alt="image" src="https://github.com/user-attachments/assets/10ee9842-b71a-4c5c-b536-c574736fb4bd" />

Bước 1: Tạo nhóm hệ thống Prometheus
Hãy bắt đầu bằng cách tạo nhóm và người dùng hệ thống Prometheus.

groupadd --system prometheus
Nhóm có ID < 1000 là nhóm hệ thống. Sau khi nhóm hệ thống được thêm, hãy tạo người dùng hệ thống Prometheus và chỉ định nhóm chính đã tạo.

adduser --system --no-create-home --shell /sbin/nologin --group prometheus

Bước 2: Tạo thư mục dữ liệu & cấu hình
Prometheus cần một thư mục để lưu trữ dữ liệu của nó. Chúng tôi sẽ tạo cái này trong  /var/lib/prometheus .

mkdir /var/lib/prometheus
Thư mục tệp cấu hình chính của Prometheus là  /etc/prometheus/ . Nó sẽ có một số thư mục con:

for i in rules rules.d files_sd; do  mkdir -p /etc/prometheus/${i}; done

<img width="1068" height="122" alt="image" src="https://github.com/user-attachments/assets/12594e85-ce41-4d77-8af8-09f092aab7b3" />

Bước 3: Tải xuống tệp Prometheus
https://github.com/prometheus/prometheus/releases

apt update && apt upgrade -y

apt -y install wget curl vim

mkdir -p /tmp/prometheus && cd /tmp/prometheus

curl -s https://api.github.com/repos/prometheus/prometheus/releases/latest | grep browser_download_url | grep linux-amd64 | cut -d '"' -f 4 | wget -qi -

tar xvf prometheus*.tar.gz

cd prometheus*/
Di chuyển các tệp  thực thi vào thư mục /usr/local/bin/

mv prometheus promtool /usr/local/bin/
Kiểm tra phiên bản đã cài đặt:

root@ubuntu:/tmp/prometheus/prometheus-3.11.2.linux-amd64# prometheus --version

<img width="1167" height="234" alt="image" src="https://github.com/user-attachments/assets/ed7c08c5-937d-4f1b-8ff3-a6217ad74ff8" />

Di chuyển mẫu cấu hình Prometheus sang thư mục /etc.

mv prometheus.yml /etc/prometheus/prometheus.yml

Bước 4: Cấu hình Prometheus trên Ubuntu
Tạo hoặc chỉnh sửa tệp cấu hình cho Prometheus – /etc/prometheus/prometheus.yml .

nano /etc/prometheus/prometheus.yml
Các cấu hình mẫu sẽ trông giống như bên dưới:
<img width="1465" height="609" alt="image" src="https://github.com/user-attachments/assets/04815c5e-506f-4dcf-b034-d5d337fd1b63" />



Tạo dịch vụ hệ thống Prometheus
Để có thể quản lý dịch vụ Prometheus bằng systemd.

nano /etc/systemd/system/prometheus.service
Thêm vào

[Unit]
Description=Prometheus
Documentation=https://prometheus.io/docs/introduction/overview/
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
User=prometheus
Group=prometheus
ExecReload=/bin/kill -HUP $MAINPID
ExecStart=/usr/local/bin/prometheus \
  --config.file=/etc/prometheus/prometheus.yml \
  --storage.tsdb.path=/var/lib/prometheus \
  --web.listen-address=0.0.0.0:9090 \
  --web.enable-lifecycle

SyslogIdentifier=prometheus
Restart=always


[Install]
WantedBy=multi-user.target


Thay đổi quyền thư mục.
Thay đổi quyền sở hữu các thư mục này cho người dùng và nhóm Prometheus.

for i in rules rules.d files_sd; do chown -R prometheus:prometheus /etc/prometheus/${i}; done

for i in rules rules.d files_sd; do chmod -R 775 /etc/prometheus/${i}; done

chown -R prometheus:prometheus /var/lib/prometheus/
Tải lại daemon systemd và khởi động dịch vụ:
systemctl daemon-reload

systemctl start prometheus

systemctl enable prometheus

systemctl status prometheus

<img width="1460" height="310" alt="image" src="https://github.com/user-attachments/assets/05a298fb-baac-4345-be5c-bd89a883fd7c" />

Nếu máy chủ của bạn có dịch vụ tường lửa đang chạy, bạn cần mở cổng 9090 .
sudo ufw allow 9090/tcp
sudo ufw reload  
<img width="552" height="162" alt="image" src="https://github.com/user-attachments/assets/04a11fca-ea14-4512-a69a-239eda4779d4" />


Xác nhận rằng bạn có thể kết nối với cổng 9090 bằng cách truy cập địa chỉ IP/tên DNS của máy chủ Prometheus trong trình duyệt web của bạn.
<img width="1920" height="864" alt="image" src="https://github.com/user-attachments/assets/820c1d90-9481-48a4-9718-6f4dcaca3e75" />



**Xác thực login Prometheus Server:**
Mặc định, khi bạn cài xong Prometheus, bất kỳ ai biết địa chỉ IP và cổng 9090 của bạn đều có thể truy cập vào để xem toàn bộ thông số hệ thống, sơ đồ mạng, 
và thậm chí là can thiệp vào các truy vấn nhạy cảm. Vậy lên cần thiết lập phần xác thực này như sau:

Cài đặt công cụ gen pass và tạo 1 tệp python.

apt install python3-bcrypt -y
vi gen-pass.py
Thêm vào
```
import getpass

import bcrypt

password = getpass.getpass("password: ")

hashed_password = bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt())

print(hashed_password.decode())
```
Sau đó

python3 gen-pass.py

Nhập mật khẩu mong muốn, sau đó file code sẽ tự gen 1 đoạn pass đã được mã hóa
<img width="781" height="156" alt="image" src="https://github.com/user-attachments/assets/a807fbad-2429-4ae3-bca7-f11dee18e464" />


Tạo tệp Web YAML
vi /etc/prometheus/web.yml
Thêm vào
<img width="1119" height="193" alt="image" src="https://github.com/user-attachments/assets/47cf2a53-bb0c-4fd7-b169-3fc5bbd86229" />


Bạn có thể xác thực tệp đó bằng các lệnh bên dưới.

promtool check web-config /etc/prometheus/web.yml

<img width="883" height="132" alt="image" src="https://github.com/user-attachments/assets/ab930ba8-4c19-427f-8494-68981e11eb19" />

**Khởi chạy máy chủ Prometheus**
Thêm 1 đoạn xác minh vào

vi /etc/systemd/system/prometheus.service
Thêm vào

--web.config.file=/etc/prometheus/web.yml \


Khởi động lại dịch vụ

systemctl daemon-reload

systemctl restart prometheus

systemctl enable prometheus

Hoàn tất thiết lập mật khẩu cho Prometheus

<img width="897" height="566" alt="image" src="https://github.com/user-attachments/assets/a88664b6-3767-449e-80d4-35ef28f3ec8f" />


**Tổng quan về PromQL (Prometheus Query Language)**
PromQL (Prometheus Query Language) là ngôn ngữ truy vấn nội tại được thiết kế chuyên biệt cho hệ thống giám sát Prometheus. Khác biệt hoàn toàn với các ngôn ngữ truy vấn cơ sở dữ liệu quan hệ truyền thống (như SQL), PromQL được tối ưu hóa dành riêng cho việc xử lý và phân tích mô hình dữ liệu chuỗi thời gian (Time-Series Data). Đây là một ngôn ngữ thuần chức năng (functional language) và hoạt động dưới cơ chế chỉ đọc (read-only), nhằm đảm bảo tính toàn vẹn tuyệt đối cho kho dữ liệu giám sát.

Vai trò cốt lõi của PromQL trong hệ thống quản trị hạ tầng phân tán được thể hiện qua các đặc điểm và khả năng sau:

Truy xuất và phân tách dữ liệu đa chiều: cho phép kỹ sư hệ thống cô lập và trích xuất chính xác các luồng thông tin cụ thể từ hàng ngàn thiết bị, máy chủ hoặc vùng mạng (zone) khác nhau.

Phân loại kiểu dữ liệu đầu ra: Một biểu thức PromQL có thể trả về các định dạng dữ liệu khác nhau tùy thuộc vào mục đích sử dụng, điển hình nhất là:

Instant Vector: Trả về một tập hợp các chuỗi thời gian chứa giá trị mới nhất tại một thời điểm duy nhất. 

Range Vector: Trả về một mảng dữ liệu trong một khoảng thời gian xác định (ví dụ: 5 phút qua). 

Trụ cột của Cảnh báo và Trực quan hóa: PromQL không chỉ dùng để truy vấn thủ công (ad-hoc) khi khắc phục sự cố (troubleshooting). Các biểu thức PromQL chính là nền tảng để xây dựng các biểu đồ giám sát động trên các nền tảng như Grafana, đồng thời là ngôn ngữ để định nghĩa các bộ quy tắc cảnh báo (Alerting Rules), tự động kích hoạt thông báo khi hệ thống vượt quá các ngưỡng an toàn đã thiết lập.

PromQL (Prometheus Query Language) tuân theo cú pháp biểu thức chính quy Re2 của Google, cho phép xây dựng các truy vấn linh hoạt trên dữ liệu chuỗi thời gian.
**Cú pháp cơ bản**
Bashmetric_name{label="value"}[range]
Ví dụ:
Bashhttp_requests_total{job="apiserver", handler="/api/comments"}[5m]

Truy vấn số lượng yêu cầu HTTP của job apiserver và handler /api/comments trong khoảng thời gian 5 phút gần nhất.
Nếu bỏ [5m], kết quả trả về là vector tức thời (giá trị gần nhất).

 **Truy vấn con (Subquery)**
Bashrate(http_requests_total{job="apiserver", handler="/api/comments"}[5m])[range:resolution]

Áp dụng hàm rate() để tính tốc độ tăng trên khoảng thời gian 5m.
Phần [range:resolution] dùng để kiểm soát độ phân giải khi hiển thị đồ thị.

Lưu ý: Nên hạn chế lồng truy vấn quá sâu (nested queries) vì sẽ làm phức tạp và khó bảo trì. Ưu tiên viết truy vấn riêng biệt khi cần.
**Hàm tích hợp (Functions)**
PromQL cung cấp nhiều hàm tiện ích. Ví dụ hàm label_join():
Bashlabel_join(up{job="api-server", src1="a", src2="b", src3="c", srcN="z"}, 
           "New", ",", "src1", "src2", "src3", "srcN")

Kết hợp nhiều label thành một label mới (New) với giá trị a,b,c,z, phân cách bằng dấu phẩy.


**Monitor server Linux**

Cài đặt Node Exporter
Trên các máy cần giám sát, chúng ta cần cài Node Exporter. Đây là công cụ giúp thu thập dữ liệu cần giám sát, gồm các thông tin như CPU, RAM, Disk, Network,…

Cài đặt Node Exporter – Bản Standalone
Cài đặt Promethues, Grafana trên Ubuntu 22.04
		Cấu trúc query promethues
		Monitor server linux
		Monitor mysql
		Đưa biểu đồ lên kết hợp grafana (biểu đồ hiển thị RAM, CPU, Uptime, CPU Load, Mysql)
# Tải ứng dụng
wget https://github.com/prometheus/node_exporter/releases/download/v1.11.1/node_exporter-1.11.1.linux-amd64.tar.gz

# Giải nén
tar -xzf node_exporter-1.11.1.linux-amd64.tar.gz

# Xóa tệp tải về
rm node_exporter-1.11.1.linux-amd64.tar.gz

# Đổi tên
mv node_exporter-1.11.1.linux-amd64 node_exporter-1.11.1

# Vào thư mục ứng dụng
cd node_exporter-1.11.1

# Kiểm tra phiên bản
./node_exporter --version

# Chạy ứng dụng
./node_exporter
Thông thường chúng ta sẽ chạy ở cấu hình mặc định, tức ở port 9100 và đường dẫn lấy dữ liệu là /metrics. 

<img width="1311" height="263" alt="image" src="https://github.com/user-attachments/assets/fd5d7827-3238-4971-995d-2ae0ab792432" />

Cập nhật cấu hình 
nano /etc/prometheus/prometheus.yml

- job_name: "node_exporter"
    static_configs:
      - targets: ["localhost:9100"]
  
<img width="1440" height="422" alt="image" src="https://github.com/user-attachments/assets/37a23acd-7830-417e-aa97-b553ab33a02c" />

Restart lại dịch vụ sau đó kiểm tra tại trang chính của Prometheus
systemctl restart prometheus

<img width="1920" height="459" alt="image" src="https://github.com/user-attachments/assets/cea4eb83-91b9-4363-9d6f-8aeeb32684d3" />


**Monitor mysql**
Bước 1: Cài mysqld_exporter lên máy chủ chứa MySQL
(Để nó chui vào Database đọc thông số). Bạn chạy lần lượt các lệnh sau:
# Vào MySQL tạo tài khoản cho Exporter
mysql -u root -p
CREATE USER 'exporter'@'localhost' IDENTIFIED BY 'MatKhauKho123';
GRANT PROCESS, REPLICATION CLIENT, SELECT ON *.* TO 'exporter'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# Tải và cài đặt Exporter
wget https://github.com/prometheus/mysqld_exporter/releases/download/v0.15.1/mysqld_exporter-0.15.1.linux-amd64.tar.gz
tar -xzf mysqld_exporter-0.15.1.linux-amd64.tar.gz
mv mysqld_exporter-0.15.1.linux-amd64/mysqld_exporter /usr/local/bin/

# Tạo file chứa mật khẩu
nano /etc/.my.cnf
Mới thỏa mãn được một nửa thôi Đông ơi!

Bước 1: Cài mysqld_exporter lên máy chủ chứa MySQL
(Để nó chui vào Database đọc thông số). Bạn chạy lần lượt các lệnh sau:

Bash
# Vào MySQL tạo tài khoản cho Exporter
mysql -u root -p
CREATE USER 'exporter'@'localhost' IDENTIFIED BY 'MatKhauKho123';
GRANT PROCESS, REPLICATION CLIENT, SELECT ON *.* TO 'exporter'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# Tải và cài đặt Exporter
wget https://github.com/prometheus/mysqld_exporter/releases/download/v0.15.1/mysqld_exporter-0.15.1.linux-amd64.tar.gz
tar -xzf mysqld_exporter-0.15.1.linux-amd64.tar.gz
mv mysqld_exporter-0.15.1.linux-amd64/mysqld_exporter /usr/local/bin/

# Tạo file chứa mật khẩu
nano /etc/.my.cnf
Dán cái này vào file .my.cnf:

Ini, TOML
[client]
user=exporter
password=MatKhauKho123


<img width="809" height="232" alt="image" src="https://github.com/user-attachments/assets/b557eb73-e25c-49ed-a3bd-66520a527c4c" />


# Vào MySQL tạo tài khoản cho Exporter
mysql -u root -p
CREATE USER 'exporter'@'localhost' IDENTIFIED BY 'MatKhauKho123';
GRANT PROCESS, REPLICATION CLIENT, SELECT ON *.* TO 'exporter'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# Tải và cài đặt Exporter
wget https://github.com/prometheus/mysqld_exporter/releases/download/v0.15.1/mysqld_exporter-0.15.1.linux-amd64.tar.gz
tar -xzf mysqld_exporter-0.15.1.linux-amd64.tar.gz
mv mysqld_exporter-0.15.1.linux-amd64/mysqld_exporter /usr/local/bin/

# Tạo file chứa mật khẩu
nano /etc/.my.cnf
Dán cái này vào file .my.cnf:

Ini, TOML
[client]
user=exporter
password=MatKhauKho123
Bước 2: Chạy dịch vụ và mở cổng 9104


nano /etc/systemd/system/mysqld_exporter.service
Dán cấu hình này vào:

[Unit]
Description=Prometheus MySQL Exporter
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/mysqld_exporter \
  --config.my-cnf=/etc/.my.cnf
Restart=always

[Install]
WantedBy=multi-user.target


Khởi động nó:


systemctl daemon-reload
systemctl enable --now mysqld_exporter

Bước 3: Trỏ Prometheus vào lấy dữ liệu và lên biểu đồ

Mở file nano /etc/prometheus/prometheus.yml, thêm vào dưới cùng:


  - job_name: "mysql_server"
    static_configs:
      - targets: ["localhost:9104"]
   
    <img width="809" height="232" alt="image" src="https://github.com/user-attachments/assets/e675a855-dfb1-4c69-952b-1925254abbed" />
Restart Prometheus: systemctl restart prometheus. kieemr tra trạng thái tại trang chủ

<img width="1920" height="446" alt="image" src="https://github.com/user-attachments/assets/b7fb50ac-0ca3-4a41-ba5a-8e85f1540384" />
import dasboard qtri sql
<img width="1023" height="539" alt="image" src="https://github.com/user-attachments/assets/b4186e42-5335-4534-a168-57de7f732364" />
đổi sang ID dasboard 6239
<img width="1519" height="890" alt="image" src="https://github.com/user-attachments/assets/f734f772-ed8b-4d53-bc6a-90a89507c9b9" />


**Đưa biểu đồ lên kết hợp grafana (biểu đồ hiển thị RAM, CPU, Uptime, CPU Load, Mysql)**
<img width="1029" height="734" alt="image" src="https://github.com/user-attachments/assets/4d18e258-42d6-428e-a0bc-43ddcb34cff5" />
kết nối với prometheus, phần bảo mật nhập tài khoản và pass đã tạo trước đó và lưu

Bây giờ chúng ta vào trình duyệt, mở link http://localhost:3000, và đăng nhập vào với tài khoản mặc định admin / admin, sau đó đổi mật khẩu mới, sau khi hoàn thành sẽ thấy giao diện như hình vẽ:

Giao diện chính của Grafana
Như theo hướng dẫn để hiển thị được dữ liệu bạn phải làm hai việc:

Thêm mới “Data Source”:
B1: Trên giao diện Dashboard ta kích vào “Add your first data source”, hoặc trên Menu trái ta chọn “Connections -> Data sources“
B2: Ta chọn prometheus, sau đó phần URL nhập “http://localhost:9090“, còn các cấu hình khác mặc định, sau đó nhấn “Save & test“. Hiện tại thấy hỗ trợ 152 kiểu Data Source khác nhau.
Tạo Dashboard để hiển thị dữ liệu:
B1: Trên giao diện Dashboard, ta kích vào “Create your first dashboard“, hoặc trên menu trái chọn “Dashboards -> New -> Import“. Việc tự tạo Dashboard hơi mất công nên ta sử dụng 1 trong các dashboard trên Grafana Dashboards, có nhiều template đẹp, hiển thị thông tin khác nhau tùy mục đích sử dụng. Đã xem nhiều mẫu thích nhất mẫu 11074, ta nhập ID 11074 vào rồi nhấn Load, sau đó bạn đổi tên và chọn Data Source, rồi nhấn nút Import. Ngoài ra tôi thấy nhiều mẫu khác hiển thị tập trung vào dữ liệu cần thiết, bạn nên tham khảo:
11074 – Node Exporter Dashboard EN 20201010-StarsL.cn
15172 – Node Exporter for Prometheus Dashboard based on 11074
1860 – Node Exporter Full: Mẫu này khá nhiều thông tin, chắc dành cho DevOpts chuyên nghiệp, mình nhìn không quen lắm.
6417 – Kybernetes Cluster
7249 – Kubernetes Cluster
8171 – Kubernetes Nodes
11956 – Nodejs Metrics
2583 – MongoDB
B2: Sau khi Import thì vào lại giao diện để xem
Sau khi xong, chúng ta nhìn thấy Dashboard hiển thị như sau (Còn rất nhiều chart ở bên dưới):
<img width="1487" height="840" alt="image" src="https://github.com/user-attachments/assets/4f723615-2494-40d1-bda7-4a5f083cf967" />
<img width="1489" height="854" alt="image" src="https://github.com/user-attachments/assets/bed9e4ee-531e-423a-b217-d13b2d7b5e10" />


### Tổng quan về giải pháp HAProxy 
# HAProxy là gì
HAProxy (High Availability Proxy) là một công cụ mã nguồn mở ứng dụng cho giải pháp cân bằng tải TCP và HTTP. Người dùng có thể sử dụng HAProxy để cải thiện độ hoàn thiện của các trang web và ứng dụng bằng cách phân tán khối lượng công việc của chúng trên nhiều máy chủ. Cải thiện hiệu suất bao gồm giảm phản hồi thời gian và tăng thông lượng. HAProxy cũng được sử dụng trong các hệ thống lớn có lưu lượng truy cập cao như GoDaddy, GitHub, Bitbucket, Stack Overflow, Reddit, Speedtest.net, Twitter và trong nhiều sản phẩm cung cấp bởi Amazon Web Service.

<img width="1105" height="490" alt="image" src="https://github.com/user-attachments/assets/aa4c94a4-8746-4993-9721-8001af146a39" />

# Các tính năng của HAProxy bao gồm:

Hỗ trợ cân bằng tải ở lớp 4 và lớp 7 ( tương ứng với TCP và HTTP),
Support HTTP protocol, HTTP / 2, gRPC, FastCGI.
Chấm dứt SSL / TLS.
Lưu trữ chứng chỉ SSL động.
Chuyển đổi nội dung và kiểm tra.
Ủy quyền minh bạch.
Ghi nhật ký chi tiết.
CLI.
Xác thực HTTP.
Đa luồng
Rewrite URL.

# Thuật ngữ được sử dụng trong HAProxy 

**Proxy**
Proxy là một máy chủ trung gian kết nối giữa người dùng và Internet. Khi người dùng truy cập một trang web thông qua Proxy, yêu cầu này sẽ được gửi đến Proxy server trước, sau đó Proxy server sẽ gửi yêu cầu đến trang web đích và nhận kết quả trả về.  Proxy có chức năng tương tự như một bức tường lửa (firewall) hay một hệ thống lọc truy cập trang web nhằm quản lý và đảm bảo tính bảo mật khi sử dụng.

**Access Control List (ACL)**
Xét về load balancing, ACL được sử dụng để kiểm tra một số điều kiện và thực hiện hành động (ví dụ: chọn server hoặc block một request) dựa trên kết quả kiểm tra. Việc sử dụng ACL cho phép linh hoạt chuyển tiếp lưu lượng mạng dựa trên nhiều yếu tố khác nhau, ví dụ như pattern-matching và số lượng kết nối với một backend.

Ví dụ về một ACL:
acl url_blog path_beg /blog
ACL này sẽ trở nên phù hợp nếu đường dẫn request của người dùng bắt đầu bằng /blog. Điều này sẽ tương ứng với request của http://yourdomain.com/blog/blog-entry-1 chẳng hạn.
Nếu bạn muốn được hướng dẫn chi tiết về cách sử dụng ACL, hãy tham khảo Hướng dẫn Cấu hình HAProxy.


**Backend**
Một backend là một tập các servers nhận các requests được chuyển tiếp. Các backends được định nghĩa thuộc backend section của cấu hình HAProxy. Về cơ bản, một backend có thể được định nghĩa bởi:
- Thuật toán load balance được sử dụng.
- Danh sách servers và ports.
Một backend có thể chứa một hoặc nhiều máy chủ trong đó, việc thêm nhiều servers hơn vào backend sẽ tăng khả năng tải bằng cách mở rộng tải trên nhiều servers. Đồng thời độ tin cậy cũng tăng theo, trong trường hợp một số backend servers của bạn không khả dụng.

Dưới đây là một ví dụ về cấu hình backend, web-backend và blog-backend với hai webservers cho mỗi loại trên port 80:

banner Load balancer
backend web-backend  

balance roundrobin   

server web1 web1.yourdomain.com:80 check   

server web2 web2.yourdomain.com:80 check 

backend blog-backend   

balance roundrobin   

mode http   

server blog1 blog1.yourdomain.com:80 check   

server blog1 blog1.yourdomain.com:80 check

- "balance roundrobin" xác định thuật toán load balancing.

- "mode http" xác định layer 7 proxying sẽ được sử dụng.

Tùy chọn "check" ở phần cuối các chỉ thị của server chỉ định rằng các kiểm tra sức khỏe sẽ được thực hiện trên các backend servers đó.

**Frontend**
Một frontend xác định cách các requests sẽ được chuyển tiếp đến các backends. Frontend được định nghĩa trong phần frontend của cấu hình HAProxy. Định nghĩa của chúng bao gồm các thành phần sau:
- Một tập hợp các địa chỉ IP và một cổng (ví dụ: 10.1.1.7:80, *: 443,...)
- Các ACL
- Các quy tắc use_backend, các quy tắc này xác định các backend nào sẽ sử dụng, tùy thuộc vào điều kiện ACL nào thích hợp, và/ hoặc một quy tắc default_backend xử lý mọi trường hợp khác.
Một frontend có thể được cấu hình cho các loại lưu lượng mạng khác nhau.

**Sticky Sessions** 
Sicky Sessions trong HAProxy (còn gọi là Session Persistence hoặc Session Affinity) là kỹ thuật gắn kết (persist) một người dùng (client) với một backend server cố định trong suốt phiên làm việc (session).
Tại sao cần Sticky Sessions?

Khi ứng dụng là stateful (lưu dữ liệu phiên trên server): ví dụ login, giỏ hàng, session PHP, JSESSIONID, WebSocket…
Nếu không có sticky, HAProxy cân bằng tải (roundrobin, leastconn…) sẽ phân phối request ngẫu nhiên → người dùng có thể bị chuyển sang server khác → mất session, logout, lỗi ứng dụng.

Ý nghĩa trong Load Balancing:

 HAProxy phân phối request đều giữa các server để cân bằng tải.
Với Sticky Sessions: Tất cả request từ cùng một user sẽ luôn được gửi về cùng một server trong thời gian phiên hoạt động.

Lưu ý :
Sticky Sessions không phải giải pháp tốt nhất lâu dài.
Giải pháp lý tưởng là làm ứng dụng stateless (lưu session vào Redis, Memcached, Database chung).
HAProxy hỗ trợ triển khai Sticky Sessions chủ yếu qua Cookie hoặc Stick Table (dựa trên IP, cookie, header…).

**Load Balancing là gì?**

Load balancing (Cân bằng tải) là việc phân phối hiệu quả lưu lượng truy cập đến trên một nhóm backend servers, hay còn được gọi là server farm hoặc server pool.

Các website hiện đại có lưu lượng truy cập cao phải phục vụ hàng trăm nghìn, thậm chí hàng triệu các requests đồng thời từ người dùng hoặc khách hàng, đồng thời phải phản hồi chính xác lại các văn bản, hình ảnh, video hoặc dữ liệu ứng dụng, tất cả đều được thực hiện rất nhanh chóng và đáng tin cậy. Để đáp ứng hiệu quả khối lượng lớn các yêu cầu và phản hồi một cách tiết kiệm nhất, best practice tốt nhất thường sẽ là sử dụng thêm servers.



**Các loại Load balancer và thuật toán sử dụng**
Load balancer (cân bằng tải) thường được nhóm thành 2 loại: Layer 4 và Layer 7.

Cân bằng tải Layer 4 hoạt động dựa trên dữ liệu tìm thấy trong các giao thức mạng và giao vận layer (IP, TCP, FTP, UDP).
Các bộ cân bằng tải Layer 7 phân phối các yêu cầu dựa trên dữ liệu được tìm thấy trong các giao thức layer ứng dụng như HTTP.
Bộ cân bằng tải Layer 7 (Load balancer Layer 7) có thể phân phối thêm các requests dựa trên dữ liệu cụ thể của ứng dụng như HTTP header, cookies hoặc dữ liệu trong chính thông báo ứng dụng, chẳng hạn như giá trị của một thông số cụ thể.
banner Load balancer
Yêu cầu (request) được nhận bởi cả 2 loại Load balancer (cân bằng tải) và chúng được phân phối đến một server cụ thể dựa trên một thuật toán được cấu hình. 

Một số thuật toán được sử dụng trong các hệ thống cân bằng tải là:

Round robin: Là thuật toán điều phối vòng tròn, các máy chủ sẽ được xem ngang hàng và sắp xếp theo một vòng quay. Các truy vấn dịch vụ sẽ lần lượt được gửi tới các máy chủ theo thứ tự sắp xếp.
Weighted round robin: Tương tự như kỹ thuật Round Robin nhưng WRR còn có khả năng xử lý theo cấu hình của từng server đích. Mỗi máy chủ được đánh giá bằng một số nguyên (giá trị trọng số Weight – mặc định giá trị là 1). Một server có khả năng xử lý gấp đôi server khác sẽ được đánh số lớn hơn và nhận được số request gấp đôi từ bộ cân bằng tải.
Least connections: Các requests sẽ được chuyển vào server có ít kết nối nhất trong hệ thống. Thuật toán này được coi như thuật toán động, vì nó phải đếm số kết nối đang hoạt động của server.
Các loại Load balancer và thuật toán sử dụng
Least connections

Least response time:  Đây là thuật toán dựa trên tính toán thời gian đáp ứng của mỗi server (response time), thuật toán này sẽ chọn server nào có thời gian đáp ứng nhanh nhất. Thời gian đáp ứng được xác định bởi khoảng thời gian giữa thời điểm gửi một gói tin đến server và thời điểm nhận được gói tin trả lời.
IP Hash: Thuật toán xác định kết nối chính xác từ một IP của máy khách sẽ được kết nối trực tiếp đến một server backend.
Vì sao bạn phải sử dụng Load balancer cho Server?
banner Load balancer
**Khi nào sử dụng Load balancer (Cân bằng tải)?**
Load balancer (Cân bằng tải) đước sử dụng khi website của bạn được triển khai trên nhiều hơn một cloud server.

Sau đây là 2 khó khăn chính bạn sẽ gặp phải nếu chỉ triển khai website của mình trên một server duy nhất:

Không thể đảm bảo xử lý được lượng traffic của một khối lượng user lớn truy cập vào website (đủ lớn để 1 cloud server không thể tải nổi).
Cụ thể, khi hệ thống của bạn có quá nhiều người sử dụng, một server đơn lẻ không thể đáp ứng được lượng requests khổng lồ được gửi đến cùng lúc. Bạn cần chia sẻ công việc của server hiện tại với một hoặc nhiều server khác nữa. Tức là sử dụng nhiều server một lúc, khi này, việc tải dữ liệu từ server nào sẽ do bộ Load balancer (Cân bằng tải)  quyết định.

Không đảm bảo tính sẵn sàng của dịch vụ doanh nghiệp (nếu chỉ sử dụng duy nhất 1 cloud server, khi cloud server này gặp sự cố, không hề có server nào có thể thay thế tiếp tục các tiến trình đang dang dở, đấn đến website rơi vào tình trạng downtime theo). Gây tổn thất và ảnh hưởng to lớn đến trải nghiệm người dùng, doanh thu, uy tín và hiệu quả hoạt động của doanh nghiệp.
Đó chính là lý do mà chúng ta phải triển khai website trên nhiều cloud server cùng một lúc, bởi vì không có gì đảm bảo cho việc một server sẽ luôn hoạt động trơn tru và hoàn hảo mãi mãi. Bằng cách triển khai nhiều, khi server gặp sự cố, server còn lại ngay lập tức là phương án hỗ trợ, không làm gián đoạn hoạt động của doanh nghiệp. Trên thực tế, những tổn thất do hệ thống thông tin ngừng hoạt động là cực kỳ to lớn và không thể lường trước được.


Tuy nhiên, khi triển khai website lên nhiều server, hiệu quả là vậy nhưng bạn vẫn chắc chắn sẽ gặp phải các khó khăn tiếp theo sau đây:
Triển khai nhiều cloud servers, tức đồng thời bạn sẽ sở hữu nhiều địa chỉ của các cloud servers. Trong khi đó, một domain website lại không thể trỏ về nhiều địa chỉ cùng một lúc.
Bạn càng sở hữu nhiều servers, việc quản trị, triển khai và bảo trì càng trở nên phức tạp và khó khăn gấp nhiều lần. Đòi hỏi tính chuyên môn và kĩ thuật cao, chưa kể chi phí cho các thao tác này.
Lúc này, Load balancer (Cân bằng tải) chính là câu trả lời và là giải pháp tuyệt vời có thế giải quyết hoàn hảo triệt để các vấn đề kể trên.

## Cài Đặt Triển Khai HAProxy + Keepalive cho Apache trên Ubuntu 22.04
Phần 2. Cấu hình Apache (Web backend)
Thực hiện tại node1 (Web1 - 10.10.11.86)
Bashsudo apt update
sudo apt install apache2 -y

# Đổi port từ 80 thành 8081
sudo sed -i 's/Listen 80/Listen 8081/' /etc/apache2/ports.conf
sudo sed -i 's/:80/:8081/' /etc/apache2/sites-enabled/000-default.conf

echo '<h1>Chào mừng tới Blog Cloud365 (Web1)</h1>' | sudo tee /var/www/html/index.html

sudo systemctl start apache2
sudo systemctl enable apache2
Thực hiện tại node2 (Web2 - 10.10.11.87)
Bashsudo apt update
sudo apt install apache2 -y

sudo sed -i 's/Listen 80/Listen 8081/' /etc/apache2/ports.conf
sudo sed -i 's/:80/:8081/' /etc/apache2/sites-enabled/000-default.conf

echo '<h1>Chào mừng tới Blog Cloud365 (Web2)</h1>' | sudo tee /var/www/html/index.html

sudo systemctl start apache2
sudo systemctl enable apache2



Phần 2. Triển khai Keepalived (1 VIP)
Cài đặt gói Keepalived trên cả 2 node
Bashsudo apt update
sudo apt install keepalived -y
<img width="885" height="365" alt="image" src="https://github.com/user-attachments/assets/070788f4-386c-4720-9c8a-3cde21a0f35e" />
<img width="825" height="188" alt="image" src="https://github.com/user-attachments/assets/ff45f4f2-154f-4034-b3cc-89f17d9905f2" />

Cấu hình Keepalived
Trên Node1 (Master)
Bashsudo cat > /etc/keepalived/keepalived.conf << 'EOF'
vrrp_script chk_haproxy {
    script "killall -0 haproxy"
    interval 2
    weight 2
}

vrrp_instance VI_1 {
    interface enp2s1                
    state MASTER
    virtual_router_id 51
    priority 101
    virtual_ipaddress {
        10.10.10.89/24
    }
    track_script {
        chk_haproxy
    }
}
EOF
Trên Node2 (Backup)
Bashsudo cat > /etc/keepalived/keepalived.conf << 'EOF'
vrrp_script chk_haproxy {
    script "killall -0 haproxy"
    interval 2
    weight 2
}

vrrp_instance VI_1 {
    interface enp2s1
    state BACKUP
    virtual_router_id 51
    priority 100
    virtual_ipaddress {
        10.10.10.89/24
    }
    track_script {
        chk_haproxy
    }
}
EOF

<img width="1340" height="624" alt="image" src="https://github.com/user-attachments/assets/f0591c36-8f0a-4cd7-b487-1220867d2675" />


**Phần 3. Cài đặt HAProxy (trên cả 2 node)**
Cài đặt HAProxy
sudo apt update
sudo apt install haproxy -y

Backup và cấu hình HAProxy
Bashsudo cp /etc/haproxy/haproxy.cfg /etc/haproxy/haproxy.cfg.bak 2>/dev/null || true

sudo cat > /etc/haproxy/haproxy.cfg << 'EOF'
global
    log 127.0.0.1 local2
    chroot /var/lib/haproxy
    pidfile /var/run/haproxy.pid
    maxconn 4000
    user haproxy
    group haproxy
    daemon
    stats socket /var/lib/haproxy/stats

defaults
    mode http
    maxconn 8000
    log global
    option httplog
    option dontlognull
    option http-server-close
    retries 3
    timeout http-request 20s
    timeout queue 1m
    timeout connect 10s
    timeout client 1m
    timeout server 1m
    timeout http-keep-alive 10s
    timeout check 10s

listen stats
    bind *:8080
    mode http
    stats enable
    stats uri /stats
    stats realm HAProxy Statistics
    stats admin if TRUE

listen web-backend
    bind *:80
    balance roundrobin
    cookie SERVERID insert indirect nocache
    mode http
    option httpchk
    option httpclose
    option httplog
    option forwardfor
    server node1 10.10.11.86:8081 check cookie node1 inter 5s fastinter 2s rise 3 fall 3
    server node2 10.10.11.87:8081 check cookie node2 inter 5s fastinter 2s rise 3 fall 3
EOF


lưu ý đổi cổng của apache2 hoặc nginx nhường cho haproxy
sudo ss -tulpn | grep ':80'

sudo sed -i "s/<VirtualHost \*:80>/<VirtualHost \*:8081>/g" /etc/apache2/sites-available/000-default.conf

<img width="1453" height="432" alt="image" src="https://github.com/user-attachments/assets/d183d322-fead-4a4f-9e20-dd10208b9f62" />
**Cấu hình Log cho HAProxy**
Bashsudo sed -i 's/#module(load="imudp")/module(load="imudp")/' /etc/rsyslog.conf
sudo sed -i 's/#input(type="imudp" port="514")/input(type="imudp" port="514")/' /etc/rsyslog.conf

echo '$UDPServerAddress 127.0.0.1' | sudo tee -a /etc/rsyslog.conf

echo 'local2.*    /var/log/haproxy.log' | sudo tee /etc/rsyslog.d/haproxy.conf

sudo systemctl restart rsyslog
Cho phép kernel bind VIP
echo 'net.ipv4.ip_nonlocal_bind = 1' | sudo tee -a /etc/sysctl.conf
sudo sysctl -p

<img width="1049" height="169" alt="image" src="https://github.com/user-attachments/assets/69d40573-2b59-4f95-8b15-66108c73c6c3" />

Khởi động HAProxy
Bashsudo systemctl restart haproxy
sudo systemctl enable haproxy

<img width="1919" height="859" alt="image" src="https://github.com/user-attachments/assets/84659298-f75f-4336-ac19-ea218f1ad9ca" />

trang truy cập, có thẻ truy cập trang còn lại bằng tab ẩn danh
<img width="1015" height="342" alt="image" src="https://github.com/user-attachments/assets/09ba643c-1137-413f-8742-302101e5f933" />

TRang theo dõi, quản trị 








11	Firewall	(Hoàn thiện sau)
Triển khai pfsense
		NAT Local Internet
		Firewall Rule
		DHCP
		Setup OpenVPN với Pfsense (OpenVPN mode tab, OpenVPN mode Tun)
		
