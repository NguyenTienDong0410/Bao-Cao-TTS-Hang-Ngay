# BÁO CÁO THỰC TẬP NGÀY 8/5

---

## I. PHÂN TÍCH VÀ ĐÁNH GIÁ CÁC CONTROL PANEL Free QUẢN TRỊ MÁY CHỦ (SERVER)

## PHẦN 1: NHÓM KIẾN TRÚC HIỆN ĐẠI & TỐI ƯU HIỆU NĂNG CAO (HIGH PERFORMANCE)

Nhóm này tập trung vào việc áp dụng các công nghệ Web Server tiên tiến, giao diện trực quan (UI/UX) và tích hợp sẵn các công cụ tự động hóa (CI/CD, Docker, Backup).

### 1. CyberPanel

CyberPanel là Control Panel đầu tiên và duy nhất tích hợp sẵn OpenLiteSpeed ở cấp độ core, mang lại lợi thế tuyệt đối về tốc độ xử lý nội dung động (Dynamic Content).

* **Kiến trúc lõi:** Vận hành trên nền tảng OpenLiteSpeed (miễn phí) hoặc LiteSpeed Enterprise (trả phí). Tích hợp sẵn LSCache ở mức độ server, giúp tối ưu hóa TTFB (Time to First Byte) cho các CMS như WordPress, Joomla.
* **Quản trị hệ thống:** Hỗ trợ quản lý Docker Container trực tiếp trên giao diện, tích hợp Git Webhooks cho phép tự động deploy code từ GitHub/GitLab.
* **Bảo mật & Backup:** Tích hợp sẵn ModSecurity, CSF (ConfigServer Security & Firewall) và SpamAssassin. Hỗ trợ backup tự động lên Google Drive hoặc AWS S3.
* **Ưu điểm:** Tốc độ phản hồi cực nhanh; tiết kiệm tài nguyên CPU so với Apache truyền thống; hỗ trợ chuẩn HTTP/3 & QUIC.
* **Nhược điểm:** Cấu trúc file cấu hình (Rewrite Rules) của LiteSpeed đôi khi khác biệt so với Nginx/Apache, đòi hỏi thời gian làm quen.
* **Hệ điều hành tối ưu:** Ubuntu 20.04/22.04/24.04, AlmaLinux 8/9.

<img width="1140" height="500" alt="image" src="https://github.com/user-attachments/assets/4d7a39f1-a64e-4cad-84e4-5ce35452ecfb" />

### 2. aaPanel

aaPanel (phiên bản quốc tế của BAOTA Panel) nổi bật nhờ kiến trúc mô-đun hóa (Modular Architecture). Người quản trị chỉ cài đặt những dịch vụ thực sự cần thiết thông qua App Store nội bộ.

* **Kiến trúc lõi:** Cung cấp khả năng biên dịch (Compile) và cài đặt Nginx, Apache, MySQL/MariaDB, PHP (LNMP/LAMP stack) chỉ bằng một click. Hỗ trợ chạy đồng thời nhiều phiên bản PHP (PHP-FPM).
* **Quản trị hệ thống:** Tích hợp bộ quản lý cronjob trực quan, giám sát tài nguyên (CPU, RAM, Disk I/O) theo thời gian thực với biểu đồ chi tiết. Có sẵn công cụ quản lý các dự án Python và Node.js.
* **Bảo mật & Backup:** Bao gồm hệ thống WAF (Web Application Firewall) cơ bản (bản miễn phí) và nâng cao (bản Pro). Quản lý SSH và fail2ban dễ dàng.
* **Ưu điểm:** Giao diện cực kỳ thân thiện; dễ dàng trouble-shooting qua log hiển thị trực tiếp trên web; kho ứng dụng phong phú.
* **Nhược điểm:** Có thể phát sinh lỗi xung đột thư viện nếu cài đặt quá nhiều module không tương thích cùng lúc. Cần cẩn trọng về bảo mật vì server gốc đặt tại Trung Quốc (dù bản quốc tế đã được độc lập).
<img width="1920" height="1279" alt="image" src="https://github.com/user-attachments/assets/95e1e49e-b5e5-486e-88fd-403bea3d2bff" />

### 3. FastPanel

Một giải pháp đang trỗi dậy mạnh mẽ nhờ sự cân bằng xuất sắc giữa tốc độ giao diện và sự ổn định của hệ thống lõi.

* **Kiến trúc lõi:** Sử dụng Nginx làm Reverse Proxy đứng trước Apache (để xử lý tĩnh/động) hoặc Nginx độc lập (Nginx + PHP-FPM). Hỗ trợ MariaDB, PostgreSQL.
* **Quản trị hệ thống:** Tự động phát hiện và cài đặt các chứng chỉ SSL Let’s Encrypt cùng cơ chế tự động gia hạn trước khi hết hạn. Quản lý File Manager tích hợp sẵn rất mạnh mẽ.
* **Bảo mật & Backup:** Hệ thống sao lưu cực kỳ linh hoạt, hỗ trợ kết nối trực tiếp qua WebDAV, Dropbox, Google Drive hoặc FTP ngoài. Tích hợp AI-bolstered Malware Scanner (tuỳ chọn).
* **Ưu điểm:** Cài đặt "sạch", ít rác hệ thống; tốn rất ít RAM (hoạt động tốt trên VPS 1GB RAM); quản lý DNS zone hiệu quả.
* **Nhược điểm:** Cộng đồng hỗ trợ chưa rộng lớn bằng aaPanel hay CyberPanel.
<img width="1348" height="616" alt="image" src="https://github.com/user-attachments/assets/994cf133-bbf9-40fd-9521-6d93b4d1839d" />

### 4. CloudPanel

Được thiết kế chuyên biệt cho môi trường Cloud Hosting, loại bỏ các tính năng không cần thiết (như quản lý Mail Server) để dồn toàn lực cho Web Server.

* **Kiến trúc lõi:** Ngăn xếp (Stack) tĩnh cực kỳ tối ưu: Nginx + PHP-FPM + MariaDB + Redis. Hoàn toàn không sử dụng Apache. Hỗ trợ Varnish Cache.
* **Quản trị hệ thống:** Hỗ trợ routing cực nhanh cho các ứng dụng PHP, Node.js, Python, và các framework tĩnh (React, Vue). Giao diện tối giản đến mức tối đa.
* **Ưu điểm:** Hiệu năng thuần túy cực cao; bảo mật tốt do hạn chế mở các port không cần thiết (như port 25, 110 của Mail); routing chuẩn chỉ.
* **Nhược điểm:** Không hỗ trợ Mail Server nội bộ (phải dùng SMTP ngoài); chỉ hoạt động trên Debian và Ubuntu (không hỗ trợ họ RHEL/CentOS).

<img width="1500" height="988" alt="image" src="https://github.com/user-attachments/assets/968f0307-c579-417c-aecb-1f2dee5bfabb" />

---

## PHẦN 2: NHÓM QUẢN TRỊ CHUYÊN SÂU & GIẢI PHÁP DOANH NGHIỆP (ENTERPRISE / SYSADMIN)

Nhóm này cung cấp quyền kiểm soát sâu vào hệ điều hành (OS Level), phù hợp cho các cấu trúc mạng phức tạp, Load Balancing, hoặc mô hình High Availability.

### 5. Webmin & Virtualmin

Cặp đôi hoàn hảo cho những System Engineer muốn kiểm soát mọi ngóc ngách của máy chủ nhưng vẫn cần một GUI (Graphical User Interface) để thao tác nhanh.

* **Kiến trúc lõi:** Webmin quản lý OS (User, Cron, iptables, Network Interfaces). Virtualmin mở rộng khả năng đó thành một cPanel thay thế, quản lý Apache/Nginx, BIND DNS, Postfix/Dovecot.
* **Quản trị hệ thống:** Cấu trúc thư mục bám sát hoàn toàn tiêu chuẩn của Linux (FHS). Các file cấu hình được tạo ra chuẩn xác và có thể chỉnh sửa song song bằng Command Line (CLI) qua SSH mà không làm lỗi Control Panel.
* **Ưu điểm:** Tính ổn định cực cao; phân quyền người dùng (ACLs) ở mức độ siêu chi tiết; miễn phí hoàn toàn (bản GPL); thích hợp để dựng các hệ thống mạng doanh nghiệp nội bộ.
* **Nhược điểm:** Giao diện có phần lỗi thời, số lượng tuỳ chọn quá đồ sộ gây "ngợp" cho người mới bắt đầu.

<img width="985" height="494" alt="image" src="https://github.com/user-attachments/assets/951e2b83-9cfc-430f-af51-fa21e8fb25e8" />

### 6. ISPConfig

Một tượng đài mã nguồn mở trong việc xây dựng hệ thống Hosting đa máy chủ (Multi-server).

* **Kiến trúc lõi:** Kiến trúc Master-Slave xuất sắc. Bạn có thể sử dụng 1 máy chủ ISPConfig để quản lý tập trung: 2 máy chủ Web (Nginx/Apache chạy Load Balancing), 2 máy chủ DNS (Master/Slave), và 1 máy chủ Mail riêng biệt.
* **Quản trị hệ thống:** Hỗ trợ Quotas (giới hạn dung lượng ổ cứng, băng thông) chuẩn xác; quản lý máy ảo (Virtualization) tích hợp (OpenVZ).
* **Ưu điểm:** Kiến trúc phân tán hoàn hảo cho môi trường doanh nghiệp cần mở rộng (Scale-out); miễn phí hoàn toàn.
* **Nhược điểm:** Quá trình cài đặt ban đầu phức tạp, yêu cầu kiến thức vững về Linux Networking và Database.

<img width="828" height="413" alt="image" src="https://github.com/user-attachments/assets/40a0b167-c51e-4ad5-85be-4144468aff11" />

### 7. Control Web Panel (CWP)

Giải pháp thay thế cPanel mạnh mẽ nhất trên nền tảng Red Hat Enterprise Linux (RHEL).

* **Kiến trúc lõi:** Cung cấp các tuỳ chọn Web Server linh hoạt và phức tạp nhất, ví dụ: Nginx (Port 80/443 Reverse Proxy) -> Varnish (Cache 82) -> Apache (Backend 81.81.81).
* **Bảo mật:** Bộ công cụ Security Center tích hợp sẵn CWP Secure, ModSecurity (với rule của OWASP), rkhunter (quét rootkit), và quản lý iptables chuyên sâu.
* **Ưu điểm:** Cung cấp thông tin phần cứng cực kỳ chi tiết; bộ đệm (Caching) mạnh; phù hợp cho các máy chủ Enterprise.
* **Nhược điểm:** Giao diện hơi rối mắt; bản miễn phí bị khoá một số tính năng nâng cao (như chuyển đổi PHP version linh hoạt).

<img width="653" height="320" alt="image" src="https://github.com/user-attachments/assets/7580c4eb-0ff3-4e16-84d4-9a692db56ae5" />

---

## PHẦN 3: NHÓM KIẾN TRÚC MỚI (CONTAINER-BASED) & TỐI GIẢN

### 9. EasyPanel

Đại diện cho xu hướng DevOps và kiến trúc Microservices hiện đại.

* **Kiến trúc lõi:** Toàn bộ hệ thống được xây dựng trên nền tảng Docker. EasyPanel đóng vai trò là một giao diện trực quan cho Docker Compose. Sử dụng Traefik hoặc Nginx làm Reverse Proxy định tuyến traffic vào các container.
* **Quản trị hệ thống:** Chỉ với 1 click, bạn có thể triển khai hàng trăm ứng dụng từ kho có sẵn (WordPress, Redis, MongoDB, n8n, Ghost...). Hỗ trợ CI/CD từ GitHub.
* **Ưu điểm:** Môi trường hoàn toàn cách ly (Isolated); loại bỏ 100% rủi ro xung đột phiên bản phần mềm (ví dụ: chạy App 1 dùng Node.js 14 và App 2 dùng Node.js 18 dễ dàng).
* **Nhược điểm:** Đòi hỏi cấu hình phần cứng (RAM) nhiều hơn một chút do tính chất của Container; cần kiến thức cơ bản về mạng Docker.

<img width="1536" height="864" alt="image" src="https://github.com/user-attachments/assets/19fd407e-20f1-4cc8-8622-4e03a0e0a9d0" />


### 10. Hestia Control Panel (HestiaCP)

Nhánh phát triển (Fork) xuất sắc từ VestaCP, tối ưu hóa sâu cho hệ sinh thái Debian/Ubuntu.

* **Kiến trúc lõi:** Nginx (Frontend) kết hợp Apache (Backend) hoặc Nginx Standalone + PHP-FPM. Tích hợp sẵn Exim (Mail) và Dovecot.
* **Ưu điểm:** Cực kỳ nhẹ nhàng (Lightweight), có thể chạy trên VPS 512MB RAM; Giao diện quản trị hiện đại, sạch sẽ; Cơ chế tự động quản lý iptables rất thông minh qua GUI.
* **Nhược điểm:** Không hỗ trợ hệ điều hành CentOS hay AlmaLinux; thiếu các tính năng quản lý nâng cao cho Docker hay NodeJS.

<img width="780" height="440" alt="image" src="https://github.com/user-attachments/assets/495b076b-324b-4fae-9d1c-7e346b34dc17" />

---

II. Plesk
Giới thiệu về bảng điều khiển Plesk
Người dùng có nhiều lựa chọn để install bảng điều khiển trên Linux, chẳng hạn như cPanel và Direct Admin, nhưng do tính mượt mà của nó, bảng điều khiển Plesk được xem xét là một trong những bảng điều khiển tốt nhất và phổ biến nhất cho hệ điều hành Linux và cả hệ điều hành Windows.
Giao diện người dùng của Plesk rất thông thường và đã giúp tăng tốc việc sử dụng. Bảng điều khiển này cũng có hệ thống bảo vệ mạnh mẽ để bảo vệ máy chủ của bạn khỏi thư rác.
Lưu ý rằng bảng điều khiển Plesk là một bảng điều khiển trả phí và bạn phải có giấy phép tương ứng để sử dụng nó. Bạn có thể nhận một giấy phép tạm thời để mua giấy phép gốc từ công ty để install và sử dụng trong vòng 14 ngày.
Cách mua giấy phép tạm thời từ trang web của Plesk
Bạn có thể nhận giấy phép miễn phí cho bảng điều khiển Plesk từ trang web của Plesk:

plesk license

Nhập thông tin được yêu cầu và hoàn tất đăng ký, sau đó bạn sẽ nhận một mã kích hoạt qua email. Bạn sẽ sử dụng mã serial này khi kết thúc việc install và kích hoạt bảng điều khiển Plesk.

Phương pháp install Plesk trên Ubuntu 22.04
Bạn có thể install Plesk trên Ubuntu 22.04 theo 2 cách:

Install Plesk trên Ubuntu 22.04 bằng install trình quản lý Web.
Install Plesk trên Ubuntu 22.04 bằng dòng lệnh.
Cách install Plesk bằng trình quản lý web trên Ubuntu 22.04:
Đầu tiên đăng nhập vào máy chủ của bạn và cập nhật máy chủ bằng cách sử dụng lệnh sau:

sudo apt-get update
Sử dụng các lệnh sau để cấu hình UFW và cho phép các cổng giao diện web Plesk (80/tcp, 443/tcp, 8443/tcp và 8880/tcp):

sudo ufw allow 80/tcp
sudo ufw allow 443/tcp  
sudo ufw allow 8443/tcp  
sudo ufw allow 8880/tcp  
sudo ufw enable  
sudo ufw reload
Bây giờ, bạn nên tải trình install Plesk thông qua lệnh sau:

wget https://autoinstall.plesk.com/plesk-installer
Bạn có thể bật tùy chọn đọc, ghi và thực thi trên tập lệnh đã tải bằng lệnh sau:

sudo chmod 755 plesk-installer
Chạy lệnh sau để khởi chạy giao diện install web Plesk:

sudo ./plesk-installer --web-interface
Kết quả sẽ như sau:

Web interface is now started.  Please visit <https://example.com:8447/> in your browser.

 <img width="1462" height="637" alt="image" src="https://github.com/user-attachments/assets/d3af0953-c384-49fa-a44c-45d88ab0ee35" />

Cách truy cập Plesk trên Ubuntu 22.04:

Mở trình duyệt web và truy cập địa chỉ IP máy chủ của bạn trên cổng 8447 và thực hiện các bước sau:

https://example.com:8447/
Sau đó, để bắt đầu quá trình install Plesk, nhập chi tiết của tài khoản chính của máy chủ của bạn để bắt đầu quá trình install Plesk.

<img width="1207" height="750" alt="image" src="https://github.com/user-attachments/assets/4cb059a3-bdfa-4bf0-a44c-e043db8af175" />

plesk instalation process

Cấu hình phiên bản Plesk của bạn bằng cách nhấp vào "install or update product":

install or update product

 <img width="1399" height="563" alt="image" src="https://github.com/user-attachments/assets/e995b920-93d5-4a9c-b5be-bf2c3ccb03ef" />

Chọn phiên bản Plesk mong muốn của bạn và nhấp "Continue":

plesk version
<img width="1451" height="697" alt="image" src="https://github.com/user-attachments/assets/a9f7909c-a2b6-4af3-9a58-38228da054f4" />

Các tùy chọn install như sau:

Recommended: install các thành phần mặc định của Plesk.
Full: install tất cả các ứng dụng và tính năng được hỗ trợ.
Custom: Chọn các thành phần bạn muốn install trên bảng điều khiển Plesk của bạn.
Sau khi chọn tùy chọn liên quan, hãy nhấp "Continue".

plesk component

Chọn tùy chọn "Recommended" hoặc "Full" sẽ bắt đầu quá trình install Plesk tự động.

Bằng cách chọn tùy chọn "Custom", bạn sẽ thấy danh sách các thành phần có sẵn để install trên máy chủ Plesk:

choose plesk components

Sau khi chọn các thành phần, hãy nhấp "Continue" để bắt đầu quá trình install.
<img width="1161" height="711" alt="image" src="https://github.com/user-attachments/assets/a60d184b-3810-433d-a35e-5e48f4ad9cc4" />

Khi quá trình install hoàn tất, điều hướng đến phiên làm việc và nhấn CTRL+C để dừng tập lệnh.

Sau đó, khởi động lại máy chủ của bạn:

sudo shutdown -r 0
Kết nối đến máy chủ trong phiên SSH. Bạn sẽ thấy thông báo sau:

This server is powered by Plesk.
Run the 'plesk login' command and log in by browsing either of the links received in the output.  Use the 'plesk' command to manage the server. Run 'plesk help' for more info.
Để lấy liên kết đăng nhập một lần, chạy lệnh Plesk login, sau đó tạo một tài khoản quản trị Plesk (Pleskadmin account) và nhập khóa giấy phép (license key) của bạn.

Sau khi hoàn tất thông tin cần thiết và chấp nhận thoả thuận giấy phép, nhấn Enter Plesk.

Bạn có thể truy cập bảng điều khiển Plesk của mình trên cổng 8443 sử dụng HTTPS hoặc trên cổng 8880 sử dụng máy chủ HTTP không bảo mật:

https://your.server.example.com:8443  
http://your.server.example.com:8880

<img width="905" height="857" alt="image" src="https://github.com/user-attachments/assets/afa222a8-b809-40d1-a79e-f2c16aa19b2d" />

Cách install Plesk bằng dòng lệnh trên Ubuntu 22.04:

Đầu tiên, tải tập lệnh install Plesk thông qua lệnh sau:

wget https://autoinstall.plesk.com/plesk-installer
Sử dụng lệnh sau để cấp quyền đọc, ghi và thực thi cho tập lệnh:

chmod +rwx tên_tập_lệnh
Chạy tập lệnh trình install theo hướng dẫn:

sudo ./plesk-installer
Nhập "F" để đồng ý với các điều khoản và điều kiện.
Nhập "Y" để gửi thông tin về các vấn đề xảy ra cho Plesk, bao gồm các vấn đề về install và nâng cấp.
Nhập "F" để xem các loại install.
Nhập "S" để xem danh sách các gói cần install.
Cuối cùng, nhập "F" để tiếp tục.
Sau khi hoàn tất quá trình install Plesk, hãy tạo một tài khoản quản trị. Mở trình duyệt web và truy cập một trong các địa chỉ web được hiển thị bởi công cụ install. Bạn cũng có thể tạo một bộ liên kết đăng nhập mới bằng lệnh sau:

sudo plesk login
Sau khi tạo tài khoản, hãy nhập khóa giấy phép Plesk của bạn.

plesk license key

Như đã đề cập trong phương pháp trước, bạn có thể truy cập bảng điều khiển Plesk của mình theo cách sau:

https://your.server.example.com:8443 
http://your.server.example.com:8880
Bạn có thể thử Plesk miễn phí trong vòng 15 ngày, nhưng bạn có thể nhập giấy phép bạn đã mua bằng cách nhấp vào tab "Already have a license".

Setup 
<img width="911" height="974" alt="image" src="https://github.com/user-attachments/assets/7f029f3c-6e02-4aa2-99cb-522f694ade65" />

<img width="1490" height="893" alt="image" src="https://github.com/user-attachments/assets/fc938865-a24c-4659-9778-aba9681b9693" />
các chức năng
<img width="274" height="655" alt="image" src="https://github.com/user-attachments/assets/1ab4f21a-42bf-4d48-8233-a8b06d984ffd" />

theem domain
<img width="1890" height="941" alt="image" src="https://github.com/user-attachments/assets/3be913b8-1ce9-4198-b9bf-e08f78e727c5" />


các  tool

<img width="1684" height="821" alt="image" src="https://github.com/user-attachments/assets/d3b2d6e7-fe2b-4d0d-b693-7d09fc4d8a59" />

quản trị user
<img width="1670" height="553" alt="image" src="https://github.com/user-attachments/assets/73091e30-6ebb-44cd-b48f-7a10b01ad54e" />


tool scan bảo mật và lỗ hổng
<img width="1918" height="866" alt="image" src="https://github.com/user-attachments/assets/6aac8b3f-5f7b-4435-8356-a7acb293cfe7" />



II. Công cụ kiểm tra hiệu suất:

## 1. Công Cụ Đo Lường Hiệu Suất

### 1.1. Apache Benchmark (ab)

#### 1.1.1. Khái Niệm và Vai Trò

Apache Benchmark (thường gọi tắt là `ab`) là công cụ mã nguồn mở chạy trên giao diện dòng lệnh, được thiết kế để đo lường hiệu năng của máy chủ web thông qua giao thức HTTP/HTTPS. Mặc dù ban đầu được phát triển để kiểm tra riêng Apache HTTP Server, công cụ này hiện có thể kiểm thử bất kỳ loại web server hoặc ứng dụng web nào (Nginx, IIS, Node.js, Python Flask,...).

Vai trò chính của `ab` là giúp kỹ sư hệ thống trả lời câu hỏi cốt lõi: "Máy chủ hiện tại có thể chịu tải được bao nhiêu lượng truy cập đồng thời trước khi bị nghẽn hoặc từ chối dịch vụ?"

Công cụ này được dùng cho các mục đích sau:

- **Kiểm thử tải (Load Testing):** Đánh giá khả năng xử lý của server dưới mức tải dự kiến trong môi trường sản xuất, đảm bảo hệ thống vận hành ổn định khi người dùng thực tế truy cập.
- **Kiểm thử giới hạn (Stress Testing):** Đẩy lượng yêu cầu lên mức cực cao để tìm ra "điểm gãy" (bottleneck) của hệ thống, quan sát cách server phản ứng và phục hồi khi bị quá tải.
- **Đo lường tối ưu hóa (Performance Benchmarking):** Sử dụng làm thước đo so sánh hiệu năng trước và sau khi thay đổi cấu hình (ví dụ: so sánh hiệu năng trước và sau khi triển khai Load Balancer hoặc tinh chỉnh cấu hình Nginx).

#### 1.1.2. Ưu Điểm và Hạn Chế

**Ưu điểm:**

- Gọn nhẹ, tiêu tốn rất ít tài nguyên phần cứng của máy thực hiện test.
- Tích hợp sẵn trong hệ sinh thái Linux qua gói `apache2-utils`, không cần cài đặt phức tạp.
- Cú pháp đơn giản, chỉ cần một dòng lệnh để giả lập hàng nghìn kết nối đồng thời.

**Hạn chế:**

- Chỉ có thể gửi các yêu cầu tĩnh đến một endpoint cố định. Không thể giả lập hành vi người dùng phức tạp như lướt nhiều trang, đăng nhập theo kịch bản, hay thực thi JavaScript.
- Không hỗ trợ tốt việc quản lý cookie/session phức tạp trong các bài test dài hơi so với các công cụ chuyên dụng như JMeter hay K6.

#### 1.1.3. Cài Đặt

`ab` nằm trong gói `apache2-utils`. Cài đặt bằng lệnh:

```bash
sudo apt update
sudo apt install apache2-utils -y
```

Kiểm tra phiên bản sau khi cài đặt:

```bash
ab -V
```

<img width="994" height="156" alt="image" src="https://github.com/user-attachments/assets/ffec1332-6c67-4ed6-8918-5a74d5f390a7" />

#### 1.1.4. Thực Hiện Kiểm Thử

Cú pháp cơ bản:

```bash
ab -n [tổng_số_request] -c [số_request_đồng_thời] [URL]
```

Trong đó:
- `-n` (number): Tổng số lượng yêu cầu cần gửi.
- `-c` (concurrency): Số lượng yêu cầu được gửi đồng thời trong mỗi đợt.

**Ví dụ:** Gửi tổng cộng 1000 yêu cầu với 100 yêu cầu chạy đồng thời:

```bash
ab -n 1000 -c 100 http://example.com/
```

> **Lưu ý:** URL bắt buộc phải kết thúc bằng dấu gạch chéo `/` khi kiểm thử thư mục gốc của domain (ví dụ: `http://example.com/` thay vì `http://example.com`).

Chạy thử trên localhost:

```bash
ab -n 100 -c 10 http://127.0.0.1/
```

<img width="919" height="857" alt="image" src="https://github.com/user-attachments/assets/9c68e5fa-4d40-4737-87e4-a43f8eff3ef4" />

<img width="804" height="756" alt="image" src="https://github.com/user-attachments/assets/1904896e-93e8-4e84-9fd6-cced9cad3b92" />

#### 1.1.5. Đọc Hiểu Kết Quả

Sau khi chạy xong, `ab` in ra bảng thống kê. Các chỉ số quan trọng cần chú ý:

- **Failed requests:** Số lượng yêu cầu bị lỗi. Giá trị lý tưởng là 0. Nếu cao, máy chủ đang bị quá tải hoặc có lỗi cấu hình.
- **Requests per second (RPS):** Số yêu cầu máy chủ xử lý thành công mỗi giây. Đây là chỉ số quan trọng nhất để đánh giá sức chịu tải - giá trị càng cao càng tốt.
- **Time per request (mean):** Thời gian trung bình để xử lý một lượt yêu cầu đồng thời.
- **Time per request (mean, across all concurrent requests):** Thời gian trung bình thực tế cho mỗi yêu cầu đơn lẻ - giá trị càng thấp tốc độ phản hồi càng nhanh.
- **Bảng phân vị cuối:** Ví dụ `90% 250` có nghĩa là 90% yêu cầu được xử lý trong vòng 250 milliseconds. Chỉ số này cho biết trải nghiệm của phần lớn người dùng.

---

### 1.2. Apache JMeter

#### 1.2.1. Tổng Quan

JMeter là công cụ kiểm thử tải và hiệu năng mã nguồn mở, được viết bằng Java và phát triển bởi Apache Software Foundation. Khác với `ab` chỉ có thể test một endpoint tĩnh, JMeter có khả năng mô phỏng các kịch bản người dùng phức tạp và hỗ trợ nhiều loại giao thức: Web (HTTP/HTTPS), SOAP, JDBC (Database), LDAP, JMS, Mail (SMTP/POP3/IMAP) và nhiều giao thức khác.

JMeter hỗ trợ hai loại kiểm thử chính:

- **Load Testing:** Mô phỏng số lượng người dùng dự kiến truy cập đồng thời, xác minh hệ thống có đáp ứng được yêu cầu hiệu năng trong điều kiện bình thường hay không.
- **Stress Testing:** Tăng dần số người dùng vượt quá giới hạn dự kiến để tìm ra điểm giới hạn tối đa mà hệ thống có thể xử lý trước khi bắt đầu từ chối yêu cầu.

**Đặc điểm nổi bật:**

- Giao diện đồ họa (GUI) trực quan, dễ sử dụng.
- Hỗ trợ đa luồng, có thể tạo hàng nghìn kết nối đồng thời.
- Đa nền tảng: chạy trên Windows, Linux, macOS.
- Khả năng mở rộng cao thông qua hệ thống plugin phong phú.
- Cộng đồng lớn, nhiều tài liệu hướng dẫn.

#### 1.2.2. Cài Đặt

**Bước 1: Cài đặt Java (bắt buộc)**

JMeter yêu cầu Java Runtime Environment (JRE) phiên bản 8 trở lên. Kiểm tra xem Java đã được cài chưa:

```bash
java -version
```

Nếu chưa có hoặc cần cài mới, tải JDK 17 hoặc JDK 21 từ trang chủ Oracle hoặc Adoptium.

**Bước 2: Tải file JMeter**

Truy cập trang chủ Apache JMeter, tải file `.zip` trong mục **Binaries** (không phải Source).

**Bước 3: Giải nén và khởi động**

Giải nén file tải về, vào thư mục con `bin` và chạy file `jmeter.bat` (Windows) hoặc `jmeter.sh` (Linux/macOS).

<img width="1570" height="873" alt="image" src="https://github.com/user-attachments/assets/153cd4b3-ea41-4a00-8371-9a32d2b75710" />

#### 1.2.3. Hướng Dẫn Sử Dụng Cơ Bản

**Bước 1: Tạo Thread Group**

Click chuột phải vào **Test Plan** → **Add** → **Threads (Users)** → **Thread Group**.

<img width="1485" height="765" alt="image" src="https://github.com/user-attachments/assets/805a188a-648b-4a9a-a7d9-6b942df2e30f" />

Cấu hình các tham số trong Thread Group:

- **Number of Threads:** Số lượng người dùng ảo (ví dụ: 100).
- **Ramp-Up Period:** Thời gian (giây) để JMeter khởi động đủ số thread đã cấu hình. Ví dụ: 100 threads với Ramp-Up 100 giây → mỗi giây khởi động thêm 1 thread. Tham số này quan trọng để mô phỏng tải tăng dần tự nhiên thay vì đột ngột.
- **Loop Count:** Số lần lặp lại kịch bản test (ví dụ: 5).

<img width="1076" height="687" alt="image" src="https://github.com/user-attachments/assets/c38cf885-587d-47af-b701-f708cf37f76a" />

**Bước 2: Thêm HTTP Request Defaults**

Click chuột phải vào Thread Group → **Add** → **Config Element** → **HTTP Request Defaults**.

Điền thông tin máy chủ cần kiểm thử vào đây để không phải lặp lại ở mỗi HTTP Request riêng lẻ.

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/c6cf40a2-586d-47c0-b955-977f32c54167" />

**Bước 3: Thêm HTTP Request Sampler**

Click chuột phải vào Thread Group → **Add** → **Sampler** → **HTTP Request**.

<img width="1214" height="441" alt="image" src="https://github.com/user-attachments/assets/462df686-fa8d-47d7-9f84-ff6ced8ec8c9" />

Trong trường **Path**, nhập đường dẫn URL cần kiểm thử (để trống nếu kiểm thử trang chủ).

**Bước 4: Thêm Graph Results**

Click chuột phải vào Thread Group → **Add** → **Listener** → **Graph Results**.

<img width="1032" height="665" alt="image" src="https://github.com/user-attachments/assets/9fb840b0-8f2e-462d-8bce-d0246480468b" />

**Bước 5: Chạy kiểm thử**

Nhấn nút **Start** trên thanh công cụ hoặc tổ hợp phím `Ctrl + R`.

<img width="1526" height="855" alt="image" src="https://github.com/user-attachments/assets/293dd9ad-e266-4f2d-b348-17ed7da3bf51" />

Kết quả theo thời gian thực trên biểu đồ:

<img width="1468" height="848" alt="image" src="https://github.com/user-attachments/assets/9fcd1c58-fc4f-43b8-8894-bdbd5acf0387" />

#### 1.2.4. Phân Tích Kết Quả

<img width="1120" height="751" alt="image" src="https://github.com/user-attachments/assets/4726599b-cef2-45b0-aaf2-38ac3bebc1a4" />

Khi phân tích hiệu năng web server qua JMeter, cần tập trung vào hai chỉ số chính:

- **Throughput:** Chỉ số quan trọng nhất, thể hiện số lượng yêu cầu mà server xử lý được trong một đơn vị thời gian. Trong ví dụ kiểm thử trên, Throughput của máy chủ đạt 302.841 yêu cầu/phút, tức là máy chủ có thể xử lý trên 300 nghìn yêu cầu mỗi phút. Giá trị Throughput càng cao, máy chủ càng có khả năng chịu tải tốt.

- **Deviation (Độ lệch chuẩn):** Thể hiện mức độ dao động của thời gian phản hồi so với giá trị trung bình. Giá trị Deviation càng nhỏ thì thời gian phản hồi càng ổn định và nhất quán, cho thấy hệ thống đang hoạt động đều đặn. Trong ví dụ trên, Deviation là 130 milliseconds.

---

Dưới đây là phần tổng quan chi tiết về công cụ Siege, được cấu trúc chuẩn mực, cực kỳ phù hợp để bạn đưa thẳng vào mục phân tích công cụ đo lường hiệu năng trong báo cáo kỹ thuật.

TỔNG QUAN VỀ CÔNG CỤ SIEGE (HTTP LOAD TESTING)
1. Khái niệm cơ bản
Siege là một tiện ích dòng lệnh (CLI) mã nguồn mở, được thiết kế chuyên biệt để đo lường hiệu năng (benchmarking) và kiểm thử tải (stress testing) cho các máy chủ Web. Nó cho phép các quản trị viên hệ thống đánh giá mức độ chịu tải của hạ tầng bằng cách giả lập hàng trăm, thậm chí hàng ngàn người dùng truy cập đồng thời vào hệ thống.

2. Nguyên lý hoạt động và Tính năng nổi bật
Điểm làm nên sự khác biệt của Siege so với các công cụ test tải tĩnh khác là khả năng giả lập hành vi người dùng thực tế (Internet User Simulation).

Kiểm thử đa luồng (Multi-threading): Hỗ trợ tạo ra lượng lớn các kết nối đồng thời (Concurrent users) để đẩy máy chủ đến giới hạn xử lý.

Hỗ trợ đa giao thức: Hoạt động ổn định với cả HTTP và HTTPS, hỗ trợ HTTP/1.0 và HTTP/1.1.

Quản lý phiên (Session Management): Có khả năng xử lý Cookies, Basic Authentication, giúp test được cả các trang web yêu cầu đăng nhập.

Chế độ Đọc file (URL Reading): Đây là tính năng "ăn tiền" nhất của Siege. Thay vì chỉ dội "bom" request vào một URL duy nhất, Siege cho phép đọc một danh sách hàng chục URL từ file text (urls.txt). Nó sẽ truy cập ngẫu nhiên vào các link này, mô phỏng chính xác cách một đám đông người dùng đang duyệt qua nhiều bài viết/trang sản phẩm khác nhau trên website.

3. Các chỉ số đo lường (Metrics) quan trọng
Sau khi hoàn thành kịch bản test, Siege sẽ xuất ra một bảng báo cáo tóm tắt với các thông số trọng yếu sau:

Transactions (Số giao dịch): Tổng số request (bao gồm cả việc tải trang HTML và các tài nguyên đi kèm) đã được máy chủ phục vụ thành công.

Availability (Độ khả dụng): Tỷ lệ phần trăm các request thành công so với tổng số request gửi đi (thường yêu cầu đạt 100% trong điều kiện lý tưởng).

Elapsed time (Thời gian kiểm thử): Tổng thời gian diễn ra quá trình test.

Response time (Thời gian phản hồi): Thời gian trung bình để máy chủ phản hồi lại một request. Đây là chỉ số quan trọng nhất để đánh giá trải nghiệm người dùng (TTFB).

Transaction rate (Tốc độ giao dịch): Số lượng giao dịch máy chủ xử lý được trong một giây (tương đương RPS - Request Per Second).

Throughput (Băng thông): Tổng lượng dữ liệu (MB/GB) truyền tải mỗi giây giữa client và server.

Concurrency (Độ đồng thời): Số lượng kết nối duy trì đồng thời thực tế trong suốt quá trình test.

4. Cú pháp lệnh thực tế (Tham khảo cho báo cáo)
Để báo cáo thêm phần thuyết phục, bạn có thể đưa vào một vài ví dụ minh họa cách triển khai lệnh Siege trong môi trường thực tế:

Ví dụ 1: Test tải cơ bản một trang web
Giả lập 100 người dùng truy cập đồng thời, kéo dài liên tục trong vòng 1 phút:

Bash
siege -c 100 -t 1M https://example.com
(Trong đó: -c là số lượng concurrent, -t là thời gian).

Ví dụ 2: Test giả lập hành vi người dùng thực tế
Đọc danh sách URL từ file và có độ trễ ngẫu nhiên (delay từ 1-3 giây) giữa các lần click chuột để tránh bị Web Firewall (như ModSecurity) chặn quá nhanh:

Bash
siege -c 50 -d 3 -f urls.txt
(Trong đó: -d 3 là delay ngẫu nhiên từ 1 đến 3 giây, -f urls.txt là file chứa danh sách link).







5. Đánh giá ưu/nhược điểm (So sánh nhanh)
Ưu điểm: Nhẹ, chạy trực tiếp trên Terminal của Linux mà không cần giao diện đồ họa. Kịch bản test đa dạng (random URL) sát với thực tế hơn so với ApacheBench (ab).

Nhược điểm: Không có biểu đồ phân tích trực quan như JMeter. Nếu chạy với số lượng luồng quá lớn từ một IP duy nhất, rất dễ bị tường lửa ứng dụng (WAF) hoặc hệ thống chống DDoS của server gốc chặn ngay lập tức, dẫn đến kết quả test bị sai lệch (nhận toàn mã lỗi 403 hoặc 503).


Tiếp nối phần lý thuyết ở trên, đây là tài liệu hướng dẫn thực hành chi tiết (Lab Guide) cho công cụ **Siege**. Phần này được thiết kế theo từng bước (Step-by-step), rất phù hợp để bạn đưa vào mục **"Triển khai thực tế"** hoặc dùng làm kịch bản (script) khi đứng thuyết trình demo.

---

## HƯỚNG DẪN TRIỂN KHAI VÀ DEMO KIỂM THỬ TẢI VỚI SIEGE

### PHẦN 1: CÀI ĐẶT (INSTALLATION)

Siege cực kỳ nhẹ và có sẵn trên hầu hết các kho ứng dụng mặc định của Linux. Bạn thao tác trực tiếp trên Terminal của máy ảo hoặc VPS dùng để làm máy tấn công (Attacker Machine).

**Trên Ubuntu / Debian:**

```bash
sudo apt update
sudo apt install siege -y

```



**Kiểm tra cài đặt thành công:**

```bash
siege -V

```

<img width="935" height="310" alt="image" src="https://github.com/user-attachments/assets/52bda36b-72bb-4d50-b6cb-01d72f9cb35e" />


---

### PHẦN 2: CẤU HÌNH CƠ BẢN (CONFIGURATION)

Dù Siege có thể chạy ngay bằng các tham số trên dòng lệnh, nhưng để làm báo cáo chuyên nghiệp, bạn nên cấu hình file `urls.txt` để biểu diễn tính năng "giả lập hành vi người dùng" đặc trưng của nó.

**Bước 1: Khởi tạo file cấu hình mặc định (tùy chọn)**
Chạy lệnh sau để Siege tự động tạo file cấu hình `.siegerc` tại thư mục gốc của user:
/home/dong/.siege/siege.conf

```bash
siege.config

```

**Bước 2: Tạo danh sách các URL cần test (Cực kỳ quan trọng cho Demo)**
Tạo một file text chứa các đường dẫn mà bạn muốn giả lập người dùng click vào.

```bash
nano urls.txt

```
<img width="613" height="364" alt="image" src="https://github.com/user-attachments/assets/0dd90361-7f92-4e6a-a0b6-0edbd2d43a54" />



Lưu lại và thoát `nano` (`Ctrl + O` -> `Enter` -> `Ctrl + X`).

---

### PHẦN 3: KỊCH BẢN DEMO THỰC TẾ (PRACTICAL DEMO)

Trong buổi demo hoặc trong báo cáo, bạn nên chia làm 2 kịch bản để thấy rõ sự khác biệt.

#### Kịch bản 1: Basic Stress Test (Dội bom 1 URL duy nhất)

**Mục tiêu:** Kiểm tra xem trang chủ của Web Server chịu được bao nhiêu kết nối cùng lúc trước khi quá tải.
**Câu lệnh:**

```bash
siege -c 50 -t 30S http://192.168.1.50/

```


<img width="1061" height="398" alt="image" src="https://github.com/user-attachments/assets/351b4bb0-4bb8-44d5-ace5-b8a538323286" />

*Giải thích tham số:*

* `-c 50`: Giả lập 50 người dùng truy cập cùng một lúc (Concurrent).
* `-t 30S`: Chạy bài test liên tục trong 30 giây (Có thể đổi thành `1M` cho 1 phút).

#### Kịch bản 2: Advanced User Simulation (Test ngẫu nhiên theo luồng thực tế)

**Mục tiêu:** Giả lập hành vi người dùng lướt web thực tế: đọc ngẫu nhiên các trang khác nhau và có độ trễ nghỉ giữa các lần chuyển trang (tránh bị Firewall block).
**Câu lệnh:**

```bash
siege -c 100 -d 3 -t 1M -i -f urls.txt

```


<img width="826" height="780" alt="image" src="https://github.com/user-attachments/assets/331c5c36-4ff4-4e46-a8b1-034b14807389" />

<img width="756" height="380" alt="image" src="https://github.com/user-attachments/assets/054b62bf-9f54-42df-8502-1e0221a021ff" />


*Giải thích tham số:*

* `-c 100`: Giả lập 100 người dùng.
* `-d 3`: Thời gian nghỉ ngẫu nhiên (Delay) từ 1 đến 3 giây giữa mỗi luồng request (giống hành vi người đang đọc web).
* `-i`: (Internet Simulation) Chạy các URL trong file `urls.txt` một cách ngẫu nhiên.
* `-t 1M`: Chạy trong 1 phút.
* `-f urls.txt`: Chỉ định file chứa danh sách URL.

---

### PHẦN 4: ĐỌC HIỂU KẾT QUẢ BÁO CÁO (READING THE METRICS)

Sau khi chạy xong lệnh, Siege sẽ trả về một bảng kết quả. Đây là phần bạn sẽ copy để dán vào Word làm minh chứng:

```text
** SIEGE 4.1.3
** Preparing 100 concurrent users for battle.
The server is now under siege...
Lifting the server siege...

Transactions:		        2500 hits
Availability:		      100.00 %
Elapsed time:		       59.90 secs
Data transferred:	        5.50 MB
Response time:		        0.12 secs
Transaction rate:	       41.74 trans/sec
Throughput:		        0.09 MB/sec
Concurrency:		        5.01
Successful transactions:        2500
Failed transactions:	           0

```

 ## III. Công cụ bảo mật:

I ) Giới Thiệu chung về Nmap
Như chúng ta đã biết, Nmap với phiên bản mới nhất hiện nay là 7.80 được ra mắt tại DEFCON 27 là một công cụ quét mạng với mã nguồn mở, được sử dụng rất phổ biến bởi các chuyên gia an ninh bởi tính hiệu quả, minh bạch mà nó đem lại vì một công cụ có mã nguồn mở có thể cho phép cộng đồng An ninh mạng trên thế giới cùng nhau tham gia đóng góp và xây dựng, phát triển.

Trong bài viết này tôi sẽ hướng dẫn các bạn sử dụng công cụ Nmap để scan tìm ra hệ điều hành, cụ thể các service đang chạy và hơn thế nữa với đối tượng là website Viblo.asia. Bài viết này được viết với duy nhất mục đích giáo dục và tôi không chịu bất kì trách nhiệm nào và không khuyến khích về việc dựa theo bài viết này để khai thác các thông tin và tiến hành xâm nhập khi không có sự cho phép.

1. Cách thức hoạt động
Nmap sử dụng các IP trên các gói tin theo những cách đặc biệt khác nhau để có thể xác định các host trên một hệ thống mạng , để rồi từ đó xác định xem những services đang chạy trên hệ thống đó, hệ điều hành đang chạy, bộ lọc các gói tin cũng như tường lửa đang sủ dụng là gì.

2. Tính năng của Nmap
Phát hiện lỗ hổng bảo mật
khai thác lỗ hổng bảo mật
phát hiện ra backdoor
quét mạng network
quét các máy chủ và các cổng trên máy chủ trên hệ thống
xác định hệ điều hành, service, firewall đang sử dụng
cung cấp thông tin về loại thiết bị, tên DNS, địa chỉ Mac
thực thi các đoạn script NSE hoặc Lua với các đối tượng được kiểm thử
II ) Hướng dẫn tải và cài đặt
Nmap có thể sử dụng trên Windows và macOS cũng như được hỗ trợ trên các bản phân phối của Linux bao gồm Red Hat, Mandrake, SUSE và Fedora và đồng thời hoạt động được trên các hệ điều hành khác gồm BSD, Solaris, AIX và AmigaOSfaffa
---


Đối với Debian based ta sử dụng câu lệnh: #sudo apt install nmap

Đối với Red Hat Based ta sử dụng câu lệnh : #yum install nmap

Và ấn "Y" để xác nhận cài


Sau khi cài xong Nmap ta có thể kiểm tra đã cài đặt thành công chưa và phiên bản là gì bằng cách sử dụng lệnh : #nmap --version

<img width="1556" height="210" alt="image" src="https://github.com/user-attachments/assets/6a066458-f41a-44e7-922b-7e3f8e4ceaec" />

III ) Sử dụng Nmap
1. NSE script
Nmap Scripting Engine (NSE) là 1 trong những tùy chọn khá lợi hại trong Nmap. Nó cho phép người dùng viết và chia sẻ những đoạn script đơn giản để thực hiện những công việc khác nhau trong lĩnh vực networking một cách tự động. Những đoạn script này có thể sử dụng để phát hiện các lỗ hổng và khai thác các lỗ hổng.

Để thực hiện chức năng này của nmap sử dụng tùy chọn : –-script [tên_script].nse

Trong nmap chúng ta gồm có 4 loại NSE scripts như sau :

host script : là một loại script được thực thi sau khi nmap đã thực hiện tác vụ của nó như quét những host, cổng mạng hay hệ điều hành,…
prerule script : là một loại script được thực thi trước bất kì 1 tác vụ quét hay thu thập thông tin nào
service script : là loại script chạy để chống lại 1 service đang lắng nghe ở trên mục tiêu đang pentest
postrule sciprt : là loại script chạy sau khi Nmap thực thi scan mục tiêu
Vị trí của NSE script : Các script (.nse file) nằm trong thư mục script khi cài đặt nmap, người dùng có thể tùy biến chỉnh sửa, thêm các scirpt khác.

Trên mỗi hệ điều hành khác nhau chúng sẽ được lưu trữ ở một vị trí khác nhau :

Trên linux : ~/.nmap/scripts/ hoặc /usr/share/nmap/scripts/ hoặc trong $NMAPDIR. Hoặc với cách dễ nhất là chạy lệnh :locate *.nse
Trên Mac : /usr/local/Cellar/nmap//share/nmap/scripts/.
Trên windows : C:\Program Files (x86)\Nmap\scripts or C:\Program Files\Nmap\scripts
Ngoài ra trong tường hợp bạn muốn update các script NSE trên Nmap hãy sử dụng lệnh : sudo nmap --script-updatedb
Dưới đây là 1 số ví dụ về sử dụng NSE Script để scan các lỗ hổng bảo mật trên hệ thống

SSL-HEARTBLEED : Phát hiện lỗ hổng bảo mật OpenSSL Heartbleed bug (CVE-2014-0160).

Tải về : https://svn.nmap.org/nmap/scripts/ssl-heartbleed.nse

Lệnh : #sudo nmap -sV -p 443 –script=ssl-heartbleed.nse <IP_của_mục tiêu>


<img width="1239" height="778" alt="image" src="https://github.com/user-attachments/assets/7c1b37c9-fb6d-4eb0-830b-0c3390fb3352" />

NMAP-VULNERS

NSE script giúp cung cấp thông tin về lỗ hổng có thể trên hệ thống dựa theo API của Vulners.com

Github : https://github.com/vulnersCom/nmap-vulners

Lệnh : #sudo nmap -sV --script vulners [--script-args mincvss=] <IP_của_mục tiêu>

<img width="983" height="378" alt="image" src="https://github.com/user-attachments/assets/31da0f6f-6236-492e-ab13-a46a22e39a4e" />

FREEVULNSEARCH

NSE script to query vulnerabilities via the cve-search.org API.

Github : https://github.com/OCSAF/freevulnsearch

Lệnh : #sudo nmap -sV --script freevulnsearch <IP_của_mục tiêu>

<img width="1155" height="249" alt="image" src="https://github.com/user-attachments/assets/b4c40248-995d-4f1d-8537-03ced533376a" />

2. Trạng thái các cổng trong Nmap
Trong quá trình scan ta có thể thấy trạng thái các cổng như sau :

Open: Đang có một dịch vụ thực hiện kết nối ra bên ngoài nhưng không bị giám sát bởi tường lửa.
Closed: Máy mục tiêu vẫn nhận và phản hồi, nhưng ko có ứng dụng nào đang nghe trên cổng đó. Khi đó cổng được báo là đóng vẫn có thể cho ta biết host đang sống
Filtered: Đã có sự ngăn chặn bởi tường lửa, bạn sẽ chẳng nhận được bất cứ phản hồi gì từ mục tiêu cả.
Unfiltered: Không bị chặn, nhưng không thể biết được cổng đóng hay mở.
Open/Filtered: không biết là cổng mở hay bị lọc. Nó xảy ra đối với kiểu quét mà cổng dù mở nhưng không phản hồi gì cả nên biểu hiện của nó giống như bị lọc.
Closed/Filtered: Trạng thái xuất hiện khi Nmap không biết được port đó đang Closed hay Filtered. Nó được sử dụng cho quét IPID Idle.
3. Các lệnh phổ biến trong Nmap
Kiểm tra xem host còn alive không : #nmap -sn [IP_của_mục tiêu]
Kiểm tra hệ điều hành của server : #nmap -O [IP_của_mục tiêu]
Quét một port cụ thể : #nmap -p [số_cổng] [IP_của_mục tiêu]
Quét kết nối TCP, Nmap sẽ thực hiện việc quét bắt tay 3 bước : #nmap -sT [IP_của_mục tiêu]
Quét kết nối UDP : #nmap -sU [IP_của_mục tiêu]
Quét xác định phiên bản của dịch vụ đang chạy trên host : #nmap -PN -p [số_cổng] -sV [IP_của_mục tiêu]
Và còn rất nhiều các đối số nữa các bạn có thể tham khảo dưới đây : https://hackersonlineclub.com/nmap-commands-cheatsheet/




Công cụ dò quét lỗ hổng OpenVAS (GVM)
Bài đăng này đã không được cập nhật trong 6 năm

Công cụ dò quét lỗ hổng OpenVAS (GVM)
Dò quét, quản lý lỗ hổng là một bước quan trọng trong quá trình pentest cũng như duy trì hệ thống và có sẵn một công cụ như vậy thì sẽ có ích cho bạn rất nhiều. Vì vậy, trong bài viết này mình sẽ giới thiệu đến các công cụ dò quét lỗ hổng OpenVAS. Open Vulnerability Assessment System (OpenVAS) là một công cụ dò quét lỗ hổng có mã nguồn mở được đánh giá cao, nó được duy trì và phát triển bởi Greenbone Network.

1. Kiến trúc của OpenVAS
OpenVAS có 3 thành phần chính: Greenbone Vulnerability Manager (GVMd), Greenbone Security Assistant (GSA), OpenVAS Scanner.

<img width="1026" height="557" alt="image" src="https://github.com/user-attachments/assets/e0df6681-55aa-4131-955c-cfe8fefd7b94" />


GVMd: là trung tâm dịch vụ, hợp nhất các công cụ quét lỗ hổng đơn giản thành một giải pháp quản lý lỗ hổng đầy đủ. GVMd điều khiển OpenVAS Scanner qua một giao thức nội bộ và cung cấp thêm một giao thức chung Open Scanner Protocol (OSP) để có thể tích hợp các máy quét khác. Bản thân GVMd cũng cung cấp một giao thức dựa trên XML khác, Greenbone Management Protocol (GMP) để các thành phần khác có thể gửi những yêu cầu xử lý như tạo người dùng, tạo lịch quét, tạo một mục tiêu quét, tạo một nhiệm vụ để quét mục tiêu, bắt đầu nhiệm vụ quét một mục tiêu,… .GVMd cũng kiểm soát cơ sở dữ liệu SQL ( Postgres ở phiên bản GVM 10 và SQLite 3 cho các phiên bản trước) nơi tất cả dữ liệu kết quả và cấu hình quét được lưu trữ tập trung.

GSA: là giao diện web của GVM. Nó kết nối với GVMd để cung cấp giao diện cho người dùng với đầy đủ những tính năng của công cụ quản lý lỗ hổng. GSA bao gồm các thành phần: • GSA - ứng dụng web được viết bằng React. • GSAD – Là HTTP server giao tiếp với GVMd thông qua giao thức GMP.

OpenVAS Scanner: là một công cụ quét đầy đủ tính năng để thực thi một mẫu thử NVT nhằm mục đích kiểm tra một mục tiêu quét có vi phạm các lỗ hổng bảo mật, chưa cập nhật các bản vá cho hệ thống và các mục đích khác của một công cụ dò quét lỗ hổng. Và hầu hết Openvas Scanner sẽ thực hiện các nhiệm vụ và trả lại kết quả mà GVMd gọi đến.


Đúng là dạo này hãng Greenbone "quay xe" liên tục. Họ vừa mới tung ra bản cập nhật cấu trúc mới nhất, bỏ luôn cái số phiên bản 22.4 ở đuôi và đổi tên file từ docker-compose.yml thành compose.yaml chuẩn theo xu hướng Docker mới. Vì vậy cái link cũ báo lỗi 404 là điều hiển nhiên.

Cài đặt 
Bước 1: Tải file cấu hình (Link mới cập nhật)

Bash
curl -f -L https://greenbone.github.io/docs/latest/_static/compose.yaml -o compose.yaml
Bước 2: Tải các image của OpenVAS về (sẽ mất khoảng vài phút)

Bash
sudo docker compose -f compose.yaml pull

<img width="1498" height="847" alt="image" src="https://github.com/user-attachments/assets/ca14dbb7-3a89-4994-acc2-dab547b28bcd" />

Bước 3: Khởi chạy toàn bộ hệ thống ngầm

Bash
sudo docker compose -f compose.yaml up -d
(Lưu ý nhỏ: Ở phiên bản cấu hình mới này, bạn không cần phải thêm cái đuôi -p greenbone-community-edition lằng nhằng như trước nữa, trong file họ đã tự động cấu hình sẵn rồi).

Cách truy cập sau khi cài xong:
Với bản cấu hình mới nhất này, khi chạy xong, bạn sẽ truy cập vào Giao diện Web (GSA) qua cổng 9392. Tuy nhiên, hãng đã ép dùng bảo mật HTTPS và tự tạo chứng chỉ tự cấp (self-signed).

Bạn mở trình duyệt lên và gõ:
👉 https://<IP-máy-ảo>:9392
(Nhớ là có chữ https nhé, nếu báo lỗi "Not secure / Không bảo mật", bạn cứ bấm Advanced -> Proceed là vào được).

Tài khoản mặc định vẫn là:

User: admin

Pass: admin

Chạy thử ngay xem các container đã "lên đèn" chưa nhé! Thêm luôn dòng lệnh tải bản mới này vào báo cáo, sếp nhìn thấy bạn cập nhật đúng trend công nghệ mới nhất chắc chắn sẽ cộng thêm điểm đấy!


ModSecurity cho tường lửa ứng dụng web.

Mod Security là gì?
Mod_security la gi

ModSecurity là bộ công cụ giúp theo dõi, kiểm soát các quá trình hoạt động và các truy cập vào trang web với thời gian thực. Đến với ModSecurity bạn có thể làm những gì bạn muốn bằng việc lựa chọn các tính năng có sẵn phù hợp, không nhất thiết phải cứng nhắc theo một khuôn khổ nhất định.

Chi tiết hơn thì mod_security là một module của Apache, được xây dựng với mục đích chính là đảm bảo an toàn cho các trang web.

ModSecurity còn tên gọi khác là mod_security, security2_module hoặc modsec, nó hoạt động như Firewall của ứng dụng web giúp trang web tránh khỏi các cuộc tấn công của đối tượng xấu. Tùy theo từng bộ quy tắt được sử dụng mà sẽ chống lại các loại tấn công khác nhau. Với mô hình OSI 7 lớp ứng dụng nó cũng bị ảnh hưởng bởi ModSecurity.

ModSecurity sẽ nhận các yêu cầu từ Apache, sau đó so sánh các yêu cầu với các mô tả trong bộ quy tắt và thực hiện các hành động theo yêu cầu dựa trên kết quả kiểm tra.

Bạn cần hiểu được máy chủ của mình sẽ lưu trữ các loại trang web nào để chọn được bộ quy tắt phù hợp, đồng thời biết được các loại yêu cầu cụ thể từ Apache sẽ giúp ích cho việc hoạt động của trang web. Nếu ModSecurity chặn một trong các yêu cầu dựa vào bộ quy tắt thì các thành phần của các trang web sẽ không hoạt động được nếu cài đặt bộ quy tắt đó.

Bộ quy tắc mod_security
Để mod_security được hoạt động thì cần có bộ quy tắc. SpiderLabs đã hợp tác với OWASP để thành lập nên bộ quy tắt “generic” dùng cho các chức năng từ mod_security.

<img width="1097" height="804" alt="image" src="https://github.com/user-attachments/assets/45437589-499f-4483-8d51-133bd9f93c0b" />


Bạn có tham khảo và tải xuống bộ quy tắt từ: owasp-modsecurity-crs

Bạn cũng có thể tự tạo cho mình bộ quy tắt hoặc mua các bộ quy tắt tùy chỉnh khác. Hãy tìm kiếm trên Google theo nhu cầu mà bạn muốn.

Tích hợp cấu hình và sử dụng MoSecurity với Plesk
Bước 1: Kiểm tra và bật ModSecurity trong Plesk
Đăng nhập vào trang quản trị Plesk với quyền Admin.

Ở menu bên trái, nhấp vào Tools & Settings (Công cụ & Cài đặt).

Tìm đến cột Security (Bảo mật), nhấp vào Web Application Firewall (ModSecurity).

Nếu bạn thấy mục này, nghĩa là ModSecurity đã có sẵn. Bạn chỉ cần chuyển trạng thái sang Detection only (Chỉ phát hiện và ghi log - Khuyên dùng lúc đầu để tránh chặn nhầm người dùng thật) hoặc On (Bật chặn chủ động), sau đó chọn một bộ luật (Rule set) như OWASP hoặc Comodo và lưu lại là xong.

Bước 2: Cài đặt bổ sung (Nếu không thấy mục ở Bước 1)
Nếu bạn vào Security mà không tìm thấy Web Application Firewall, nghĩa là lúc cài Plesk ban đầu bạn chưa tick chọn module này. Bạn cài bổ sung cực kỳ an toàn như sau:

Vẫn ở menu Tools & Settings, nhấp vào Updates (hoặc Updates and Upgrades trong phần Plesk).

Một tab/cửa sổ mới sẽ mở ra, nhấp vào Add/Remove Components (Thêm/Xóa thành phần).

Tìm đến nhóm Web hosting, bấm dấu cộng + để mở rộng danh sách.

Tìm dòng ModSecurity (hoặc Web Application Firewall) và chọn Install.

Bấm Continue ở cuối trang để Plesk tự động tải và cài đặt cấu hình chuẩn chỉ cho cả Apache và Nginx.

Sau khi quá trình chạy xong khoảng vài phút, bạn quay lại Tools & Settings > Web Application Firewall (ModSecurity) để bật nó lên theo hướng dẫn ở Bước 1.

1. Chọn chế độ hoạt động (Tab Tổng quan)
Như trong hình đầu tiên, bạn đang có 3 tùy chọn:

Tắt (Off): Tắt hoàn toàn tường lửa (không khuyến nghị, trừ khi cần cô lập để tìm nguyên nhân lỗi web).

Chỉ phát hiện (Detection only): ModSecurity sẽ quét các luồng truy cập và ghi lại vào nhật ký (log) nếu phát hiện nguy hiểm, nhưng không chặn. Nếu bạn mới cài đặt hoặc web có nhiều tính năng phức tạp, hãy dùng chế độ này trong 1-2 ngày đầu để theo dõi xem hệ thống có nhận diện nhầm các thao tác hợp lệ hay không.

TRÊN (Bật/On): Chế độ bảo vệ chủ động. Mọi truy cập vi phạm quy tắc sẽ bị chặn đứng (thường trả về lỗi 403 Forbidden trên trình duyệt). Bạn hiện đang để chế độ này, rất tốt cho bảo mật.
<img width="1484" height="454" alt="image" src="https://github.com/user-attachments/assets/e96a29e8-b9ab-49a1-bc01-6a6d5f3457cc" />

2. Bộ quy tắc (Rule sets)
Hệ thống của bạn đang sử dụng bộ quy tắc Comodo (miễn phí) chạy trên Apache. Đây là một bộ quy tắc được đánh giá là khá nhẹ nhàng, ổn định và ít gây lỗi "chặn nhầm" hơn so với bộ OWASP miễn phí. Bạn hoàn toàn có thể yên tâm duy trì bộ quy tắc này.

3. Xử lý khi Website bị lỗi 403 (Tab Cài đặt - Tắt quy tắc bảo mật)
Khi để chế độ TRÊN, đôi khi người dùng thao tác bình thường trên web (ví dụ: đăng bài viết có chứa ký tự đặc biệt, lưu file cấu hình WordPress) sẽ bị ModSecurity chặn lại. Hình ảnh thứ hai chính là nơi để bạn "nới lỏng" tường lửa cho các trường hợp đó:

Vô hiệu hóa bằng Mã định danh (Rule ID): Trong hình, bạn đang điền các ID như 210710, 222212, 218500. Đây là cách xử lý chuẩn xác và an toàn nhất.

Cách lấy ID: Khi một thao tác hợp lệ bị chặn, bạn quay lại Tab Tổng quan, kéo xuống phần Nhật ký kiểm toán ModSecurity (Audit Log). Tìm dòng log báo lỗi tương ứng với thời gian bị chặn, bạn sẽ thấy một mã ID (Rule ID). Copy ID đó và dán vào ô này để ModSecurity bỏ qua quy tắc đó.

Vô hiệu hóa theo Thẻ (Tags): Ở phần bên dưới (Tích cực), bạn có thể chọn bỏ qua hẳn một nhóm quy tắc (như Bruteforce, Cửa sau/Backdoor...). Lưu ý: Nên hạn chế tối đa việc dùng cách này vì nó sẽ tắt diện rộng, làm giảm đáng kể sức mạnh bảo vệ của tường lửa. Chỉ nên tắt chính xác từng ID bị lỗi.
<img width="1591" height="647" alt="image" src="https://github.com/user-attachments/assets/26f022f2-c1f4-4c48-8d85-40f0b1f932ec" />



  •  Công cụ quản lý web server:
  o  cPanel, DA, Plesk
	•  Công cụ kiểm tra hiệu suất:
	o  ApacheBench (ab), JMeter, Siege.
	•  Công cụ bảo mật:
	o  Nmap, OpenVAS để quét lỗ hổng.
	o  ModSecurity cho tường lửa ứng dụng web.
