# BÁO CÁO THỰC TẬP NGÀY 11/5 

## I. Tổng quan về FTP

1. FTP là gì ?

FTP (File Transfer Protocol) là giao thức truyền tải tập tin trên mạng TCP/IP, cho phép người dùng tải lên (upload) và tải xuống (download) tập tin giữa máy tính cá nhân và máy chủ (server).

Giao thức FTP hoạt động theo mô hình client-server, trong đó:

Client (máy người dùng) gửi yêu cầu kết nối đến server.
Server (máy chủ FTP) xử lý và phản hồi yêu cầu.

<img width="773" height="391" alt="image" src="https://github.com/user-attachments/assets/b83fe5ab-9cc9-449d-bb56-c7f8ac6937fa" />


2. Vai trò của FTP trong việc truyền tải dữ liệu
Giao thức FTP (File Transfer Protocol) được dùng để truyền tải, quản lý và chia sẻ tệp tin qua mạng Internet hoặc mạng nội bộ. 

Truyền tải tệp tin: Giúp người dùng tải lên (upload) hoặc tải xuống (download) các tập tin giữa máy tính cá nhân và máy chủ từ xa.
Quản lý tệp tin từ xa: Cho phép thực hiện các thao tác như tạo, sửa, xóa, đổi tên thư mục hoặc tập tin ngay trên máy chủ từ xa.
Hỗ trợ phát triển website: FTP là công cụ phổ biến để lập trình viên cập nhật mã nguồn, hình ảnh, nội dung… lên website.
Sao lưu và phục hồi dữ liệu: Dữ liệu quan trọng có thể được sao lưu định kỳ lên máy chủ FTP và tải lại khi cần phục hồi.
Chia sẻ dữ liệu giữa các bộ phận/chi nhánh: Doanh nghiệp sử dụng FTP để đồng bộ và chia sẻ dữ liệu nội bộ một cách an toàn và nhanh chóng.
Tích hợp với hệ thống tự động hóa: FTP có thể được tích hợp vào quy trình tự động để truyền dữ liệu giữa các ứng dụng hoặc hệ thống.


3. các thành phần chính
Để một hệ thống FTP có thể hoạt động trơn tru và truyền tải được dữ liệu, nó bắt buộc phải có sự tương tác của 3 thành phần cốt lõi: **FTP Server**, **FTP Client**, và **Kết nối mạng**.

Dưới đây là chi tiết vai trò và nhiệm vụ của từng thành phần:

### 1. FTP Server (Máy chủ FTP)

Đây là "trái tim" của hệ thống, nơi lưu trữ dữ liệu và cung cấp dịch vụ cho người dùng.

* **Bản chất:** Là một máy tính (hoặc máy chủ ảo/Cloud) được cài đặt các phần mềm chuyên dụng (như FileZilla Server, vsftpd, ProFTPD) và luôn trong trạng thái "bật" để lắng nghe các yêu cầu kết nối.
* **Nhiệm vụ chính:**
* **Lưu trữ dữ liệu:** Cung cấp không gian đĩa cứng để chứa các thư mục và tập tin.
* **Xác thực người dùng:** Kiểm tra tên đăng nhập (Username) và mật khẩu (Password) khi có ai đó muốn truy cập.
* **Phân quyền (Permissions):** Quản lý xem user A được quyền làm gì (chỉ đọc/tải về), user B được quyền làm gì (đọc, ghi, xóa, tạo thư mục mới).
* **Xử lý yêu cầu:** Thực thi các lệnh do máy khách gửi đến (ví dụ: lệnh "gửi cho tôi file A", lệnh "xóa file B").



### 2. FTP Client (Máy khách FTP)

Đây là công cụ nằm ở phía người dùng (người quản trị hoặc khách hàng), dùng để "giao tiếp" với FTP Server.

* **Bản chất:** Là một phần mềm được cài đặt trên máy tính, điện thoại, hoặc đôi khi chỉ là một dòng lệnh.
* **Nhiệm vụ chính:**
* **Khởi tạo kết nối:** Gõ địa chỉ IP (hoặc tên miền) của Server, nhập Username/Password để xin phép kết nối.
* **Giao diện thao tác (UI):** Dịch các thao tác của người dùng (kéo thả bằng chuột, click đúp) thành các tập lệnh FTP gửi lên máy chủ.
* **Truyền nhận dữ liệu:** Tải file từ máy cá nhân lên server (Upload) hoặc tải file từ server về máy cá nhân (Download).


* **Các dạng FTP Client phổ biến:**
* **Phần mềm chuyên dụng:** FileZilla Client, WinSCP (rất được dân IT ưa chuộng), Cyberduck (phổ biến trên Mac). Cung cấp giao diện trực quan gồm 2 cửa sổ (1 bên là máy cá nhân, 1 bên là server) để dễ kéo thả.
* **Tích hợp sẵn trong Hệ điều hành:** Ngay trên Windows File Explorer, bạn có thể gõ `ftp://địa-chỉ-ip` lên thanh địa chỉ để truy cập như một ổ đĩa bình thường.
* **Giao diện dòng lệnh (CLI):** Dùng Command Prompt (Windows) hoặc Terminal (Linux/macOS) gõ lệnh `ftp`. Thường dùng cho các lập trình viên hoặc tự động hóa bằng script.



### 3. Kết nối mạng (Network Connection)

Thiếu thành phần này, Client và Server không thể "nhìn thấy" nhau. Đây là môi trường truyền dẫn tín hiệu.

* **Bản chất:** Có thể là mạng nội bộ (LAN) trong một công ty, hoặc môi trường Internet toàn cầu, dựa trên giao thức TCP/IP.
* **Đặc điểm cốt lõi (Rất quan trọng khi xử lý lỗi):** Mạng FTP không đi qua 1 đường ống, mà nó tạo ra **2 đường ống riêng biệt**:
* **Kênh điều khiển (Port 21):** Giống như "đường dây điện thoại", chỉ dùng để "nói chuyện" (Client bảo: "Cho tôi đăng nhập", Server đáp: "Ok, mật khẩu đúng").
* **Kênh dữ liệu (Port 20 hoặc cổng ngẫu nhiên):** Giống như "đường cao tốc chở hàng", chỉ mở ra khi bắt đầu copy file, copy xong sẽ đóng lại.


* **Yếu tố ảnh hưởng:** Các thiết bị mạng như **Router** và **Firewall (Tường lửa)** nằm ở giữa Client và Server. Nếu Firewall chặn Kênh dữ liệu, bạn sẽ gặp tình trạng "Đăng nhập thành công nhưng không thể xem được danh sách file".

5. So Sánh FTP với các giao thức khác 

1. Tổng quan về FTP Server
•  FTP là gì? (Giao thức truyền file - File Transfer Protocol)
•  Vai trò của FTP server trong việc truyền tải dữ liệu.
•  So sánh FTP với các giao thức khác (SFTP, FTPS, HTTP, SCP).
•  Các thành phần chính: FTP server, FTP client, và kết nối mạng.
2. Nguyên lý hoạt động của FTP
• Mô hình client-server trong FTP.
• Cơ chế kết nối:
o Port 21 (control) và Port 20 (data - active mode).
o Passive mode vs. Active mode.
• Quy trình xác thực (anonymous vs. authenticated users).
3. Các loại FTP Server
• Anonymous FTP: Cho phép truy cập công khai mà không cần tài khoản.
• Authenticated FTP: Yêu cầu tên người dùng và mật khẩu.
• FTPS (FTP Secure): FTP với mã hóa SSL/TLS.
• SFTP (SSH File Transfer Protocol): Giao thức thay thế an toàn hơn.
4. Phần mềm FTP Server phổ biến
• FileZilla Server (miễn phí, mã nguồn mở).
• vsftpd (Very Secure FTP Daemon, phổ biến trên Linux).
• ProFTPD (cấu hình linh hoạt, mã nguồn mở).
• Microsoft IIS FTP (tích hợp trên Windows Server).
• Pure-FTPd, Cerberus FTP, và các phần mềm thương mại khác.
