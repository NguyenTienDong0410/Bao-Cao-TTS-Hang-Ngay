# BÁO CÁO THỰC TẬP NGÀY 29/4

# TÌM HIỂU TỔNG QUAN VỀ WEBSERVER

---

# I. Giao thức và các công nghệ liên quan 

Các mô hình xử lý yêu cầu trong web server:

### 1. Apache Prefork MPM: Mô hình đa tiến trình độc lập (Process-based)

Mô hình này hoạt động dựa trên cơ chế `fork()` của hệ điều hành Linux.

* **Cách thức khởi tạo:** Tiến trình cha (Master Process - chạy bằng quyền `root`) sẽ sao chép chính nó (fork) để tạo ra một lượng các tiến trình con (Child Processes - chạy bằng quyền user thường). Chữ "Pre-fork" nghĩa là nó sinh ra sẵn một nhóm tiến trình con đứng đợi trước khi có request đến.
* **Không gian bộ nhớ (Memory Space):** Mỗi child process là một thực thể độc lập hoàn toàn. Chúng có **không gian bộ nhớ riêng biệt**, môi trường biến riêng biệt.
* **Vấn đề I/O Blocking:** Khi một connection (request) đi vào, một process sẽ nhận nhiệm vụ xử lý nó từ đầu đến cuối. Nếu request đó cần đọc file từ ổ cứng hoặc truy vấn database (tốn thời gian), process đó sẽ rơi vào trạng thái **Block (chặn)**. Nó đứng im chờ đợi, CPU rảnh nhưng không thể phục vụ request khác.
* **Nhược điểm :** * **Context Switching (Chuyển đổi ngữ cảnh):** Khi có 1000 request, CPU phải liên tục chuyển đổi qua lại giữa 1000 process. Quá trình OS lưu lại trạng thái process này, nạp trạng thái process kia ngốn cực kỳ nhiều sức mạnh CPU.
    * ** KeepAlive:** Nếu bật tính năng KeepAlive (giữ kết nối để load ảnh/css/js nhanh hơn), client tải xong trang rồi nhưng chưa đóng trình duyệt, tiến trình đó của Apache vẫn bị "giam lỏng" để giữ kết nối. Kết quả là cạn kiệt số lượng process tối đa (MaxRequestWorkers).



---

### 2. Apache Worker MPM: Mô hình đa luồng (Thread-based)

Để khắc phục nhược điểm ngốn RAM của Prefork, Worker ra đời kết hợp giữa process và thread (luồng).

* **Cách thức khởi tạo:** Master process vẫn sinh ra các child process, nhưng số lượng ít hơn rất nhiều. Thay vào đó, bên trong mỗi child process, nó sử dụng thư viện `pthreads` của C để sinh ra hàng chục **Luồng (Threads)**.
* **Không gian bộ nhớ:** Các luồng (threads) nằm trong cùng một tiến trình sẽ **dùng chung một không gian bộ nhớ (Shared Memory)**. Điều này giúp giảm lượng RAM tiêu thụ xuống mức thấp nhất so với Prefork.
* **Bản chất kỹ thuật:** Mỗi request sẽ được giao cho 1 luồng xử lý (thay vì 1 tiến trình). 
* **Vấn đề cốt lõi:**
    * **Thread-Safety (An toàn luồng):** Vì dùng chung bộ nhớ, nếu hai luồng cùng ghi vào một vùng RAM mà không kiểm soát tốt sẽ gây sập. Tệ hơn, nếu 1 luồng bị lỗi (crash) do thư viện bên thứ ba (như một module PHP bị lỗi), toàn bộ tiến trình đó bị sập theo, kéo theo tất cả các luồng (và người dùng) đang chạy chung tiến trình đó "chết chùm".
    * **Vẫn dính Blocking:** Dù là luồng thì nó vẫn tuân theo cơ chế 1 luồng xử lý 1 connection. 10.000 kết nối KeepAlive nhàn rỗi vẫn sẽ trói chân 10.000 luồng.

---

### 3. Nginx Event-Driven: Mô hình Hướng sự kiện và Bất đồng bộ

Đây là sự thay đổi hoàn toàn về mặt tư duy kiến trúc để giải quyết bài toán C10K (10.000 kết nối đồng thời).

* **Cách thức khởi tạo:** Nginx chỉ tạo ra một vài Worker Process (thường thiết lập bằng đúng số lõi CPU của máy chủ). Ví dụ máy có 4 Core, Nginx chỉ chạy 4 Worker Process. Không sinh thêm process, không đẻ thêm thread.
* **Vòng lặp sự kiện (Event Loop) & Cơ chế `epoll`:** Thay vì gán 1 connection cho 1 luồng, Nginx dựa vào cơ chế I/O Multiplexing của nhân Linux (cụ thể là `epoll` trên Linux). 
    * Khi có hàng ngàn request đến, HĐH Linux chuyển chúng thành các **File Descriptors (FD)**.
    * Tiến trình Worker của Nginx chạy một vòng lặp vô hạn siêu tốc (Event Loop). Nhiệm vụ của nó là hỏi `epoll` của OS: *"Trong hàng ngàn cái FD này, cái nào đã sẵn sàng để đọc/ghi dữ liệu?"*.
* **Non-blocking (Không chặn):** * Worker lấy request A, ném lệnh truy xuất file tĩnh cho OS. Thay vì đứng đợi OS đọc đĩa cứng, Worker lập tức vứt request A đó qua một bên và hỏi `epoll` xem có ai cần phục vụ không, rồi chạy ra phục vụ request B.
    * Khi OS đọc đĩa xong, nó ném ra một "sự kiện" (Event). Ở vòng lặp tiếp theo, Worker nhận được sự kiện, quay lại lấy dữ liệu của request A và trả về cho client.
* **Bản chất sức mạnh:**
    * **Không Context Switching:** Chỉ có 4 process chạy trên 4 CPU core, OS không phải tốn sức chuyển đổi qua lại giữa hàng ngàn process nữa. CPU tập trung 100% công lực để xử lý data.
    * **KeepAlive không còn là gánh nặng:** Một kết nối KeepAlive nhàn rỗi đối với Nginx chỉ là trạng thái của một File Descriptor nằm trong bộ nhớ của `epoll` (tốn khoảng vài byte RAM). Một Worker process có thể dễ dàng quản lý hàng vạn kết nối nhàn rỗi mà không hề hấn gì.

### 4.  Module/Plugin: Các module mở rộng


## 1. Module của Apache

Apache sử dụng kiến trúc module linh hoạt, cho phép bạn bật/tắt các tính năng tùy theo nhu cầu.

### Các module Reverse Proxy

Reverse Proxy là một máy chủ trung gian đứng trước các máy chủ backend (như Node.js, Tomcat, hoặc một server khác), nhận request từ client và chuyển tiếp (forward) đến backend xử lý, sau đó trả kết quả lại cho client.

* **`mod_proxy`**: Đây là module nền tảng (core proxy module) của Apache. Nó cung cấp các tính năng cơ bản để Apache có thể hoạt động như một proxy server. Để sử dụng proxy cho các giao thức cụ thể, bạn bắt buộc phải bật module này kèm theo các module phụ trợ bên dưới.
* **`mod_proxy_http`**: Module phụ trợ của `mod_proxy`. Nó cho phép Apache chuyển tiếp các request dạng **HTTP/HTTPS** đến các máy chủ backend. Đây là module bạn dùng khi muốn Apache đứng trước và proxy traffic cho một ứng dụng Node.js chạy ở port 3000, chẳng hạn.
* **`mod_proxy_fcgi`**: Module phụ trợ để chuyển tiếp request bằng giao thức **FastCGI**. Trong kiến trúc web hiện đại, module này thường được dùng để kết nối Apache với **PHP-FPM**. Nó giúp tách biệt hoàn toàn tiến trình của web server (Apache) và tiến trình xử lý code (PHP), mang lại hiệu suất và khả năng mở rộng cao hơn rất nhiều.

### Quản lý mã nguồn PHP
* **`mod_php`**: Đây là cách thức "cổ điển" để chạy PHP trên Apache. Nó nhúng trực tiếp trình thông dịch PHP vào bên trong từng process (tiến trình) của Apache. 
    * *Ưu điểm:* Dễ cấu hình, hoạt động ngay (out-of-the-box).
    * *Nhược điểm:* Tiêu tốn nhiều RAM. Kể cả khi user chỉ request một file ảnh tĩnh (JPG/CSS), Apache vẫn sinh ra một process chứa sẵn engine PHP khổng lồ. Hiện nay, người ta thường bỏ `mod_php` và chuyển sang dùng kết hợp `mod_proxy_fcgi` + PHP-FPM.

### Bảo mật (WAF)

* **`mod_security`**: Đây là một module cung cấp tính năng **Tường lửa Ứng dụng Web (WAF - Web Application Firewall)** mã nguồn mở nổi tiếng nhất. Nó hoạt động bằng cách phân tích mọi luồng traffic (đặc biệt là POST request) dựa trên một bộ quy tắc (như OWASP Core Rule Set) để chặn đứng các cuộc tấn công phổ biến như: SQL Injection, Cross-Site Scripting (XSS), Local File Inclusion, và các hành vi dò quét lỗ hổng.

---

## 2. Module của Nginx

Khác với Apache, Nginx được thiết kế theo kiến trúc hướng sự kiện (event-driven) và các module thường được biên dịch sẵn vào lõi (core) thay vì load động.

### Xử lý Web / HTTP Cơ bản
* **`ngx_http_core_module`**: Đây là trái tim của Nginx khi xử lý web. Nó cung cấp cấu trúc cơ bản nhất của Nginx như các block `server {}`, `location {}`. Module này định nghĩa cách Nginx lắng nghe trên port nào (VD: 80, 443), xử lý file tĩnh, định tuyến các URL path và giới hạn tài nguyên (rate limiting, client body size).
* **`ngx_http_rewrite_module`**: Tương đương với `mod_rewrite` của Apache. Nó sử dụng Regular Expression (Biểu thức chính quy) để thay đổi URL (Rewrite), thực hiện chuyển hướng (Redirect 301/302), hoặc trả về các mã lỗi cụ thể tùy thuộc vào logic bạn cấu hình. Nó rất quan trọng cho việc tạo URL thân thiện (SEO-friendly) hoặc ép buộc chuyển hướng HTTP sang HTTPS.

### Tối ưu hóa & Bảo mật
* **`ngx_http_ssl_module`**: Cung cấp khả năng hỗ trợ giao thức HTTPS. Nó quản lý các chứng chỉ SSL/TLS, cấu hình các phiên bản giao thức mã hóa (như TLS 1.2, TLS 1.3), và xử lý việc giải mã/mã hóa dữ liệu giữa client và server.
* **`ngx_http_gzip_module`**: Đảm nhiệm việc nén dữ liệu (như HTML, CSS, JS) bằng thuật toán Gzip trên bộ nhớ ngay trước khi gửi về cho trình duyệt (on-the-fly). Việc này giúp giảm mạnh dung lượng băng thông truyền tải và tăng tốc độ tải trang web.

### Giao tiếp Backend & Load Balancing
* **`ngx_http_proxy_module`**: Tương tự `mod_proxy_http` của Apache. Module này cho phép Nginx làm Reverse Proxy, chuyển tiếp các HTTP request đến các backend server. Nginx cực kỳ nổi tiếng ở vai trò này nhờ khả năng xử lý hàng chục ngàn kết nối đồng thời với lượng RAM tiêu thụ cực thấp. Nó cũng đi kèm tính năng cache phản hồi từ backend.
* **`ngx_stream_*` (TCP/UDP Load Balancing)**: 

    * Hầu hết các module `ngx_http_*` hoạt động ở **Layer 7** (Application layer - hiểu được nội dung HTTP). Trong khi đó, nhóm `ngx_stream_*` hoạt động ở **Layer 4** (Transport layer).
    * Nó cho phép Nginx đóng vai trò Load Balancer (Cân bằng tải) và Proxy cho các dịch vụ không phải là web (không dùng HTTP). Ví dụ: Cân bằng tải cho các cụm Database (MySQL, PostgreSQL chạy trên TCP), DNS server (chạy trên UDP), hoặc proxy luồng gửi nhận Email (SMTP/IMAP).


## II. CẤU HÌNH VÀ TRIỂN KHAI WEB SERVER

1. Cấu hình Vitual host (host nhiều website trên một server)
### Bước 1: Cài đặt Nginx
Đầu tiên, bạn cần đảm bảo Nginx đã được cài đặt và đang chạy trên server. Mở Terminal và chạy các lệnh sau:

```bash
sudo apt update


sudo apt install nginx -y

sudo systemctl status nginx
```
<img width="1267" height="407" alt="image" src="https://github.com/user-attachments/assets/dbe27d2b-ff72-4a31-bc4a-d12079dff9d6" />


---

### Bước 2: Tạo thư mục chứa mã nguồn cho 2 website

Chúng ta sẽ tạo 2 thư mục riêng biệt đại diện cho 2 website (web1.local và web2.local), sau đó tạo file `index.html` cho từng web để dễ phân biệt khi demo.

```bash
# Tạo thư mục cho web 1 và web 2
sudo mkdir -p /var/www/web1
sudo mkdir -p /var/www/web2

# Cấp quyền sở hữu thư mục để Nginx có thể đọc được (Nginx thường chạy dưới user www-data)
sudo chown -R www-data:www-data /var/www/web1
sudo chown -R www-data:www-data /var/www/web2

# Tạo nội dung trang chủ cho Web 1
echo "<h1>Chao mung den voi WEB 1 (Giao dien mau XANH)</h1>" | sudo tee /var/www/web1/index.html

# Tạo nội dung trang chủ cho Web 2
echo "<h1>Chao mung den voi WEB 2 (Giao dien mau DO)</h1>" | sudo tee /var/www/web2/index.html
```

<img width="1283" height="190" alt="image" src="https://github.com/user-attachments/assets/11b273cf-67e8-4f0a-8a6a-c52b7cf33956" />

---

### Bước 3: Tạo cấu hình Virtual Host (Server Block)
Nginx quản lý các website thông qua các file cấu hình nằm trong `/etc/nginx/sites-available/`. Chúng ta sẽ tạo 2 file cấu hình tương ứng cho 2 web.

**1. Cấu hình cho Web 1:**
Mở trình soạn thảo nano:
```bash
sudo nano /etc/nginx/sites-available/web1.local
```


```nginx
server {
    listen 80;
    server_name web1.local;
    root /var/www/web1;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

<img width="563" height="425" alt="image" src="https://github.com/user-attachments/assets/455b01dc-9803-4d48-a4b7-1d836e4bd1fd" />


**2. Cấu hình cho Web 2:**
Tương tự, tạo file cho web 2:
```bash
sudo nano /etc/nginx/sites-available/web2.local
```
Dán đoạn cấu hình sau vào (lưu ý `server_name` và `root` đã thay đổi):
```nginx
server {
    listen 80;
    server_name web2.local;
    root /var/www/web2;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

---

### Bước 4: Kích hoạt website và Khởi động lại Nginx
Để Nginx nhận diện các file cấu hình vừa tạo, bạn cần tạo một symlink (đường dẫn tắt) nối sang thư mục `sites-enabled`.

```bash

sudo ln -s /etc/nginx/sites-available/web1.local /etc/nginx/sites-enabled/


sudo ln -s /etc/nginx/sites-available/web2.local /etc/nginx/sites-enabled/

sudo nginx -t
```

<img width="1284" height="264" alt="image" src="https://github.com/user-attachments/assets/941f9c36-e6bb-41ca-a735-9db50e6463da" />



```bash
# Khởi động lại Nginx để áp dụng thay đổi
sudo systemctl restart nginx
```

---

### Bước 5: Giả lập tên miền (Domain) trên máy thật
Vì `web1.local` và `web2.local` là tên miền "ảo" do chúng ta tự nghi ra để demo, bạn phải ép máy tính thật của  trỏ tên miền này về IP của máy ảo.

1. **Tìm IP của máy ảo:** Chạy lệnh `ip a` hoặc `ifconfig` trên Terminal của Ubuntu. Giả sử IP là `192.168.197.133` 
2. **Sửa file Hosts trên máy Windows của bạn:**
   * Mở Notepad dưới quyền quản trị viên (**Run as Administrator**).
   * Mở file theo đường dẫn: `C:\Windows\System32\drivers\etc\hosts`
   * Thêm 2 dòng sau vào cuối cùng của file rồi lưu lại:
     ```text
     192.168.197.133    web1.local
     192.168.197.133    web2.local
     ```
   

---

### Bước 6:
 mở trình duyệt web (Chrome, Edge, Firefox) trên máy tính 

* Gõ vào thanh địa chỉ: `http://web1.local` -> **Kết quả:** Hiển thị "Chao mung den voi WEB 1".
* Gõ vào thanh địa chỉ: `http://web2.local` -> **Kết quả:** Hiển thị "Chao mung den voi WEB 2".

Dù bạn đang truy cập vào cùng 1 địa chỉ IP duy nhất, Nginx vẫn đọc được URL trên trình duyệt và điều hướng đúng vào thư mục tương ứng. Bạn đã demo thành công tính năng Virtual Host!

<img width="1282" height="790" alt="image" src="https://github.com/user-attachments/assets/879b27cc-688a-4109-9664-b014a05046e8" />

<img width="1025" height="762" alt="image" src="https://github.com/user-attachments/assets/cd1605da-726f-463f-97d6-778361879822" />



À, hiểu ý bạn rồi! Bạn muốn quay lại làm phần **Virtual Host nhưng là trên nền Apache** chứ không phải Nginx. 

Cách làm trên Apache cũng tương tự về mặt tư duy, nhưng cú pháp và lệnh sẽ khác một chút (Apache dùng lệnh `a2ensite` rất tiện). Dưới đây là hướng dẫn chi tiết từng bước:
B1 : cập nhật và cài đặt apache



### Bước 2: Tạo thư mục mã nguồn cho 2 website
Tương tự như trước, ta tạo 2 thư mục mới t tên là `apachesite1` và `apachesite2` .

```bash
# Tạo thư mục
sudo mkdir -p /var/www/apachesite1
sudo mkdir -p /var/www/apachesite2

# Cấp quyền cho thư mục
sudo chown -R www-data:www-data /var/www/apachesite1
sudo chown -R www-data:www-data /var/www/apachesite2

# Tạo file HTML cho Web 1
echo "<h1 style='color: blue;'>Chao mung den voi WEB 1 (Chay bang APACHE)</h1>" | sudo tee /var/www/apachesite1/index.html

# Tạo file HTML cho Web 2
echo "<h1 style='color: red;'>Chao mung den voi WEB 2 (Chay bang APACHE)</h1>" | sudo tee /var/www/apachesite2/index.html
```

---

### Bước 3: Tạo cấu hình Virtual Host
Với Apache trên Ubuntu, các file cấu hình bắt buộc phải có đuôi `.conf` và nằm trong thư mục `/etc/apache2/sites-available/`.

**1. Cấu hình cho Site 1:**
```bash
sudo nano /etc/apache2/sites-available/apachesite1.local.conf
```
Dán nội dung sau vào:
```apache
<VirtualHost *:8081>
    ServerName apachesite1.local
    DocumentRoot /var/www/apachesite1

    ErrorLog ${APACHE_LOG_DIR}/apachesite1_error.log
    CustomLog ${APACHE_LOG_DIR}/apachesite1_access.log combined
</VirtualHost>
```
<img width="955" height="296" alt="image" src="https://github.com/user-attachments/assets/29c2c7df-7c53-4c01-b0fd-b95d25230a5e" />

**2. Cấu hình cho Site 2:**
```bash
sudo nano /etc/apache2/sites-available/apachesite2.local.conf
```
Dán nội dung sau vào:
```apache
<VirtualHost *:80>
    ServerName apachesite2.local
    DocumentRoot /var/www/apachesite2

    ErrorLog ${APACHE_LOG_DIR}/apachesite2_error.log
    CustomLog ${APACHE_LOG_DIR}/apachesite2_access.log combined
</VirtualHost>
```

---

### Bước 4: Kích hoạt Virtual Host
Thay vì phải tự tạo symlink (đường dẫn tắt) thủ công như Nginx, Apache có sẵn các lệnh cực kỳ tiện lợi để bật/tắt site:

```bash
# Kích hoạt 2 site vừa tạo
sudo a2ensite apachesite1.local.conf
sudo a2ensite apachesite2.local.conf

# Tắt trang default mặc định của Apache (tùy chọn nhưng nên làm cho gọn)
sudo a2dissite 000-default.conf

# Kiểm tra lỗi cú pháp của Apache
sudo apache2ctl configtest
```


```bash

sudo systemctl reload apache2
```

<img width="922" height="437" alt="image" src="https://github.com/user-attachments/assets/2adc03a1-efdd-4afe-93a7-8a7086bdd21e" />




---

### Bước 5: Cấu hình file Hosts và Test
1. Quay lại máy Windows, mở file `hosts` (`C:\Windows\System32\drivers\etc\hosts`).
2. Thêm 2 dòng sau để trỏ domain ảo về IP của máy Ubuntu:
   ```text
   192.168.197.133    apachesite1.local
   192.168.197.133    apachesite2.local
   ```
<img width="699" height="294" alt="image" src="https://github.com/user-attachments/assets/c7783b20-55a6-4b64-a82a-3ff9119e844e" />


**Kết quả:**
* Gõ vào trình duyệt: `http://apachesite1.local` -> Ra chữ màu xanh.

  <img width="1219" height="539" alt="image" src="https://github.com/user-attachments/assets/2af70def-503e-49bc-91ce-9fe9783fa30c" />

* Gõ vào trình duyệt: `http://apachesite2.local` -> Ra chữ màu đỏ.
  
 <img width="1496" height="556" alt="image" src="https://github.com/user-attachments/assets/6de5b916-ab39-4f4a-bad0-bda655387678" />

#2. Reverse Proxy và Loadbalence với Nginx

### Phần 1: Dựng 2 Backend Server bằng Apache

**Bước 1.1: Đảm bảo Apache lắng nghe ở 2 Port 8081 và 8082**
Mở file cấu hình port của Apache:
```bash
sudo nano /etc/apache2/ports.conf
```
Sửa lại nội dung thành như sau (xóa bỏ `Listen 80` nếu có):
```apache
Listen 8081
Listen 8082
```
Lưu và thoát (`Ctrl + O`, `Enter`, `Ctrl + X`).

**Bước 1.2: Tạo thư mục và giao diện nhận diện cho 2 Web**
Chạy các lệnh sau để tạo 2 trang web với màu sắc khác nhau nhằm dễ phân biệt khi test chia tải:
```bash
sudo mkdir -p /var/www/node1
sudo mkdir -p /var/www/node2

sudo chown -R www-data:www-data /var/www/node1
sudo chown -R www-data:www-data /var/www/node2

echo "<h1 style='color: blue; font-size: 50px;'>Day la NODE 1 (Apache - Port 8081)</h1>" | sudo tee /var/www/node1/index.html
echo "<h1 style='color: red; font-size: 50px;'>Day la NODE 2 (Apache - Port 8082)</h1>" | sudo tee /var/www/node2/index.html
```

<img width="1282" height="497" alt="image" src="https://github.com/user-attachments/assets/9fecc41a-5e9f-4927-aa52-e4e0d530a0f7" />

**Bước 1.3: Cấu hình Virtual Host cho 2 Node**
Tạo file cấu hình cho Node 1 (chạy port 8081):
```bash
sudo nano /etc/apache2/sites-available/node1.conf
```
Dán nội dung:
```apache
<VirtualHost *:8081>
    DocumentRoot /var/www/node1
</VirtualHost>
```

Tạo file cấu hình cho Node 2 (chạy port 8082):
```bash
sudo nano /etc/apache2/sites-available/node2.conf
```
Dán nội dung:
```apache
<VirtualHost *:8082>
    DocumentRoot /var/www/node2
</VirtualHost>
```

**Bước 1.4: Kích hoạt và khởi động lại Apache**
```bash
# Bật 2 site vừa tạo
sudo a2ensite node1.conf
sudo a2ensite node2.conf

# Restart Apache để nhận port mới
sudo systemctl restart apache2
```

---

### 2: Cấu hình Nginx làm Proxy & Load Balancer

**Bước 2.1: Đảm bảo Port 80 đã được giải phóng**
Vì Nginx sẽ đứng ở mặt tiền đón khách, nên nó cần Port 80. (Nếu máy bạn vẫn đang bật HAProxy như lúc trước thì nhớ tắt đi nhé):
```bash
sudo systemctl stop haproxy
sudo systemctl start nginx
```

**Bước 2.2: Viết cấu hình Load Balancer cho Nginx**
Tạo một file Virtual Host mới cho Nginx:
```bash
sudo nano /etc/nginx/sites-available/loadbalancer.conf
```
Dán đoạn mã cốt lõi này vào:
```nginx
# 1. Khai báo cụm Backend (Pool) gồm 2 máy Apache
upstream backend_pool {
    server 127.0.0.1:8081; # Node 1
    server 127.0.0.1:8082; # Node 2
}

# 2. Khai báo Nginx đứng ở mặt tiền
server {
    listen 80;
    server_name mydemo.local; # Tên miền ảo để test

    location / {
        # Đẩy toàn bộ traffic vào cụm Pool ở trên
        proxy_pass http://backend_pool;
        
        # Đính kèm IP thật của khách hàng (Tùy chọn nhưng khuyên dùng)
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

<img width="1080" height="520" alt="image" src="https://github.com/user-attachments/assets/8cd3d52c-3b7a-4d34-a7e6-3c9304dee604" />


**Bước 2.3: Kích hoạt Nginx**
```bash
sudo ln -s /etc/nginx/sites-available/loadbalancer.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

<img width="852" height="119" alt="image" src="https://github.com/user-attachments/assets/0b56732b-4079-489f-9d28-5e77148dd931" />

---

### Phần 3: Trỏ file Hosts trên máy tính thật (Windows)

Mở file `hosts` dưới quyền Administrator theo đường dẫn: `C:\Windows\System32\drivers\etc\hosts`
Thêm duy nhất 1 dòng khai báo IP trỏ về tên miền của Nginx (Không kèm port):
```text
192.168.197.133   mydemo.local
```
---

### Phần 4: Chạy Demo nghiệm thu

1. Mở trình duyệt Chrome/Edge.
2. Gõ đường dẫn: **`http://mydemo.local`** (Nginx ở cổng 80 ).
3. Lần đầu tiên, bạn sẽ thấy màn hình chữ Xanh: **"Day la NODE 1 (Apache - Port 8081)"**.
4. Hãy bấm nút **F5** hoặc **Ctrl + R** để Refresh trang.
5. Màn hình sẽ lập tức đổi sang chữ Đỏ: **"Day la NODE 2 (Apache - Port 8082)"**.
6. Tiếp tục ấn F5, giao diện được chuyển site  luân phiên.

<img width="1203" height="473" alt="image" src="https://github.com/user-attachments/assets/79f3dfbd-8fa3-4436-b813-01e2150e274b" />

<img width="1067" height="478" alt="image" src="https://github.com/user-attachments/assets/89c672c6-5736-484d-ad09-8a0fb2e7fa9f" />

Hệ thống đã hoạt động hoàn hảo: Nginx đang làm đúng chức năng của một bộ phân tải (Load Balancer) kết hợp Reverse Proxy!

Ở đây, nginx(80) là mặt tiền để nhận các request sau đó trả về cho các site bên trong apache (8081,8082) thể hiện được việc đã che giấu được site chính ngăn các request bên ngoài truy cập vào. 

# **3. Cấu hình caching (giảm tải server).**

aching là kỹ thuật lưu trữ tạm thời những dữ liệu đã được sử dụng hoặc xử lý trước đó vào một vùng nhớ có tốc độ truy xuất rất nhanh. Mục tiêu của cơ chế này là rút ngắn thời gian phản hồi và giảm số lần hệ thống phải truy cập trực tiếp vào nguồn dữ liệu gốc — vốn tốn nhiều tài nguyên hơn.

Khi có yêu cầu truy xuất dữ liệu, hệ thống sẽ ưu tiên kiểm tra cache trước. Nếu dữ liệu cần tìm đã tồn tại trong cache, thông tin sẽ được trả về ngay lập tức mà không cần thực hiện lại truy vấn, tính toán, hoặc xử lý phức tạp. Nhờ đó, tốc độ phản hồi được cải thiện rõ rệt và áp lực lên server, database hay API cũng giảm đáng kể.

Caching đặc biệt hiệu quả trong những trường hợp dữ liệu ít thay đổi hoặc chỉ cập nhật theo chu kỳ, chẳng hạn như thông tin cấu hình, trang web tĩnh, kết quả truy vấn lặp lại, nội dung hình ảnh… Việc tận dụng cache giúp hệ thống hoạt động mượt mà hơn, tiết kiệm tài nguyên, tăng khả năng mở rộng và mang lại trải nghiệm nhanh hơn cho người dùng cuối.

Cách thức hoạt động của caching
Sau khi hiểu khái niệm cơ bản về caching, bước tiếp theo là nắm rõ cơ chế hoạt động của kỹ thuật này. Về bản chất, caching vận hành dựa trên nguyên tắc lưu trữ tạm thời những dữ liệu thường xuyên được truy cập vào một vùng nhớ tốc độ cao. Khi người dùng hoặc hệ thống gửi yêu cầu truy xuất, chương trình sẽ kiểm tra cache trước. Nếu dữ liệu đã tồn tại (cache hit), kết quả được trả về ngay lập tức mà không cần truy vấn lại vào database, ổ đĩa hay API gốc. Điều này rút ngắn đáng kể thời gian phản hồi và giảm tải cho hệ thống.

Ngược lại, nếu dữ liệu không có trong cache (cache miss), hệ thống sẽ truy xuất vào nguồn dữ liệu chính, sau đó lưu bản sao vào cache để phục vụ những lần truy cập tiếp theo. Chính cơ chế “học theo thói quen truy cập” này đã khiến caching trở thành một kỹ thuật vô cùng hiệu quả trong tối ưu hiệu suất.

<img width="893" height="598" alt="image" src="https://github.com/user-attachments/assets/95daf958-6dac-4bd0-bd0e-604b87b6883d" />

RAM và In-Memory Engines

RAM là nơi lý tưởng để lưu trữ cache vì tốc độ truy xuất nhanh hơn nhiều so với ổ cứng truyền thống. Các công nghệ In-Memory như Redis, Memcached hay các engine lưu trữ dạng key–value cũng được sử dụng nhằm đạt độ trễ thấp và đáp ứng khối lượng request lớn trong thời gian ngắn.

Khi các ứng dụng chịu tải cao, truy vấn nhiều hoặc yêu cầu số lượng IOPS lớn, việc tận dụng bộ nhớ RAM để lưu cache giúp:

giảm áp lực lên database,

tăng tốc xử lý,

tránh phải mở rộng phần cứng tốn kém,

mang lại hiệu năng cao hơn nhiều lần so với việc lấy dữ liệu từ ổ đĩa.

Thay vì đầu tư mạnh vào việc mở rộng database truyền thống, sử dụng In-Memory cache cho phép hệ thống đạt hiệu suất vượt trội với chi phí thấp hơn.

Ứng dụng của caching trong hệ thống thực tế

Caching có thể được triển khai ở nhiều tầng khác nhau của một hệ thống. Mỗi tầng đảm nhiệm một vai trò riêng:

<img width="906" height="578" alt="image" src="https://github.com/user-attachments/assets/aef93dfa-7d35-420c-9177-f1a9882e00f6" />


1. Hệ điều hành và trình duyệt
Trình duyệt lưu cache HTML, CSS, JavaScript, hình ảnh… giúp trang web tải lại nhanh chóng trong những lần truy cập tiếp theo.

2. DNS
Hệ thống DNS cache giúp rút ngắn thời gian phân giải tên miền sang địa chỉ IP, cải thiện tốc độ truy cập website.

3. CDN và reverse proxy
Các mạng phân phối nội dung như Cloudflare, CloudFront lưu bản sao nội dung tĩnh gần người dùng hơn, giúp giảm độ trễ và tiết kiệm băng thông cho máy chủ gốc.

4. Ứng dụng web và API
Kết quả truy vấn database, token phiên đăng nhập hoặc phản hồi API được lưu vào cache để tránh xử lý lại nhiều lần.

5. Database caching
Nhiều hệ quản trị cơ sở dữ liệu có lớp cache riêng nhằm hạn chế truy vấn vào disk, tăng throughput và giảm độ trễ.

Ngoài các hệ thống web, caching còn cực kỳ quan trọng trong các ứng dụng xử lý dữ liệu lớn, game online, mạng xã hội và các tác vụ tính toán hiệu năng cao.

Caching trong môi trường điện toán phân tán

Trong các hệ thống phân tán, caching đóng vai trò như một lớp chia sẻ dữ liệu trung gian giúp các node hoạt động độc lập nhưng vẫn truy xuất chung một nguồn dữ liệu nhanh.

Trong môi trường này cần phân biệt:

Local cache: chỉ phục vụ cho ứng dụng chạy trên cùng một máy.

Distributed cache: dữ liệu cache được lưu tập trung, nhiều máy chủ cùng truy cập, phù hợp cho hệ thống quy mô lớn và có khả năng mở rộng linh hoạt.

Các hệ thống như Redis Cluster hoặc Memcached được sử dụng để đảm bảo khả năng mở rộng, tính sẵn sàng cao và dữ liệu nhất quán trong môi trường nhiều node.

Các nguyên tắc cần nắm khi sử dụng caching

Để caching hoạt động hiệu quả, cần chú ý một số nguyên tắc quan trọng:

1. Quản lý vòng đời dữ liệu (TTL – Time To Live)
TTL quy định thời gian dữ liệu được lưu trong cache. Khi hết hạn, hệ thống tự động làm mới dữ liệu. Điều này ngăn ngừa việc người dùng nhận thông tin đã lỗi thời.

2. Đảm bảo tính nhất quán của cache
Dữ liệu trong cache phải phản ánh đúng dữ liệu gốc. Khi dữ liệu thay đổi, cache cần được cập nhật hoặc xóa.

3. Lựa chọn công nghệ cache phù hợp
Redis, Memcached, Hazelcast… mỗi công cụ có ưu điểm riêng về tốc độ, tính bền vững, khả năng mở rộng.

4. Hiểu rõ RTO và RPO
RTO: thời gian hệ thống cần để khôi phục sau sự cố.

RPO: lượng dữ liệu tối đa được “mất” trong quá trình khôi phục.

Điều này giúp xác định hệ thống có thể dùng In-Memory cache mà vẫn đảm bảo tiêu chuẩn vận hành hay không.

Tóm lại:

Caching là một trong những kỹ thuật tối ưu hiệu suất mạnh mẽ nhất trong lĩnh vực công nghệ. Từ trình duyệt web đến hệ thống phân tán quy mô lớn, caching giúp:

tăng tốc độ phản hồi,

giảm tải cho backend,

tối ưu chi phí,

cải thiện trải nghiệm người dùng,

nâng cao khả năng mở rộng của ứng dụng.

Các loại caching phổ biến nhất hiện nay
Caching có nhiều hình thức khác nhau, tùy thuộc vào vị trí triển khai và mục tiêu tối ưu hệ thống. Dưới đây là những loại caching phổ biến nhất được sử dụng trong các hệ thống hiện đại.

1. Browser Cache (Cache phía trình duyệt)
Cách hoạt động:
Trình duyệt lưu lại các tệp tĩnh như HTML, CSS, JavaScript, hình ảnh, font… để người dùng không phải tải lại toàn bộ mỗi lần truy cập.

Ưu điểm:
Tăng tốc độ tải trang đáng kể.

Giảm băng thông và giảm tải cho máy chủ.

Cải thiện trải nghiệm người dùng, đặc biệt với web tĩnh.

Nhược điểm:
Dễ gây lỗi hiển thị nếu dữ liệu không được cập nhật đúng cách.

Khó kiểm soát toàn bộ trên nhiều loại trình duyệt.

Dữ liệu có thể lỗi thời nếu không thiết lập cache header chuẩn.

2. CDN Cache (Cache mạng phân phối nội dung)
Cách hoạt động:
CDN lưu bản sao nội dung tĩnh tại nhiều máy chủ đặt ở nhiều khu vực địa lý. Người dùng sẽ được phục vụ từ node gần nhất.

Ưu điểm:
Giảm độ trễ khi truy cập.

Tăng tốc độ tải trang toàn cầu.

Giảm tải cho server gốc.

Dễ mở rộng cho website lớn, lượng truy cập cao.

Nhược điểm:
Chi phí có thể cao với hệ thống lớn.

Dữ liệu cập nhật chậm nếu cấu hình cache không hợp lý.

Khó debug khi kết quả bị cache ở nhiều vị trí khác nhau.

3. Application Cache (Cache trong ứng dụng)
Ví dụ: lưu cấu hình, token, session, kết quả xử lý logic…

Ưu điểm:
Linh hoạt, phù hợp tùy từng ứng dụng.

Giảm số truy vấn vào database hoặc API.

Tăng hiệu suất xử lý logic bên trong app.

Nhược điểm:
Cần thiết kế kỹ để xử lý mất đồng bộ dữ liệu.

Nếu app chạy trên nhiều server sẽ khó chia sẻ cache cục bộ.

Tốn RAM nếu lưu quá nhiều.



4. Database Cache
Bao gồm: cache query, cache index, cache table…

Ưu điểm:
Giảm đáng kể thời gian truy vấn SQL.

Tăng hiệu suất của database trong môi trường tải cao.

Phù hợp với hệ thống có dữ liệu đọc nhiều – ghi ít.

Nhược điểm:
Khi dữ liệu thay đổi thường xuyên, cache rất dễ lỗi thời.

Dễ gây phức tạp trong quản lý tính nhất quán.

Có thể chiếm nhiều bộ nhớ nếu không dọn dẹp hợp lý.

5. Distributed Cache (Cache phân tán – Redis, Memcached)
Đây là loại cache phổ biến trong microservices, hệ thống đa node.

Ưu điểm:
Các máy chủ truy cập chung một nguồn cache → đồng nhất dữ liệu.

Tốc độ rất nhanh nhờ sử dụng In-Memory.

Hỗ trợ mở rộng theo chiều ngang (scale-out).

Có thể lưu key–value, session, token, hàng đợi…

Nhược điểm:
Cần thiết lập cụm (cluster) để đảm bảo an toàn và độ sẵn sàng cao.

Chi phí cao hơn so với local cache.

Cần hiểu về phân vùng dữ liệu (sharding).

6. Object Cache
Lưu trữ các đối tượng đã được tạo/serialize (chẳng hạn như dữ liệu từ ORM).

Ưu điểm:
Giảm chi phí tạo object phức tạp.

Tối ưu hiệu năng của backend.

Nhược điểm:
Phải đảm bảo object hợp lệ và đồng bộ.

Tốn RAM nếu object lớn.

7. CPU Cache (L1, L2, L3)
Dùng trong phần cứng để tăng tốc xử lý lệnh và dữ liệu.

Ưu điểm:
Nhanh nhất trong tất cả các loại cache.

Giảm độ trễ khi CPU truy xuất dữ liệu.

Nhược điểm:
Kích thước rất nhỏ.

Không phù hợp với dữ liệu lớn hoặc dữ liệu ứng dụng.

Ưu và nhược điểm chung của caching
Dưới đây là cái nhìn tổng thể để bạn dễ bao quát:

Ưu điểm của caching
Tốc độ truy xuất nhanh hơn nhiều lần so với nguồn gốc.

Giảm tải cho server, database và phần cứng.

Tối ưu chi phí vì giảm nhu cầu nâng cấp tài nguyên.

Cải thiện trải nghiệm người dùng, đặc biệt với website và app.

Tăng khả năng mở rộng (scaling) cho hệ thống lớn.

Hỗ trợ IOPS cao, phục vụ lượng request lớn trong thời gian ngắn.

Nhược điểm của caching
Nguy cơ dữ liệu lỗi thời (stale data) nếu quản lý không đúng.

Tốn RAM hoặc tài nguyên bộ nhớ.

Phức tạp khi triển khai trong hệ thống phân tán.

Cần chiến lược invalidation tốt (xóa hoặc cập nhật cache đúng lúc).

Sai sót cache có thể dẫn đến lỗi logic trong ứng dụng.

**Cấu hình Caching trên Ubuntu**

---

Mô phỏng và đánh giá cơ chế Browser Cache (Client-Side Caching)
1. Mục đích: Kiểm chứng cơ chế gắn HTTP Header Cache-Control từ phía máy chủ (Server) để ra lệnh cho máy khách (Trình duyệt/Client) lưu trữ tài nguyên tĩnh cục bộ, giúp tiết kiệm băng thông và giảm tải request cho server ở các lần truy cập sau.

2. Quá trình thực hiện:
Tạo tệp test_browser_cache.py sử dụng thư viện http.server có sẵn của Python để dựng một web server nội bộ:

Python
import http.server
import socketserver

PORT = 8080

class BrowserCacheHandler(http.server.SimpleHTTPRequestHandler):
    def end_headers(self):
        # Them HTTP Header ra lenh cho trinh duyet luu cache file nay trong 1 gio (3600 giay)
        self.send_header("Cache-Control", "public, max-age=3600")
        super().end_headers()

# Khoi tao server
with socketserver.TCPServer(("", PORT), BrowserCacheHandler) as httpd:
    print(f"[He thong] Web Server mo phong dang chay tai port {PORT}")
    print(f"[He thong] De kiem tra Header, mo Terminal moi va go: curl -I http://localhost:{PORT}")
    
    # Duy tri server chay lien tuc
    httpd.serve_forever()


<img width="1262" height="588" alt="image" src="https://github.com/user-attachments/assets/bf601a62-0c60-4c33-a70e-854d87734062" />

Bước 1: Mở Terminal 1 và chạy server mô phỏng:

python3 test_browser_cache.py

Bước 2: Mở Terminal 2 (đóng vai trò là Client/Trình duyệt) và dùng lệnh curl -I để lấy HTTP Header trả về từ Server:

Bash
curl -I http://localhost:8080

<img width="1116" height="203" alt="image" src="https://github.com/user-attachments/assets/3d3d7c47-af83-4497-96a5-c54400a3f966" />

3. Kết quả & Đánh giá:
Kết quả trả về trên Terminal 2 (Client):

<img width="796" height="494" alt="image" src="https://github.com/user-attachments/assets/ac1c346f-a937-4de4-8104-a1163b162f7c" />


Kết luận: Khi một trình duyệt thực tế (như Chrome, Firefox) nhận được gói tin có Header này, nó sẽ tự động tải file về và lưu vào ổ cứng của máy tính người dùng. Trong vòng 3600 giây tiếp theo, nếu người dùng tải lại trang, trình duyệt sẽ móc file từ máy tính ra dùng luôn thay vì gửi request mới lên server. Điều này chứng minh cấu hình Browser Cache đã hoạt động thành công.




**Application Cache (Cache trong ứng dụng)**

Ngôn ngữ/Công cụ: Python 3 (tích hợp sẵn thư viện chuẩn functools).

Phương pháp: Mô phỏng độ trễ hệ thống bằng hàm time.sleep().

3. Các bước triển khai chi tiết
Bước 1: Tạo tệp lệnh (Script) giả lập hệ thống
Sử dụng trình soạn thảo nano để tạo tệp mã nguồn Python thực thi trên Terminal:

Bash
nano test_cache.py
Bước 2: Xây dựng logic và cấu hình Cache
Khai báo thư viện và viết hàm giả lập kết nối cơ sở dữ liệu. Sử dụng decorator @lru_cache để cấp phát một phần RAM nội bộ (In-Memory) nhằm lưu trữ kết quả của các hàm xử lý nặng.

Thêm đoạn mã sau vào tệp test_cache.py:



# Cấu hình Caching: Lưu trữ tối đa 32 kết quả truy vấn gần nhất vào RAM

import time
from functools import lru_cache

# Cau hinh Caching: Luu tru toi da 32 ket qua truy van gan nhat vao RAM
@lru_cache(maxsize=32)
def query_database(user_id):
    print(f"[He thong] Dang ket noi Database de lay du lieu cho User {user_id}... (Quy trinh ton tai nguyen)")
    
    # Gia lap do tre 3 giay khi he thong phai xu ly tac vu nang
    time.sleep(3) 
    
    return f"Du lieu chi tiet cua User {user_id}"

print("--- TEST CASE 1: Lan truy cap dau tien (Cache Miss) ---")
start_time_1 = time.time()
print("Ket qua:", query_database(99))
print(f"Thoi gian phan hoi: {time.time() - start_time_1:.4f} giay\n")

print("--- TEST CASE 2: Truy cap lai cung mot du lieu (Cache Hit) ---")
start_time_2 = time.time()
print("Ket qua:", query_database(99))
print(f"Thoi gian phan hoi: {time.time() - start_time_2:.6f} giay\n")

<img width="1517" height="718" alt="image" src="https://github.com/user-attachments/assets/4692e206-0a26-406d-b896-08dcbbc1e36b" />

python3 test_cache.py
4. Kết quả và Đánh giá
4.1. Kết quả đầu ra (Output Log)

<img width="1171" height="273" alt="image" src="https://github.com/user-attachments/assets/8abbb29a-bd4e-4edf-a322-f1c5d455f191" />


Ở Test Case 1 (Truy cập lần đầu): Do dữ liệu chưa tồn tại trong bộ nhớ đệm, hệ thống buộc phải thực thi toàn bộ luồng logic bên trong hàm (mô phỏng việc gọi xuống Database). Quá trình này tiêu tốn hơn 3 giây và tiêu hao tài nguyên CPU/IO.

Ở Test Case 2 (Truy cập lần sau): Cơ chế Application Cache đã can thiệp ngay tại lớp ứng dụng. Hệ thống nhận diện tham số đầu vào (user_id = 99) đã được lưu trước đó, do đó bỏ qua hoàn toàn bước thực thi logic nặng và lấy thẳng dữ liệu từ RAM. Thời gian phản hồi giảm xuống chỉ còn xấp xỉ 0.000004 giây (nhanh hơn hàng trăm nghìn lần).

##  Cấu hình CPU Cache (L1, L2, L3)

**Đặc thù:** Bạn **không thể** cấu hình CPU Cache bằng các file text trên Ubuntu như các loại cache khác. Đây là loại cache tĩnh được hàn cứng trên kiến trúc vi xử lý (Hardware level) và được nhân hệ điều hành (Linux Kernel) quản lý tự động.

Tuy nhiên, trên Ubuntu, bạn có thể kiểm tra dung lượng và cấu trúc CPU Cache đang cấp phát cho server của mình bằng lệnh sau:

**Thực hiện kiểm tra:**
Mở terminal trên Ubuntu và gõ lệnh:
`lscpu | grep cache`

**Kết quả trả về mẫu:**

<img width="835" height="198" alt="image" src="https://github.com/user-attachments/assets/7b9d9438-d608-4116-a082-4119eaff4098" />


4. Cấu hình và Triển khai Web Server
•  Cấu hình virtual host (host nhiều website trên một server)
•  Thiết lập reverse proxy, load balancing (ví dụ: Nginx làm proxy cho Apache).
•  Cấu hình caching (giảm tải server).
•  Tối ưu tốc độ (nén dữ liệu, CDN, HTTP/2).
•  Tuning thông số: Max connections, keep-alive, worker processes.

5. Bảo mật Web Server
•  Phòng chống DDoS, brute force.
•  Cấu hình firewall (iptables, Cloudflare).
•  Hardening server (tắt dịch vụ không cần thiết, cập nhật bảo mật).
•  Xử lý các lỗ hổng (SQL injection, XSS, CSRF).
