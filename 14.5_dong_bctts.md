# BÁO CÁO THỰC TẬP NGÀY 14/5

---

## I. Hiệu xuất và tối ưu hóa trong FTP

---

### 1. Tối ưu băng thông và tốc độ truyền tải

**A. Đối với vsftpd (Linux)**
Mở file cấu hình `/etc/vsftpd.conf` và thêm/sửa các dòng sau:

* **Giới hạn băng thông (tính bằng Bytes/giây):**
```ini
# Giới hạn user local tải tối đa ~1MB/s (1000000 bytes)
local_max_rate=1000000
# Giới hạn user ẩn danh (anonymous) tải tối đa ~500KB/s
anon_max_rate=500000

```


* **Cấu hình Passive Mode (tránh lỗi ngắt quãng do tường lửa):**
```ini
pasv_enable=YES
pasv_min_port=50000
pasv_max_port=51000

```


<img width="777" height="309" alt="image" src="https://github.com/user-attachments/assets/c3e289db-d6f1-4cb9-bf47-fb8f39f5213a" />


*(Lưu ý mở port 50000-51000 trên firewall của Linux).*

**B. Đối với FileZilla Server (Windows/Linux UI)**

Bấm vào menu Server ở góc trên cùng bên trái.

Chọn Configure... (hoặc bạn có thể bấm phím tắt Ctrl + F).

Lúc này, một cửa sổ "Configuration" lớn sẽ hiện ra. 

Cấu hình Passive Mode: Ở cột bên trái, bạn tìm mục Server -> FTP and FTP over TLS -> Nhìn sang cửa sổ bên phải chọn tab Passive mode.
Tại đây bạn tích vào Use custom port range và điền dải port (VD: 50000 - 51000) nhé.
<img width="1194" height="731" alt="image" src="https://github.com/user-attachments/assets/b806fe1c-9511-4e16-be0d-b848ee4aa90b" />


Giới hạn kết nối (Connection Limits): Vào mục Rights management -> Users (hoặc Groups). Chọn tài khoản bạn muốn giới hạn, sau đó nhìn sang bảng bên phải, 
chuyển sang tab Session limits. Ở đây bạn sẽ thấy các ô Maximum session count (Tổng số kết nối) và Maximum session count per IP (Số kết nối tối đa trên 1 IP).



Giới hạn băng thông (Speed Limits): Cũng ngay trong phần Rights management -> Users, bạn chuyển sang tab Speed limits nằm ngay cạnh tab Session limits để set tốc độ
Download/Upload theo Kb/s.

<img width="1170" height="743" alt="image" src="https://github.com/user-attachments/assets/602248f7-858f-4ff6-ab22-57b7a98083ee" />

Cấu hình Timeout:
Vào Server -> FTP and FTP over TLS -> Chọn tab Timeouts. Ở đây sẽ có Session timeout và Login timeout để bạn tinh chỉnh.



---

### 2. Quản lý số lượng kết nối đồng thời

**A. Đối với vsftpd**
Chỉnh sửa trong `/etc/vsftpd.conf`:

```ini
# Tổng số kết nối tối đa vào server cùng lúc
max_clients=200
# Số kết nối tối đa từ 1 địa chỉ IP (rất quan trọng để chống IDM spam kết nối)
max_per_ip=3

```
<img width="341" height="152" alt="image" src="https://github.com/user-attachments/assets/118baacc-dc1d-4b6a-96a6-62b91f30396a" />

**B. Đối với FileZilla**

* **Trên FileZilla Server:**
* Vào **Edit > Settings > General settings**.
* Tìm mục **Performance / Connection limits**.
* Đặt số tại **Maximum number of connections** (VD: 200).
* Để giới hạn theo IP, bạn vào phần **Edit > Users** (hoặc Groups), chọn tài khoản, nhìn sang cột bên phải sẽ có mục **Connection limits** (Maximum connection count / Maximum connections per IP).
* 
<img width="816" height="642" alt="image" src="https://github.com/user-attachments/assets/1c6d0352-8532-40d6-82f5-377d85fa2af4" />

* **Trên FileZilla Client (Dành cho người dùng cuối):**
* Vào **File > Site Manager > Chọn kết nối > tab Transfer Settings**.
* Tích vào **Limit number of simultaneous connections** và đặt là `2` hoặc `3`. 
* 
<img width="1311" height="399" alt="image" src="https://github.com/user-attachments/assets/4564bd09-45d6-4f26-a54f-d56a841b8be2" />




---

### 3. Sử dụng nén file trước khi truyền

**A. Đối với vsftpd**

* **Đặc thù của vsftpd:** Thiết kế của vsftpd cực kỳ chú trọng vào bảo mật và sự gọn nhẹ, do đó nó **không hỗ trợ** tính năng nén trực tiếp MODE Z (on-the-fly compression) vì nén dữ liệu dễ dính các lỗ hổng bảo mật liên quan đến tràn bộ nhớ.
* **Giải pháp:** Bắt buộc áp dụng "Nén thủ công" (Pre-compression). Người quản trị cần dùng lệnh bash script (như `cron`) để tự động nén log hoặc file backup thành `.tar.gz` trước khi đẩy lên FTP.
```bash
tar -czvf backup_data.tar.gz /path/to/data

```



**B. Đối với FileZilla (Hỗ trợ MODE Z)**

* **Trên FileZilla Server:** Tính năng MODE Z thường được bật mặc định nếu Client yêu cầu. Bạn có thể kiểm tra trong phần cấu hình mã hóa/truyền tải.
* **Trên FileZilla Client:**
* Vào **Edit > Settings > Connection > FTP**.
* Tìm mục **FTP compression** và chọn chức năng nén. (Tuy nhiên, phần lớn các server hiện nay vô hiệu hóa tính năng này để tiết kiệm CPU, nên nén thủ công `.zip` hoặc `.rar` vẫn là phương án tối ưu nhất).

Hiện trên các phiên bản mới đã ko còn phần nén này nữa do những bất cập về tốc độ, tài nguyên của nó, vẫn ưu tiên nén thủ công trước rồi truyền file

---

### 4. Giám sát hiệu suất và khắc phục sự cố (Timeout, Log)

**A. Đối với vsftpd**

* **Xử lý Timeout (ngắt kết nối treo) trong `/etc/vsftpd.conf`:**
```ini
# Ngắt kết nối nếu người dùng không thao tác lệnh gì trong 10 phút (600s)
idle_session_timeout=600
# Ngắt nếu đường truyền dữ liệu bị đứng trong 2 phút (120s)
data_connection_timeout=120

```
<img width="529" height="83" alt="image" src="https://github.com/user-attachments/assets/5aec30e4-de03-4f28-ae08-bc3913ce6d60" />


* **Giám sát Log:**
```ini
# Bật tính năng ghi log chi tiết mọi thao tác upload/download
xferlog_enable=YES
xferlog_file=/var/log/xferlog
log_ftp_protocol=YES 

```
<img width="700" height="158" alt="image" src="https://github.com/user-attachments/assets/fe92f161-89dd-466c-8ea7-d3be8b06865a" />


*(Sau đó, dùng lệnh `tail -f /var/log/xferlog` để xem luồng dữ liệu realtime).*
* **Khắc phục sự cố (Cho phép Resume/Ghi đè file thiếu):**
vsftpd tự động hỗ trợ tính năng resume download. Nhưng để resume upload (chép nối tiếp file), bạn cần bật:
```ini
write_enable=YES
# Tùy chọn (với một số phiên bản) có thể cần thêm:
# ascii_upload_enable=YES
# ascii_download_enable=YES 

```



**B. Đối với FileZilla Server**

* **Xử lý Timeout:**
* Vào **Edit > Settings > General settings > Timeouts**.
* Cấu hình **Connection timeout** (Tương đương idle timeout - VD: 600s) và **No Transfer timeout** (Tương đương data timeout - VD: 120s).


* **Giám sát Log:**
* Vào **Edit > Settings > Logging**.
* Tích chọn **Enable logging to file**, thiết lập giới hạn dung lượng file log để tránh đầy ổ cứng (Limit log file size). Log này sẽ lưu lại mọi sự kiện ngắt kết nối
*  (Disconnected), timeout hay lỗi permission.

<img width="852" height="466" alt="image" src="https://github.com/user-attachments/assets/baee440a-bd2c-41b0-b777-9547d24baafa" />

* **Khắc phục sự cố (Resume):** Giao diện FileZilla Client cực kỳ mạnh mẽ trong việc này. Khi tải lên/tải xuống một file đã tồn tại một phần, hộp thoại sẽ hiện lên,
* bạn chỉ cần chọn **"Resume"** (Tiếp tục) thay vì "Overwrite" (Ghi đè). Chức năng này hoạt động trơn tru nếu server (kể cả là vsftpd hay FileZilla Server) cấp đủ quyền ghi.



## II. Các công cụ và giao thức liên quan

1. FTP client phổ biến

1.1 ## BÁO CÁO CHUYÊN SÂU: GIẢI PHÁP TRUYỀN TẢI TỆP TIN FILEZILLA

**1. Tổng quan Dự án**
FileZilla (chính xác hơn là FileZilla Client) là một phần mềm truyền tải tệp tin qua mạng dựa trên giao thức FTP mã nguồn mở và miễn phí. Nhờ việc được xây dựng để hỗ trợ đa nền tảng (Windows, macOS, Linux), phần mềm này đã thiết lập tiêu chuẩn toàn cầu cho các tác vụ truyền tải tệp tin và duy trì vị thế là một trong những giải pháp FTP/SFTP phổ biến nhất hiện nay.

---

<img width="3840" height="3840" alt="image" src="https://github.com/user-attachments/assets/eec89c1d-ec27-4d0b-ae23-0f433b01bd60" />

**2. Phân tích Thiết kế và Giao diện (Workflow)**
Giao diện của FileZilla được thiết kế tối ưu hóa cho sự trực quan, phân chia không gian làm việc thành các phân vùng logic rõ ràng, đáp ứng nhu cầu giám sát toàn diện của người dùng:

* **Khung Nhật ký (Message Log):** Nằm ở trên cùng, hiển thị chi tiết các câu lệnh giao tiếp giữa client và server (dưới dạng mã hồi đáp FTP), giúp kỹ thuật viên chẩn đoán lỗi kết nối hoặc xác thực ngay lập tức.
* **Không gian chia đôi (Dual-pane):** Cây thư mục máy tính cục bộ (Local) nằm bên trái và cây thư mục máy chủ (Remote) nằm bên phải. Người dùng thao tác chủ yếu thông qua phương thức kéo thả (Drag & Drop) hoặc nhấn đúp chuột.
* **Khung Hàng đợi (Transfer Queue):** Nằm ở dưới cùng, phân tách rõ ràng các tệp đang chờ truyền tải (Queued), đã truyền thất bại (Failed) và đã thành công (Successful).

<img width="927" height="727" alt="image" src="https://github.com/user-attachments/assets/9fb9fb07-37d9-4e6b-b68e-2a103d73ad19" />

---

**3. Trình quản lý Điểm truy cập (Site Manager)**
Đây là tính năng cốt lõi khiến FileZilla trở nên không thể thiếu đối với những người phải quản lý hạ tầng hàng chục hoặc hàng trăm máy chủ:

* **Tổ chức theo cây thư mục:** Cho phép nhóm các máy chủ theo dự án, phòng ban hoặc môi trường (Ví dụ: Môi trường Staging, Môi trường Production).
* **Lưu trữ Cấu hình Chuyên sâu:** Tùy biến từng phiên kết nối với các tham số riêng biệt như: giao thức (FTP, FTPS bảo mật SSL/TLS, hoặc SFTP), số cổng (Port), kiểu đăng nhập (Hỏi mật khẩu, Lưu mật khẩu, hoặc sử dụng tệp Key file `.pem`/`.ppk` cho SSH).
* **Chỉ định Thư mục Mặc định:** Khả năng thiết lập tự động truy cập vào một thư mục cụ thể trên Local và Remote ngay khi kết nối thành công, giúp rút ngắn thao tác mở tệp thủ công.

---

**4. Hiệu suất Truyền tải và Cơ chế Phục hồi (Resume)**
Được thiết kế để xử lý khối lượng dữ liệu khổng lồ, FileZilla sở hữu bộ máy truyền tải mạnh mẽ với các đặc tính:

* **Tính năng Resume (Nối tệp):** Trong quá trình truyền tải các tệp tin dung lượng lớn (vài chục GB như tệp sao lưu database), nếu xảy ra sự cố rớt mạng hoặc mất điện, FileZilla có khả năng đối chiếu dung lượng và tiếp tục truyền tải phần dữ liệu còn thiếu thay vì phải tải lại từ con số 0.
* **Truyền tải đa luồng (Concurrent Transfers):** Cho phép thiết lập số lượng kết nối đồng thời đến máy chủ (lên đến 10 kết nối). Điều này giúp khai thác tối đa băng thông mạng khi cần upload/download hàng ngàn tệp tin nhỏ (như mã nguồn website), khắc phục điểm yếu của giao thức FTP tuần tự truyền thống.
* **Kiểm soát Băng thông (Speed Limits):** Cung cấp công cụ giới hạn tốc độ tải lên và tải xuống, đảm bảo tác vụ truyền file không làm nghẽn toàn bộ đường truyền mạng của hệ thống.

---

**5. Đánh giá Ưu và Nhược điểm**

| Tiêu chí | Điểm mạnh | Hạn chế |
| --- | --- | --- |
| **Bản quyền & Tương thích** | Hoàn toàn miễn phí (GPL), chạy ổn định trên mọi hệ điều hành (Windows, Linux, macOS). | Trình cài đặt trên Windows đôi khi đi kèm các phần mềm quảng cáo (Bundleware) nếu không chú ý khi cài. |
| **Quản trị Máy chủ** | Thay đổi quyền tệp tin (CHMOD) dễ dàng. Site Manager cực kỳ mạnh mẽ. | Trình chỉnh sửa văn bản tích hợp không mạnh bằng tính năng "Edit on the fly" của Total Commander (cần dựa vào app ngoài). |
| **Độ tin cậy của Dữ liệu** | Quản lý hàng đợi xuất sắc, cho phép lọc lại các file lỗi để tải lại. Tính năng Resume hoạt động hoàn hảo. | Việc quản lý mật khẩu trong bản miễn phí từng lưu dưới dạng plain-text (hiện tại đã được cải thiện với tính năng Master Password nhưng cần người dùng tự kích hoạt). |

---


2.  Tích hợp với các công cụ quản lý file

2.1 TÍNH NĂNG FTP/SFTP TRÊN TOTAL COMMANDER

**1. Tổng quan về Client FTP Tích hợp**
Total Commander không chỉ là trình quản lý tệp tin cục bộ mà còn tích hợp sẵn một trình khách (Client) FTP/FTPS mạnh mẽ. Bằng cách tận dụng triết lý giao diện hai cửa sổ, người dùng có thể đặt một bên là thư mục máy chủ (Remote) và một bên là thư mục máy tính cá nhân (Local), biến các thao tác truyền tải dữ liệu mạng trở nên trực quan và nhanh chóng như đang thao tác trên cùng một ổ cứng. Đối với các kỹ sư hệ thống, điều này giúp loại bỏ sự phụ thuộc vào các phần mềm bên thứ ba như FileZilla hay WinSCP.

---

**2. Các tính năng cốt lõi trong giao thức FTP**

* **Quản lý danh mục kết nối (FTP Connect):** Tổ chức hàng tá kết nối đến các máy chủ khác nhau. Mật khẩu của các phiên đăng nhập được mã hóa an toàn bằng một Mật khẩu chủ (Master Password), ngăn chặn nguy cơ rò rỉ thông tin đăng nhập khi tệp cấu hình của Total Commander bị đánh cắp.
* **Chỉnh sửa cấu hình trực tiếp (Edit on the fly):** Khi nhấn phím `F4` (Edit) vào một tệp trên máy chủ, phần mềm sẽ tự động tải tệp đó vào thư mục Temp, mở bằng trình soạn thảo mã nguồn mặc định (như Notepad++). Ngay khi người dùng nhấn `Ctrl+S` để lưu, Total Commander sẽ phát hiện sự thay đổi và tự động tải (upload) tệp cấu hình mới đè lên máy chủ. Tính năng này tiết kiệm rất nhiều thời gian khi cần tinh chỉnh liên tục các file cấu hình dịch vụ web như Nginx, Apache hoặc các file định tuyến HAProxy.
* **Thay đổi quyền hạn (CHMOD):** Hỗ trợ thay đổi quyền tệp tin/thư mục trực tiếp trên các máy chủ Linux/Unix thông qua giao diện đồ họa hoặc câu lệnh (ví dụ: cấp quyền 755 hoặc 644) mà không cần phải mở Terminal gõ lệnh thủ công.
* **Truyền tải chạy ngầm (Background Transfer Manager):** Khi cần chuyển các tệp log dung lượng lớn hoặc sao lưu cơ sở dữ liệu, người dùng có thể đẩy tiến trình này xuống chạy ngầm (Background). Công cụ này cho phép thiết lập hàng đợi (Queue), tạm dừng/tiếp tục (Pause/Resume) khi mạng chập chờn, và giới hạn băng thông để không làm nghẽn đường truyền mạng nội bộ.

---

**3. Khả năng Mở rộng Bảo mật (FTPS & SFTP)**

Trong môi trường vận hành thực tế, FTP truyền thống (truyền bản rõ - plaintext) không còn đáp ứng tiêu chuẩn bảo mật. Total Commander giải quyết bài toán này qua hai phương thức:

* **FTPS (FTP over SSL/TLS):** Hỗ trợ sẵn (Native). Total Commander có thể kết nối với các máy chủ cấu hình FTP mã hóa bảo mật nhờ việc liên kết với các thư viện OpenSSL (`libeay32.dll` và `ssleay32.dll` hoặc phiên bản mới hơn).
* **SFTP (SSH File Transfer Protocol):** Không hỗ trợ mặc định nhưng được tích hợp hoàn hảo thông qua Plugin **SFTP (WFX plugin)**. Tính năng này tận dụng giao thức SSH (cổng 22), biến Total Commander thành một công cụ quản lý file trực tiếp trên mọi máy chủ Linux (Ubuntu, CentOS, Kali...) một cách an toàn tuyệt đối. Hỗ trợ đăng nhập bằng cả mật khẩu và private key (như tệp `.pem` hoặc `.ppk`).

---

**4. Tính năng Nâng cao cho Mạng máy chủ**

* **Chuyển tiếp máy chủ - máy chủ (FXP):** Nếu cấu hình máy chủ cho phép, Total Commander hỗ trợ chuẩn FXP, cho phép copy dữ liệu trực tiếp từ Server A sang Server B mà không cần tải dữ liệu trung gian qua máy tính người dùng.
* **Giữ kết nối (Keep-Alive):** Tự động gửi các gói tin rỗng (dummy packets) theo chu kỳ để ngăn chặn các thiết bị Firewall hoặc Router cắt đứt phiên FTP do trạng thái chờ (Idle) quá lâu.

---

**5. Đánh giá: Total Commander vs. Client FTP Chuyên dụng**

| Tiêu chí | Total Commander (Tích hợp) | FileZilla / WinSCP (Chuyên dụng) |
| --- | --- | --- |
| **Luồng công việc (Workflow)** | Tối ưu hóa tuyệt đối. Vừa quản lý file nội bộ, giải nén, vừa thao tác FTP trên cùng 1 phần mềm. | Thường phải chuyển đổi qua lại giữa trình quản lý file của Windows và ứng dụng FTP. |
| **Sửa đổi tệp tin tức thì** | Vượt trội nhờ cơ chế phát hiện thay đổi và tự động upload tức thì. | Có hỗ trợ nhưng độ phản hồi thao tác chuỗi (mở -> sửa -> lưu) kém mượt mà hơn. |
| **Thiết lập SFTP ban đầu** | Yêu cầu tải thêm Plugin và thư viện DLL, thao tác cấu hình ban đầu mang tính kỹ thuật. | Hỗ trợ Native, sử dụng được ngay sau khi cài đặt. |




9.  Các công cụ và giao thức liên quan
• FTP client phổ biến: FileZilla, WinSCP, Cyberduck.
• Tích hợp với các công cụ quản lý file (Windows Explorer, Total Commander).
• So sánh với các dịch vụ lưu trữ đám mây (Google Drive, Dropbox).
10. Khắc phục sự cố thường gặp
• Lỗi kết nối: Kiểm tra firewall, NAT, và cấu hình cổng.
• Lỗi quyền truy cập: Xem lại quyền thư mục và tài khoản người dùng.
• Vấn đề hiệu suất: Kiểm tra băng thông, cấu hình Active/Passive.
11. Thực hành
11.1 Cài đặt FTP Server trên Ubuntu vsftpd.
•  Kết nối từ client bằng FileZilla/WinSCP.
•  Thiết lập chế độ passive mode.
11.2  Cài đặt FTP Server trên Windows.
•  Kết nối từ client bằng FileZilla/WinSCP.
•  Thiết lập chế độ passive mode.
