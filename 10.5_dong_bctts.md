1. Tổng quan về Cloudflare
1.1. Khái niệm cơ bản
Cloudflare là một dịch vụ cung cấp mạng lưới phân phối nội dung (CDN - Content Delivery Network), quản lý phân giải tên miền (DNS) và các giải pháp bảo mật internet toàn cầu. Về mặt kiến trúc hệ thống, Cloudflare hoạt động như một Reverse Proxy (máy chủ proxy ngược) đứng trung gian giữa người dùng cuối (Client) và máy chủ gốc (Origin Server - VPS/Hosting).

Thay vì người dùng kết nối trực tiếp đến địa chỉ IP của máy chủ gốc, mọi truy vấn dữ liệu sẽ được định tuyến đi qua hệ thống máy chủ của Cloudflare.

1.2. Các chức năng và vai trò cốt lõi
Trong mô hình triển khai ứng dụng web thực tế, việc tích hợp Cloudflare đóng vai trò giải quyết các bài toán về hiệu năng và bảo mật hệ thống:

Mạng phân phối nội dung (CDN) và Bộ nhớ đệm (Caching): Cloudflare sở hữu mạng lưới trung tâm dữ liệu toàn cầu. Hệ thống sẽ tự động lưu trữ các tệp tin tĩnh (hình ảnh, CSS, JavaScript) tại các điểm máy chủ (Edge Server). Khi có luồng truy cập, nội dung sẽ được trả về từ máy chủ gần với người dùng nhất, giúp giảm đáng kể độ trễ (latency) và giảm tải băng thông cho máy chủ gốc.

Bảo mật và chống tấn công DDoS: Bằng cách ẩn IP thực của máy chủ gốc, hệ thống ngăn chặn các nỗ lực rà quét lỗ hổng trực tiếp. Đồng thời, Cloudflare được tích hợp Tường lửa ứng dụng web (WAF) giúp lọc các lưu lượng truy cập độc hại, ngăn chặn botnet và giảm thiểu tác động của các cuộc tấn công từ chối dịch vụ phân tán (DDoS).

Quản lý phân giải tên miền (DNS): DNS của Cloudflare được đánh giá là một trong những hệ thống phân giải tên miền nhanh nhất thế giới. Việc thay đổi bản ghi DNS được cập nhật gần như ngay lập tức thay vì phải chờ đợi lâu như các hệ thống DNS truyền thống.

Mã hóa dữ liệu (SSL/TLS): Hệ thống cung cấp chứng chỉ SSL linh hoạt, giúp toàn bộ dữ liệu trao đổi giữa người dùng và máy chủ được mã hóa (HTTPS) một cách tự động mà không đòi hỏi các bước cấu hình phức tạp trên máy chủ vật lý.

1.3. Lợi ích khi triển khai hệ thống
Việc triển khai website kết hợp với hệ thống quản trị DirectAdmin và Cloudflare mang lại mô hình quản lý tối ưu: DirectAdmin đảm nhiệm vai trò quản trị tài nguyên và môi trường thực thi cục bộ (Local), trong khi Cloudflare đảm nhiệm vai trò tối ưu hóa đường truyền, định tuyến lưu lượng và bảo vệ vành đai an ninh mạng (Perimeter Security) cho toàn bộ ứng dụng.

Cấu hình:
tạo trên DA
 tạo domain
 <img width="1206" height="298" alt="image" src="https://github.com/user-attachments/assets/22761d51-e7fd-4f9b-93b4-c44fb77b59e6" />

add source
<img width="1877" height="316" alt="image" src="https://github.com/user-attachments/assets/55736c90-1f60-4d51-8cf7-fe73a72517dc" />


them A va c name
<img width="1540" height="690" alt="image" src="https://github.com/user-attachments/assets/bc4eb001-2ff7-4fce-a43c-15f0a062ec27" />


Bước 1: Thêm Website vào CloudflareĐăng nhập vào Cloudflare, chọn Add a Site.Nhập tên miền của bạn: tdong41.id.vn.Chọn gói Free (kéo xuống dưới cùng sẽ thấy).Cloudflare sẽ quét các bản ghi hiện có. Nếu trống trơn cũng không sao, bấm tiếp tục.Bước 2: Thay đổi Nameservers (Quan trọng nhất)Cloudflare sẽ đưa cho bạn 2 cái tên (ví dụ: abby.ns.cloudflare.com và ben.ns.cloudflare.com).Bạn đăng nhập vào trang quản lý tên miền (nơi bạn mua tên miền .id.vn).Tìm mục Thay đổi Nameservers (hoặc Máy chủ tên miền).Xóa các NS cũ đi và dán 2 cái của Cloudflare vào.Lưu lại. (Lưu ý: Bước này có thể mất từ 5-30 phút để "thông" mạng).Bước 3: Cấu hình bản ghi DNS trên CloudflareTrong giao diện Cloudflare, vào mục DNS > Records và thêm 2 bản ghi sau để dẫn web về VPS:TypeName (Host)Content (IP Address)Proxy statusA@Địa-chỉ-IP-VPS-của-bạnProxied (Mây cam)CNAMEwwwtdong41.id.vnProxied (Mây cam)Mẹo: Địa chỉ IP VPS bạn có thể lấy trong mail bàn giao VPS hoặc trong giao diện quản lý của nhà cung cấp.Bước 4: Cấu hình SSL để tránh lỗi "Không bảo mật"Vì bạn dùng Cloudflare, hãy vào mục SSL/TLS > Overview:Nếu VPS/DirectAdmin của bạn chưa cài SSL: Chọn chế độ Flexible.Nếu VPS đã cài Let's Encrypt: Chọn chế độ Full hoặc Full (Strict).

cau hinh auto tls ssl
<img width="1148" height="858" alt="image" src="https://github.com/user-attachments/assets/07120340-7987-4358-8112-a136ba6a431d" />


trang web đã được triển khai ,
<img width="1920" height="980" alt="image" src="https://github.com/user-attachments/assets/1c3eb398-a2dd-4647-80cd-6c269d1603dd" />

kiểm tra các bản ghi của web
<img width="1335" height="948" alt="image" src="https://github.com/user-attachments/assets/1e1ef281-dfe8-4c1c-b803-0949953b268f" />

như đã thấy, ip của vps đã được che bởi ip của cloudflare 

<img width="1153" height="862" alt="image" src="https://github.com/user-attachments/assets/ba795b7e-3df4-4ed0-a076-83da0e269ca6" />




Yêu cầu hệ thống tối thiểu để cài đặt cPanel trên Ubuntu
Để đảm bảo quá trình cài đặt và vận hành cPanel diễn ra suôn sẻ, máy chủ Ubuntu của bạn cần đáp ứng các yêu cầu tối thiểu về cấu hình.

Hệ điều hành: Ubuntu 20.04 LTS. Đây là phiên bản được cPanel hỗ trợ chính thức tại thời điểm hiện tại.
Địa chỉ IP: Yêu cầu một địa chỉ IPv4 tĩnh, công cộng.
Tên miền đầy đủ: Máy chủ cần được đặt một tên miền có cấu trúc hoàn chỉnh như server.yourdomain.com. FQDN là bắt buộc để các dịch vụ như email, FTP và đặc biệt là chứng chỉ SSL/TLS của server hoạt động chính xác.
CPU: Khuyến khích là 2GHz.
RAM: Khuyến khích 4GB.
Dung lượng đĩa: Tối thiểu 20GB.
Architecture: 64-bit.
Yêu cầu hệ thống tối thiểu để cài đặt cPanel trên Ubuntu
Yêu cầu hệ thống tối thiểu để cài đặt cPanel trên Ubuntu


Chuẩn bị máy chủ Ubuntu trước khi cài đặt cPanel
Các bước chuẩn bị máy chủ Ubuntu
Đăng nhập vào Server
Cập nhật hệ thống
Thiết lập Hostname (FQDN)
Quản lý Firewall
Cài đặt screen (Khuyến nghị)
Trước khi chạy script cài đặt, bạn cần thực hiện một vài bước chuẩn bị quan trọng để đảm bảo quá trình diễn ra thuận lợi.

Đăng nhập vào Server
Sử dụng SSH với quyền root:

ssh root@your_server_ip
Cập nhật hệ thống
Đây là bước cực kỳ quan trọng để vá các lỗ hổng bảo mật và đảm bảo các gói phần mềm trên hệ điều hành là mới nhất, tránh xung đột khi cài đặt.

Copy
sudo apt update && sudo apt upgrade -y
Thiết lập Hostname (FQDN)
Bạn cần đảm bảo hostname của server được đặt đúng theo định dạng FQDN và kiểm tra hostname hiện tại:

Copy
hostname -f
Nếu chưa đúng, đặt lại bằng lệnh sau (thay server.yourdomain.com bằng FQDN của bạn):
Copy
hostnamectl set-hostname whm.test.com
Quản lý Firewall
Firewall mặc định của Ubuntu là UFW (Uncomplicated Firewall) có thể chặn các cổng mà cPanel cần để cài đặt và hoạt động. Cách đơn giản nhất nhưng mang tính chất tạm thời là vô hiệu hóa UFW.

Copy
sudo ufw disable
icon
Lưu ý

Cài đặt screen (khuyến nghị)
Quá trình cài đặt cPanel mất khá nhiều thời gian. Việc sử dụng screen sẽ tạo ra một phiên làm việc độc lập, giúp tiến trình cài đặt không bị ngắt quãng ngay cả khi kết nối mạng của bạn bị gián đoạn. Cài đặt screen:

Copy
sudo apt install screen -y
Bắt đầu một phiên screen:
Copy
screen
Các bước cài đặt cPanel/WHM trên Ubuntu chi tiết
Sau khi đã hoàn tất các bước chuẩn bị, bạn có thể tiến hành cài đặt cPanel.

Bắt đầu phiên làm việc an toàn: Nếu bạn đã làm theo khuyến nghị ở trên, hãy đảm bảo bạn đang ở trong một phiên screen.
Chuyển đến thư mục /home: Đây là thư mục được khuyến nghị để tải về script cài đặt.
Copy
cd /home
Tải về script cài đặt mới nhất: Sử dụng lệnh curl để tải script chính thức từ cPanel.
Copy
curl -o latest -L https://securedownloads.cpanel.net/latest

<img width="1105" height="222" alt="image" src="https://github.com/user-attachments/assets/89fb889d-7a5e-4a4b-b453-01e74a4c9054" />

Tải về script cài đặt mới nhất
Chạy script cài đặt: Thực thi script vừa tải về.
Copy
sh latest
Chạy script cài đặt

<img width="1523" height="699" alt="image" src="https://github.com/user-attachments/assets/ec48e3a5-7877-459a-be2d-eebb8226fa60" />

Chạy script cài đặt
Theo dõi quá trình: Script sẽ tự động kiểm tra hệ thống, tải về và cài đặt hàng trăm gói phần mềm cần thiết. Bạn sẽ thấy rất nhiều dòng log hiển thị trên màn hình. Quá trình này có thể kéo dài từ 30 đến 60 phút hoặc hơn, tùy thuộc vào cấu hình máy chủ và tốc độ mạng. Hãy kiên nhẫn và tuyệt đối không ngắt tiến trình giữa chừng.
Kinh nghiệm Vietnix: Việc cài đặt cPanel yêu cầu kết nối mạng ổn định và tốc độ đọc/ghi ổ cứng cao để xử lý lượng lớn file nhỏ. Hạ tầng VPS mạnh mẽ với đường truyền tốc độ cao và ổ cứng NVMe hiệu năng vượt trội của Vietnix sẽ rút ngắn đáng kể thời gian cài đặt, tiết kiệm thời gian và giảm thiểu rủi ro lỗi do gián đoạn.


Khả năng xử lý siêu khủng với ổ cứng NVMe và CPU AMD

Truy cập và cấu hình cPanel/WHM lần đầu sau khi cài đặt
Khi quá trình cài đặt hoàn tất, bạn sẽ nhận được thông báo trên terminal. Bây giờ là lúc truy cập giao diện web để thực hiện các cấu hình ban đầu.

<img width="1483" height="791" alt="image" src="https://github.com/user-attachments/assets/81a4d251-88c7-4b96-9d2f-6848a84c9e39" />

Truy cập vào WHM: Mở trình duyệt web và truy cập vào địa chỉ: https://your_server_ip:2087.
Thay your_server_ip bằng địa chỉ IP của máy chủ bạn.
Trình duyệt có thể hiển thị cảnh báo bảo mật vì chứng chỉ SSL lúc này là tự ký. Bạn chỉ cần chấp nhận cảnh báo để tiếp tục.
Truy cập vào WHM
Truy cập vào WHM
Đăng nhập WHM:
<img width="943" height="615" alt="image" src="https://github.com/user-attachments/assets/884d6159-1672-44d8-9c0e-40de4ac23a39" />

Username: root
Password: Mật khẩu root của máy chủ.
Cấu hình ban đầu:
Bước 1: Đọc và đồng ý với các điều khoản dịch vụ của cPanel.
<img width="1920" height="903" alt="image" src="https://github.com/user-attachments/assets/694c14a8-2a32-4262-a540-55914922fb0b" />

Bước 2: Nhập địa chỉ email của quản trị viên và cấu hình Nameservers (ví dụ ns1.yourdomain.com và ns2.yourdomain.com).
Bước 3: Hoàn tất và bạn sẽ được chuyển đến giao diện chính của WHM.
<img width="1920" height="973" alt="image" src="https://github.com/user-attachments/assets/613d7f5a-9bc6-414e-a4fd-103eac0cd6c0" />


Hoàn tất và bạn sẽ được chuyển đến giao diện chính của WHM
Truy cập vào cPanel: Sau khi tạo một tài khoản hosting mới trong WHM, người dùng cuối có thể truy cập vào giao diện cPanel của họ qua https://your_domain.com:2083 hoặc https://your_server_ip:2083.

Một số chức năng chính của Cpanel


1. Cấu hình hệ thống & Mạng (Hạ tầng cốt lõi)
Server Configuration (Cấu hình Máy chủ): Nơi thiết lập các thông số "xương sống" của server. Ông có thể đổi mật khẩu root, tinh chỉnh giới hạn tài nguyên (RAM, CPU cho từng tiến trình), và cấu hình thời gian hệ thống.
<img width="1779" height="846" alt="image" src="https://github.com/user-attachments/assets/7096faec-b9da-4af8-8e5f-5a8f4e6ef307" />

Networking Setup (Cấu hình Mạng): Rất quan trọng. Nơi đây dùng để thay đổi Hostname của máy chủ, quản lý địa chỉ IP, và đặc biệt là thiết lập thông số Nameservers mặc định (để kết nối với các dịch vụ DNS bên ngoài như Cloudflare).

<img width="865" height="615" alt="image" src="https://github.com/user-attachments/assets/2175d65a-f59b-437a-929d-ce907d6a8133" />

Clusters (Cụm máy chủ): Dành cho hệ thống lớn. Giúp ông liên kết nhiều server lại với nhau. Ví dụ: tách riêng một máy chủ chỉ làm nhiệm vụ phân giải DNS, hoặc cấu hình đồng bộ để chạy High Availability (HA) - tính sẵn sàng cao.

2. Quản lý Dịch vụ & Bảo mật
Service Configuration (Cấu hình Dịch vụ): Nơi ông can thiệp sâu vào các dịch vụ đang chạy trên server như Web Server (Apache/Nginx/LiteSpeed), tinh chỉnh thông số PHP, cấu hình FTP server, hoặc Mail server (Exim).

Security Center (Trung tâm Bảo mật): "Lá chắn" của server. Chứa các công cụ quản lý tường lửa (như cPHulk để chống Brute Force), cấu hình độ khó của mật khẩu, thiết lập giới hạn truy cập SSH, và quản lý các chứng chỉ bảo mật SSL/TLS.

Backup (Sao lưu dữ liệu): Tính mạng của một Sysadmin nằm ở đây. Khu vực này cho phép ông lập lịch tự động sao lưu toàn bộ cấu hình server, tài khoản cPanel và database ra một nơi lưu trữ khác (như Google Drive, Amazon S3, hoặc máy chủ FTP từ xa).

3. Giám sát & Vận hành hàng ngày
Server Status (Trạng thái Máy chủ): Bảng taplo giám sát sức khỏe VPS. Ông có thể xem theo thời gian thực RAM, CPU, Disk I/O đang tiêu thụ ra sao, và đặc biệt là bảng Service Status để biết dịch vụ nào (MySQL, Apache...) đang bị "tèo" để kịp thời restart.

System Reboot (Khởi động lại): Khởi động lại server trực tiếp từ giao diện web một cách an toàn (Graceful - chờ các tiến trình xử lý xong) hoặc ép buộc (Forceful - cắt ngang lập tức).

Server Contacts (Liên hệ Máy chủ): Nơi ông điền email của mình vào để hệ thống tự động gửi cảnh báo (Alerts) mỗi khi server có biến (ổ cứng sắp đầy, RAM quá tải, dịch vụ sập).

4. Quản lý Tài khoản (Kinh doanh)
Resellers (Đại lý): Nếu ông muốn chia nhỏ con server ra cho người khác kinh doanh lại. Chức năng này cho phép cấp quyền tạo tài khoản cPanel cho các đại lý cấp dưới.

Locales: Đơn giản là cài đặt ngôn ngữ và định dạng thời gian cho giao diện hiển th



Tạo 1 tài khoản Cpanel cho người dùng 

Giai đoạn 1: Tạo Gói tài nguyên (Packages)
Ông không nên tạo tài khoản ngay mà chưa có Gói (Package). Nếu không giới hạn dung lượng, một website bị lỗi sinh ra file log khổng lồ có thể ăn hết ổ cứng và làm sập toàn bộ VPS.

Tại thanh tìm kiếm bên trái của WHM, gõ và chọn Add a Package.

Điền các thông số cơ bản cho "gói Hosting" này:

Package Name: Tên gói (Ví dụ: Goi_Co_Ban, Goi_Doanh_Nghiep).

Disk Quota (MB): Dung lượng ổ cứng cho phép (Ví dụ: 2048 cho 2GB).

Monthly Bandwidth (MB): Băng thông hàng tháng.

Max FTP Accounts / Max Email Accounts / Max Databases: Giới hạn số lượng tài khoản con, email và database họ được phép tạo (có thể để Unlimited nếu tự dùng).

Cuộn xuống dưới cùng và nhấn Add.

<img width="776" height="782" alt="image" src="https://github.com/user-attachments/assets/daf21197-3d68-456b-b424-3d17e181b0f3" />

Giai đoạn 2: Tạo tài khoản cPanel (Create a New Account)


Tại thanh tìm kiếm bên trái, gõ và chọn Create a New Account (nằm trong cụm Account Functions).

Điền thông tin vào khung Domain Information:

Domain: Tên miền chính của website (Ví dụ: tenmien.com - không cần nhập www hay http).

Username: Hệ thống sẽ tự động tạo dựa trên tên miền, hoặc ông có thể tự gõ (tối đa 16 ký tự, viết liền không dấu). Đây sẽ là tên đăng nhập của họ.

Password: Nhập mật khẩu đủ độ khó.

Email: Điền email của chủ tài khoản để hệ thống gửi thông báo khi dung lượng sắp đầy.

Chuyển xuống khung Package:

Choose a Package: Xổ menu ra và chọn cái gói Goi_Co_Ban mà ông vừa tạo ở Giai đoạn 1.

Khung DNS Settings và Mail Routing Settings:

Cứ để mặc định (Local Mail Exchanger và Enable DKIM/SPF) trừ khi ông dùng hệ thống mail server riêng biệt.

<img width="748" height="795" alt="image" src="https://github.com/user-attachments/assets/a38c3094-1599-4796-8d42-6b127e396df7" />

Nhấn nút Create màu xanh ở dưới cùn

<img width="1620" height="196" alt="image" src="https://github.com/user-attachments/assets/0ed287c3-34db-402f-a357-4b32675b2f5e" />

trang quản trị người dùng Cpanel tại port 2083, gồm các chức năng như 
<img width="1920" height="857" alt="image" src="https://github.com/user-attachments/assets/7e6bb4d4-b1c0-4c27-aba5-cb1f1cf920aa" />

Một số tool
<img width="953" height="730" alt="image" src="https://github.com/user-attachments/assets/22087602-e5ae-4400-b1ae-18af91ca46df" />

<img width="1259" height="837" alt="image" src="https://github.com/user-attachments/assets/2d730a98-9a7a-482f-a694-1451a267459a" />

<img width="1159" height="822" alt="image" src="https://github.com/user-attachments/assets/58c04429-5887-48be-a8ef-a8ef4a69fa26" />




### 1. EasyApache 4 (Tinh chỉnh Web Server & PHP)

Thay vì phải gõ lệnh cài đặt từng package qua SSH (như kiểu `apt install php8.1-fpm`), WHM cung cấp một công cụ giao diện trực quan cực mạnh gọi là **EasyApache 4** (Nằm trong phần *Software*).

* **Chức năng:** Đây là nơi ông "nhào nặn" môi trường chạy web cho toàn bộ server.
* **Ứng dụng thực tế:**
* **Cài đặt Web Server:** Ông có thể chọn chạy thuần Apache, hoặc cấu hình Nginx làm Reverse Proxy đứng trước Apache chỉ bằng vài cú click.
* **Quản lý phiên bản PHP:** Cài đặt song song nhiều bản PHP (từ 7.4 đến 8.3). Mỗi khách hàng (mỗi cPanel) có thể tự chọn chạy một phiên bản PHP khác nhau mà không bị xung đột.
* **Bật/tắt PHP Extensions:** Cần thư viện `mbstring`, `zip`, hay `memcached` cho source code? Chỉ cần vào đây gõ tên, tick chọn và nhấn *Provision* là hệ thống tự build lại.
<img width="1115" height="777" alt="image" src="https://github.com/user-attachments/assets/d971a781-f40a-4192-86e5-ecc7055eff3c" />



### 2. DNS Zone Manager (Quản lý bản ghi máy chủ)

Ở các bước trước, chúng ta đã nhắc đến Cloudflare. Nhưng nếu khách hàng của ông không dùng Cloudflare mà muốn trỏ IP trực tiếp về server, thì ông phải quản lý DNS của họ tại đây.

* Nằm trong mục **DNS Functions**.
* Khi ông tạo 1 tài khoản cPanel mới, hệ thống tự động sinh ra một file "DNS Zone" cho tên miền đó.
* **Thao tác:** Ông vào **DNS Zone Manager**, chọn tên miền và nhấn *Manage*. Tại đây, ông có quyền tối cao trong việc thêm, sửa, xóa các bản ghi A, CNAME, MX, TXT cho tên miền đó (giống y hệt giao diện cấu hình DNS trên Cloudflare hay các nhà cung cấp tên miền).
<img width="1408" height="353" alt="image" src="https://github.com/user-attachments/assets/c453cc61-68e3-40e7-9640-73df6c5792d3" />


### 3. Tường lửa và Chống Brute Force (Security)

Đã đưa server ra internet là chắc chắn sẽ bị botnet cào quét liên tục. Trong WHM có 2 "bảo vệ" ông phải biết:

* **cPHulk Brute Force Protection (Có sẵn trong Security Center):** * Công cụ này theo dõi các lượt đăng nhập sai mật khẩu vào cPanel, SSH, FTP, hoặc Email.
* Nếu một IP nào đó gõ sai pass quá 5 lần trong 5 phút (ông có thể tự set mức này), cPHulk sẽ block thẳng cổ IP đó, thậm chí block luôn tài khoản đang bị nhắm tới để an toàn.
<img width="820" height="790" alt="image" src="https://github.com/user-attachments/assets/5186014f-dcaa-4f44-bbfc-6cc91c77d8cb" />


* **ConfigServer Security & Firewall - CSF (Plugin cài thêm):** * WHM mặc định khá yếu về quản lý port tĩnh. Hầu hết các sysadmin đều cài thêm CSF. Nó cung cấp giao diện để ông đóng/mở Port (TCP/UDP), chặn IP theo quốc gia, và phát hiện các tiến trình chạy ngầm đáng ngờ. Nếu ông đã quen với các dòng firewall chuyên dụng thì cấu hình CSF sẽ rất vào tay.

### 4. Backup Configuration (Đường lùi sinh tử)

Đừng bao giờ để server chạy mà không có backup tự động. Một thao tác xóa nhầm qua SSH cũng có thể đi tong dữ liệu của hàng chục khách hàng.

* Nằm trong mục **Backup**.
* **Scheduling (Lên lịch):** Ông có thể hẹn giờ backup tự động vào lúc 2h sáng (lúc ít người truy cập nhất), có thể chạy hàng ngày, hàng tuần hoặc hàng tháng.
* **Retention (Giữ lại):** Cài đặt lưu tối đa bao nhiêu bản backup để không bị đầy ổ cứng (ví dụ: chỉ giữ 3 bản gần nhất, có bản mới thì xóa bản cũ).
* **Destination (Nơi lưu trữ):** Không nên lưu backup trên cùng con VPS đang chạy. WHM hỗ trợ đẩy thẳng file backup (qua FTP, rsync, Amazon S3, Google Drive...) sang một con server khác. Nếu VPS chính bị "chết" ổ cứng, ông vẫn có file để khôi phục.

<img width="1075" height="791" alt="image" src="https://github.com/user-attachments/assets/85331d93-4c6c-4e79-af2a-ae6376916317" />



